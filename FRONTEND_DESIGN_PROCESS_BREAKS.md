# 前端交互设计审查报告

> 检查对象: `FRONTEND_INTERACTION_DESIGN.md`（含 v1.4 变更记录）  
> 交叉依据: `API_AUTOMATION_SPEC.md` v1.3 / `DEVELOPMENT_PLAN.md` v0.2 / Quiet Console 设计规范  
> 检查范围: 流程闭环、执行可观测性、信息架构、权限与安全、危险操作、深链、响应式与设计系统一致性  
> 日期: 2026-08-02

---

## 零、修复状态（对照文档 v1.5）

> 本报告所有问题已在本轮修订中处理，交互文档已升级至 **v1.5**（`基于: API_AUTOMATION_SPEC.md v1.3`）。以下为逐条修复落点，供验收核对。
>
> 后续迭代：文档于 **v1.6** 进一步将「接口用例」并入「接口管理」（接口工作台全屏化、侧栏 `[调试记录 | 用例]` 分段、用例完全快照隔离、跨接口用例选择器），相关章节已随文档同步更新，不在上表逐条跟踪范围内。

| 问题 | 优先级 | 修复位置 |
| --- | --- | --- |
| 1. 定时调度只支持流程 | 高 | 3.7：目标对象两步选择（流程/测试套件/CI 任务）、列表补目标类型/下次执行/失败原因、资源内快捷创建 |
| 2. 测试套件缺少完整闭环 | 高 | 3.13：编辑器（成员选择/去重/动态命中固化语义/失效子项）、执行策略校验、执行摘要、执行详情与仅重跑失败项 |
| 3. 复杂结果强制 520px 抽屉 | 高 | 1.4：两段式结果模型（就地摘要 + 用户主动进入完整详情）；宽度响应式 |
| 4. 长任务执行生命周期 | 高 | 1.4 统一状态机（queued/running/cancelling/cancelled/succeeded/failed/timed_out）+ 取消语义；3.12.3 CI 补充取消/失联处理 |
| 5. 重新执行替换历史身份 | 高 | 1.4「重跑身份不可变」+ A/B 对比；3.6.1 同步改写 |
| 6. Secret 查看/变量预览无权限边界 | 高 | 4.6 Secret 权限边界；3.2/3.9/4.1 揭示权限；3.1.2 hover 不泄密 |
| 7. 删除缺依赖检查与历史策略 | 高 | 4.4 危险操作统一规则；3.0.3 项目删除、3.1.1 批量删除、3.2 环境删除 |
| 8. 保存并执行缺异常路径 | 高 | 4.5 两阶段反馈/幂等/版本冲突；3.1.2「保存并测试」 |
| 9. 导入冲突规则未同步 | 中 | 3.1.4：冲突检测（method+URL）、新增/已存在/变更、字段级 diff、全局+逐项策略、逐项结果 |
| 10. 项目切换保留模块路径不可靠 | 中 | 6.1：仅保留可访问的模块级路径、回退概览、离开确认 |
| 11. 调度失败跳转编辑页 | 中 | 3.7 + 6.2 d：携带 ExecutionIndex 进统一执行入口，次级入口查看调度配置 |
| 12. 看板指标下钻不完整 | 中 | 3.0.2：逐项口径/无样本/下钻 URL，保留筛选上下文 |
| 13. URL 保存批量选中行 | 中 | 5.1 + 4.2：批量选中为会话级状态，不进 URL |
| 14. 会话过期丢失编辑内容 | 中 | 6.1：保存 canonical URL + 非敏感草稿，恢复并提示；敏感字段不持久化 |
| 15. 导航展示未交付模块 | 中 | 1.2 权限可见性补充：导航只展示已启用且角色可访问能力 |
| 16. 空状态未区分类型 | 中 | 9.3：五类空状态（首次/筛选/无权限/未启用/失败）+ 创建按钮显隐规则 |
| 17. 响应式与窄屏策略缺失 | 中 | 9.4：940/700/560/420 断点、抽屉全屏、表格列裁剪、复杂编辑器窄屏策略 |
| 18. 内容区 1440px 居中冲突 | 中 | 1.1：Shell 全宽，阅读宽度下沉到内容子元素 |
| 19. antd 使用边界冲突 | 中 | 2 技术栈：保留 Modal/Drawer/Form/Select/message，弃用 Table/Card/Statistic/Tag |
| 20. 持续动画超动效预算 | 低 | 1.4 + 3.5.3 + 9.2：仅一个呼吸点、稳定骨架、prefers-reduced-motion |

---

## 一、结论摘要

当前交互文档已覆盖平台主要页面、执行结果就地呈现和全站深链，但仍存在 6 类问题：

1. **流程断裂**：定时调度、测试套件、长任务取消等能力未形成完整闭环。
2. **执行模型不一致**：简单接口与复杂流程、套件、CI 任务被要求共用同一种结果抽屉，重跑又会替换当前历史记录身份。
3. **权限与安全缺口**：Secret 查看、变量预览、资源删除和批量删除缺少细粒度权限及依赖检查。
4. **导航与深链问题**：项目切换、统计下钻、调度失败跳转和 URL 状态边界不够合理。
5. **异常状态缺失**：会话过期、保存后执行失败、版本冲突、取消执行和部分成功没有统一语义。
6. **实现规范冲突**：固定宽度、内容区居中、Ant Design 组件范围和持续动画与现有 Quiet Console 规范不一致。

其中包含 8 项高优先级、11 项中优先级和 3 项低优先级问题。建议先完成高优先级闭环，再同步处理跨文档和设计系统问题。

---

## 二、检查基线问题

### 1. 文档版本标识不一致

**类型**：文档治理  
**优先级**：低

**依据**

- `FRONTEND_INTERACTION_DESIGN.md` 文档头仍标记为 v1.3。
- 同一文档的变更记录已经包含 v1.4。
- 文档仍声明基于 `API_AUTOMATION_SPEC.md` v1.2，但产品规格已经更新到 v1.3。

**影响**

- 无法确定评审、开发和验收应采用哪个版本。
- 已写入产品规格的规则可能在交互实现时被遗漏。

**建议**

- 将交互文档头升级为 v1.4，并将依据更新为 Spec v1.3。
- 后续每次更新同时修改文档头、变更记录和引用版本。

### 2. 原报告检查基线已过期

**类型**：文档治理  
**优先级**：低

原报告以交互文档 v1.2 为检查对象，而当前交互文档已经加入 v1.3、v1.4 规则。本报告改为检查当前完整文档，并通过产品规格和开发计划判断问题究竟是产品能力缺失，还是文档之间尚未同步。

---

## 三、高优先级问题

### 1. 定时调度只支持流程，与其他可执行资产冲突

**类型**：流程断裂 / 跨文档冲突  
**优先级**：高

**依据**

交互文档 3.7 节的 Cron 配置器只有：

```text
目标流程: [订单处理流程 ▼]
```

但平台还存在可独立执行的接口用例、场景、测试套件和 CI 任务。产品规格进一步明确：

- 测试套件支持手动、定时调度、Webhook/API 和 CI 回调。
- CI 任务由平台 Scheduler 统一驱动。

**影响**

- 测试套件和 CI 任务无法按产品规格创建定时任务。
- 用户可能被迫把其他资产包装成流程，增加不必要的学习和维护成本。
- 仓库模式的 CI 任务没有定时入口，与“两种模式相互独立”的定位不符。

**建议**

- 将“目标流程”改为“目标对象”，第一步选择资源类型，第二步选择具体资源。
- 至少支持流程、测试套件和 CI 任务；接口用例、场景是否支持直接调度，应先在产品规格中统一。
- 在资源详情内提供“创建定时任务”快捷入口，反向写入统一调度模块。
- 调度列表增加目标类型、目标名称、下次执行时间、上次结果和触发失败原因。

### 2. 测试套件缺少完整创建、执行和排障闭环

**类型**：流程断裂  
**优先级**：高

**依据**

交互文档 3.13 节只有套件列表和两句规则说明；路由却已经声明：

```text
test-suites/:id
test-suites/:id/executions/:execId
```

**缺失内容**

- 手动选择用例/场景的选择器和去重规则。
- 按标签动态命中的实时预览，以及执行时还是保存时固化成员。
- 串行/并行、并发度、fail-fast 的配置和校验。
- 子项被删除、归档、禁用或无权限时的处理。
- 执行摘要、子项结果、失败原因、筛选、重跑和导出交互。
- “上次结果”点击后如何打开该次执行。

**影响**

- 用户只能创建概念上的套件，无法判断实际会执行哪些内容。
- 执行失败后无法定位失败子项，也无法区分失败、跳过和未执行。

**建议**

- 补充套件编辑器：基本信息、组成方式、成员预览、执行策略和触发方式。
- 动态套件执行前展示最终命中清单及相对上次的变化。
- 补充套件执行详情：总数、通过、失败、跳过、取消、耗时、触发方式和环境。
- 子项列表支持按结果筛选、查看请求快照、跳回源用例、仅重跑失败项。
- 列表“上次结果”和执行后的摘要抽屉都可进入完整执行详情。

### 3. 复杂执行结果不应强制塞入统一 520px 抽屉

**类型**：交互模型冲突  
**优先级**：高

**依据**

1.4 节要求接口、用例、套件、场景、流程和 CI 任务统一在当前页通过 520px 结果抽屉呈现。与此同时：

- 流程需要执行时间线、节点详情和画布双向联动。
- 套件和场景可能包含大量子项。
- CI 执行需要阶段进度、持续日志、用例表和产物下载。
- 文档已为 CI 单独定义完整执行详情页和稳定路由。

**影响**

- 复杂结果在窄抽屉内信息密度过高，日志、表格和节点详情难以阅读。
- “结果必须就地可见”和“复杂详情需要完整页面”被错误地当成二选一。

**建议**

- 保留“执行后不自动跳页”的原则。
- 单接口和简单用例使用完整结果抽屉。
- 场景、套件、流程和 CI 在当前页先展示摘要抽屉或面板，包含状态、进度、关键失败和少量子项。
- 摘要提供明确的“打开完整详情”，由用户主动进入稳定详情路由。
- 抽屉宽度使用响应式约束，不写死为所有资源统一 520px。

### 4. 长任务执行生命周期不完整

**类型**：流程断裂 / 状态模型缺失  
**优先级**：高

**依据**

CI 执行详情提供“取消执行”，但场景、流程和测试套件只描述等待、执行中、成功、失败，没有排队取消、运行取消、取消中、超时和部分结果语义。

**影响**

- 用户无法停止误触发、参数错误或长时间阻塞的任务。
- fail-fast、主动取消和系统超时可能在 UI 中都表现成失败，导致报告口径错误。

**建议**

- 统一执行状态：`queued / running / cancelling / cancelled / succeeded / failed / timed_out`。
- 排队中允许立即取消；运行中取消需确认，并说明已发出的 HTTP 请求无法回滚。
- 取消后保留已完成节点和子项结果，未开始项标记为“取消”而非“失败”。
- 明确哪些角色可取消，以及取消请求失败、Worker 失联时的反馈。

### 5. 重新执行会替换历史记录身份

**类型**：审计与深链冲突  
**优先级**：高

**依据**

3.6.1 节规定重跑生成新的 `ExecutionIndex`，同时原地把当前报告标题和执行编号更新成新记录。

**影响**

- 用户打开历史执行 A 后点击重跑，当前页面会悄然变为执行 B。
- 原链接、浏览器历史、分享语义和审计定位变得模糊。
- 用户无法直接对比重跑前后的差异。

**建议**

- 原执行记录保持不可变。
- 可以在原面板中跟踪新执行，但必须明确显示“由 #A 重新执行，生成 #B”。
- 新执行创建后将 URL 显式切换到 B，或保留 A 页面并提供“查看新执行 B”。
- 提供“返回原执行”和“对比 A/B”入口，不用导出 JSON 代替基本对比流程。

### 6. Secret 查看和变量预览缺少权限边界

**类型**：安全与权限  
**优先级**：高

**依据**

- 环境 secret 和数据源密码支持眼睛按钮直接显示。
- 接口编辑器规定变量引用 hover 展示变量值预览。
- 文档未定义 secret 查看权限、重新认证、审计、自动隐藏和复制限制。

**影响**

- 普通开发者或屏幕旁观者可能看到生产凭据。
- Secret 可能通过 hover、截图或浏览器状态意外泄露。

**建议**

- 普通用户仅看到“已配置”和掩码，变量 hover 对 secret 永不显示真实值。
- 只有具备独立 secret 权限的管理员可执行短时揭示，并要求重新认证。
- 查看、复制和更新 secret 均写入审计日志，揭示后自动恢复掩码。
- 页面缓存、URL、日志、执行快照和本地草稿不得保存 secret 明文。

### 7. 删除操作缺少依赖检查和历史策略

**类型**：危险操作  
**优先级**：高

**依据**

- 环境 Tab 可直接删除，但环境可能被用例、场景、流程、调度和项目默认设置引用。
- 接口支持单项和批量删除，但接口可能被用例、流程节点、Mock 和仓库用例关联。
- 项目删除只定义二次确认，没有说明执行历史、日志、Token、凭据和报告如何处理。

**影响**

- 删除后可能产生失效配置、悬空引用或无法解释的历史报告。
- 批量删除可能只完成一部分，却没有逐项结果说明。

**建议**

- 所有删除先执行依赖扫描，展示引用类型、数量和受影响资源。
- 被引用资源默认禁止硬删除，优先归档或要求先迁移引用。
- 历史执行始终保留资源名称和环境快照，不依赖当前资源继续存在。
- 项目永久删除需展示资源统计、数据保留期和不可恢复说明，并输入项目名确认。
- 批量删除结果分为“可删除、被引用、无权限、失败”，允许只提交可安全删除项。

### 8. 保存并执行缺少两阶段结果和并发编辑冲突

**类型**：异常流程缺失  
**优先级**：高

**依据**

接口、用例、场景和 CI 任务都提供“保存并执行”，但仅描述正常路径，没有定义保存成功而执行创建失败、订阅断开、重复点击和多人同时编辑的处理。

**影响**

- 用户可能误以为执行失败意味着内容也没有保存，或反过来。
- 后保存者可能无提示覆盖其他人的修改。

**建议**

- 把操作反馈拆成“保存结果”和“执行结果”两个明确阶段。
- 保存成功但执行失败时保留已保存状态，并提供重试执行，不重复保存。
- 使用版本号或 ETag 检测并发修改，冲突时提供重新加载、对比和另存副本。
- 提交按钮在请求期间禁用，触发执行携带幂等键。

---

## 四、中优先级问题

### 9. 导入冲突规则已存在，但交互文档未同步

**类型**：跨文档不一致  
**优先级**：中

**依据**

交互文档 3.1.4 节的导入预览只有勾选、接口名称、方法、路径和目标分类。产品规格及开发计划已经定义：

- 以 `method + 标准化 URL` 检测冲突。
- 标示新增、已存在和变更。
- 支持跳过、更新、另建和逐项处理。

**建议**

- 预览表增加检测结果、差异摘要和处理策略列。
- 提供全局策略，并允许单项覆盖。
- “更新”前显示字段级 diff；“另建”要求新的名称或版本标识。
- 导入完成后显示新增、更新、跳过、另建和失败的逐项结果，可重试失败项。

### 10. 项目切换时无条件保留模块路径不可靠

**类型**：导航断裂  
**优先级**：中

**依据**

路由规则要求切换项目时保留当前模块路径，但目标项目可能没有仓库、模块未启用、用户角色不同，具体资源 ID 也不可能跨项目复用。

**建议**

- 只保留目标项目中可访问的模块级路径，不保留资源 ID、编辑态和执行 ID。
- 目标模块不可访问时回退项目概览，并说明“该项目未启用此模块”或“当前角色无权限”。
- 切换前存在未保存内容时先触发离开确认。

### 11. 调度失败告警跳到了编辑配置，而不是失败执行

**类型**：排障流程断裂  
**优先级**：中

**依据**

统计跳转约定将“调度失败”链接到 `/schedules?edit=:scheduleId`。用户收到失败告警后的首要任务通常是查看该次执行错误，而不是修改 Cron。

**建议**

- 调度失败通知携带对应 `ExecutionIndex`，优先进入统一执行详情。
- 执行详情展示触发来源和调度名称，并提供“查看调度配置”次级入口。
- 只有调度本身无法触发时，才直接进入调度诊断页。

### 12. 全局看板指标下钻规则不完整

**类型**：信息架构  
**优先级**：中

**依据**

文档只笼统规定“项目指标进入项目内资源页”，没有逐项定义项目数、接口数、通过率、完成率、覆盖率、用例数和任务数的目标页面及筛选参数。

**建议**

- 为每个指标定义计算口径、无样本状态、目标 URL 和携带参数。
- 下钻必须保留看板的项目、标签和时间范围。
- 指标未启用时不可点击；部分数据接入时明确覆盖范围。

### 13. URL 不应保存批量选中的行

**类型**：状态边界不合理  
**优先级**：中

**依据**

状态管理章节要求列表的筛选、分页、排序和“选中”都进入 URL。

**影响**

- 大量选中 ID 会导致 URL 过长。
- 分享链接可能意外携带危险操作上下文。
- 数据变化后恢复的选中项可能已失效。

**建议**

- URL 只承载筛选、分页、排序、当前资源和抽屉状态。
- 批量选择保留为页面会话状态；筛选变化或离开页面时清空并提示。

### 14. 会话过期会丢失编辑内容

**类型**：异常流程缺失  
**优先级**：中

**依据**

401 由 axios 拦截器直接跳转登录页。虽然路由章节声明未保存离开确认，但没有说明鉴权跳转如何保存草稿和恢复原页面。

**建议**

- 会话过期时保存 canonical URL 和非敏感草稿，再进入登录页。
- 登录成功后恢复原页面和草稿，并提示重新提交。
- Secret、Token、密码和敏感请求头不得持久化到本地草稿。

### 15. 导航展示了大量未交付模块

**类型**：信息架构  
**优先级**：中

**依据**

项目导航同时列出接口、环境、用例、套件、场景、流程、Mock、数据源、MCP、仓库用例、CI、报告和调度。当前 P0 只交付其中少数模块，但文档只规定看板指标显示“未启用”，没有规定导航如何处理未交付能力。

**建议**

- 主导航只展示当前项目已启用且当前角色可访问的能力。
- 尚未交付的功能不要形成大量空页面，可集中到“能力中心”或完全隐藏。
- 已交付但尚未配置的模块保留入口，并用有行动指引的空状态引导配置。

### 16. 空状态没有区分权限、筛选和错误

**类型**：反馈状态缺失  
**优先级**：中

**依据**

通用空状态统一要求展示“创建”入口，但只读用户必须隐藏创建操作。文档也没有区分真正为空、筛选无结果、模块未启用和加载失败。

**建议**

- 定义五类空状态：首次为空、筛选无结果、无创建权限、模块未启用、加载失败。
- 只有具备创建权限且模块已启用时显示创建按钮。
- 筛选无结果提供“清除筛选”，加载失败提供“重试”和错误摘要。

### 17. 响应式和窄屏策略缺失

**类型**：可用性  
**优先级**：中

**依据**

交互稿大量使用固定 520px、600px、750px 抽屉和左右双栏，但没有定义断点、移动端导航、表格列优先级或 DAG 编辑器的最低宽度行为。

**建议**

- 按 Quiet Console 规范补充 940px、700px、560px、420px 断点行为。
- 窄屏抽屉切为全屏；表格保留主列和操作入口，次要字段进入行详情。
- 明确 DAG、SQL、日志等复杂编辑器在小屏是只读、简化编辑还是提示转到桌面端。
- 保证 320px 起无整页横向滚动，只有明确的代码和表格区域可内部滚动。

---

## 五、设计系统一致性问题

### 18. 内容区最大宽度规则与现有 Shell 冲突

**类型**：实现规范冲突  
**优先级**：中

交互文档要求右侧内容区域最大宽度 1440px 居中。Quiet Console 规定 Shell 和 `.content` 必须全宽，阅读宽度应限制在内容子元素上，否则宽屏会出现死区并破坏顶部边界连续性。

**建议**

- 删除“内容区域最大宽度 1440px 居中”的全局规则。
- Shell 保持全宽，按具体页面为正文、表单或详情设置阅读宽度；表格、DAG、日志等工作区使用可用全宽。

### 19. Ant Design 使用边界与现有设计规范冲突

**类型**：实现规范冲突  
**优先级**：中

交互文档技术栈将 Ant Design 用于表格、卡片、树和通知等通用 UI。Quiet Console 已明确：保留 antd 的 Modal、Drawer、Form、Select 和 message 行为能力，不使用 antd Table、Card、Statistic、Tag，以避免回到通用后台模板视觉。

**建议**

- 在技术栈章节写明 antd 边界。
- Shell、导航、表格、状态、HTTP method、指标和空状态使用项目共享原语。
- 新交互优先复用 `apitest-web/src/ui.tsx`，颜色和主题统一来自现有 token。

### 20. 持续动画与动效预算冲突

**类型**：可访问性 / 实现规范冲突  
**优先级**：低

交互文档要求流程节点脉冲动画、列表 shimmer 等持续动效。Quiet Console 只允许一个执行中呼吸点作为环境动画，并要求全局遵循 `prefers-reduced-motion`。

**建议**

- 执行状态主要通过语义色、图标、文字和阶段进度表达。
- 仅当前活动执行使用统一呼吸点，不让多个节点同时脉冲。
- 首次加载使用稳定骨架，占位不做持续扫光；减少动态模式下完全关闭非必要动画。

---

## 六、优先修复顺序

| 顺序 | 问题 | 优先级 | 建议交付物 |
| --- | --- | --- | --- |
| 1 | 测试套件完整闭环 | 高 | 编辑器、成员预览、执行详情、失败重跑 |
| 2 | 调度对象与产品规格冲突 | 高 | 多类型目标选择器、资源内快捷创建 |
| 3 | 长任务执行生命周期 | 高 | 统一状态、取消流程、部分结果语义 |
| 4 | 复杂结果载体与重跑身份 | 高 | 摘要抽屉、完整详情、执行 A/B 关系 |
| 5 | Secret 权限边界 | 高 | 掩码、二次认证、审计、自动隐藏 |
| 6 | 删除依赖与历史策略 | 高 | 依赖扫描、归档、批量结果、保留策略 |
| 7 | 保存并执行异常路径 | 高 | 两阶段反馈、幂等、版本冲突处理 |
| 8 | 导入交互同步 | 中 | 冲突状态、diff、全局和逐项策略 |
| 9 | 深链与导航修正 | 中 | 项目切换、指标下钻、告警目标、URL 边界 |
| 10 | 响应式与设计系统同步 | 中 | 断点、窄屏策略、组件边界、动效预算 |

---

## 七、验收标准

交互文档完成修订后，应满足以下条件：

- 每一种可执行资源都有创建、配置、执行、执行中、取消、结果、历史和重跑路径。
- 简单执行结果可就地完整查看，复杂执行可就地看到摘要并主动进入稳定详情页。
- 每一条历史执行都有稳定、不可变的身份和可分享 URL。
- 所有删除操作都说明依赖、权限、历史保留和部分失败处理。
- Secret 在默认交互、hover、URL、本地草稿、日志和快照中均不会泄露。
- 看板每个指标都具有明确口径、空值语义和下钻目标。
- URL 只保存需要分享或恢复的导航状态，不保存危险操作选择。
- 320px 起无整页横向滚动，复杂工作区具有明确的窄屏策略。
- 交互文档与 Spec、开发计划及 Quiet Console 的版本和规则保持一致。
