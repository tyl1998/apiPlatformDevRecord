# Nuwax 智能体平台 — 第三方接入接口文档

> 仅包含 API Key 鉴权接口，适用于第三方系统对接。
> 源码：`ChatApiController.java`（`/api/v1/chat`）

---

## 1. 基础信息

| 项目 | 值 |
|------|-----|
| 基础路径 | `/api/v1/chat` |
| 鉴权方式 | API Key |
| 协议 | HTTPS |
| 数据格式 | JSON |
| 字符编码 | UTF-8 |

### 鉴权

所有接口在请求头携带 API Key：

```
Authorization: Bearer ak-xxxxxxxxxxxxxxxxxxxxxxxx
```

- API Key 格式：`ak-xxx` 或 `ck-xxx`
- 每个 API Key 绑定一个智能体，调用时自动关联
- API Key 由平台管理员在管理后台创建

### 通用响应结构

```json
{
  "code": "0000",
  "displayCode": "0000",
  "message": "success",
  "data": {},
  "tid": "trace-id"
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `code` | String | 业务状态码，`0000` 成功 |
| `message` | String | 错误描述 |
| `data` | Object | 业务数据 |

---

## 2. 接口列表

### 2.1 创建会话

在与智能体对话前，需先创建会话。

```
POST /api/v1/chat/conversation/add
```

**请求体**

```json
{
  "agentId": 100,
  "variables": {},
  "devMode": false
}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `agentId` | Long | 是 | 智能体 ID |
| `variables` | Map | 否 | 会话变量（智能体配置了变量组件时需要） |

**响应**

```json
{
  "code": "0000",
  "message": "success",
  "data": 123
}
```

> `data` 为会话 ID（`conversationId`），后续所有接口都需要。

---

### 2.2 发送消息（SSE 流式）

核心接口。向智能体发送消息，以 SSE 流式接收回复。

```
POST /api/v1/chat/{conversationId}
Content-Type: application/json
Accept: text/event-stream
```

**请求体（简单场景）**

```json
{
  "message": "你好"
}
```

**请求体（带附件）**

```json
{
  "message": "请分析这个文件",
  "attachments": [
    {
      "fileUrl": "https://example.com/file.pdf",
      "fileName": "file.pdf",
      "mimeType": "application/pdf"
    }
  ]
}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `conversationId` | Long | 是 | 会话 ID（路径参数） |
| `message` | String | 是 | 用户消息内容 |
| `attachments` | List | 否 | 附件列表（不传附件时可省略） |
| `attachments[].fileUrl` | String | 是 | 文件 URL |
| `attachments[].fileName` | String | 否 | 文件名 |
| `attachments[].mimeType` | String | 是 | MIME 类型（如 `application/pdf`、`image/png`） |

> 其他字段（`selectedComponents`、`skillIds`、`variableParams`、`modelId` 等）为平台前端交互使用，第三方对接无需传。

**SSE 响应**

每条事件格式：

```
data: {"requestId":"xxx","eventType":"MESSAGE","data":{"text":"你"},"completed":false,"error":null}
```

| eventType | 说明 | data 结构 |
|-----------|------|-----------|
| `MESSAGE` | 文字 token 流 | `{"text": "内容", "think": "思考过程"}` |
| `PROCESSING` | 工具执行状态 | `{"type": "Knowledge", "status": "EXECUTING", "name": "名称"}` |
| `FINAL_RESULT` | 执行完成 | `{"outputText": "完整回复"}` |
| `HEART_BEAT` | 心跳保活（10s） | 忽略 |
| `ERROR` | 异常 | `{"error": "错误描述"}` |

**客户端示例**

```javascript
const response = await fetch('/api/v1/chat/123', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'Authorization': 'Bearer ak-xxx'
  },
  body: JSON.stringify({ message: '你好' })
});

const reader = response.body.getReader();
const decoder = new TextDecoder();
let fullText = '';

while (true) {
  const { done, value } = await reader.read();
  if (done) break;
  const chunk = decoder.decode(value);
  for (const line of chunk.split('\n')) {
    if (!line.startsWith('data: ')) continue;
    const event = JSON.parse(line.slice(6));
    switch (event.eventType) {
      case 'MESSAGE':
        fullText += event.data.text;  // 逐 token 追加
        break;
      case 'FINAL_RESULT':
        console.log('完成:', fullText);
        break;
      case 'HEART_BEAT':
        break;  // 忽略心跳
      case 'ERROR':
        console.error('错误:', event.error);
        break;
    }
  }
}
```

---

### 2.3 停止会话

```
POST /api/v1/chat/{conversationId}/stop
```

**响应**

```json
{ "code": "0000", "message": "success", "data": null }
```

---

### 2.4 查询历史会话列表

```
GET /api/v1/chat/conversation/list?agentId=100&lastId=&limit=10&topic=
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `agentId` | Long | 是 | 智能体 ID |
| `lastId` | Long | 否 | 分页游标（上一页最后一条 ID） |
| `limit` | Integer | 否 | 返回数量，默认 10 |
| `topic` | String | 否 | 按主题模糊搜索 |

**响应**

```json
{
  "code": "0000",
  "message": "success",
  "data": [
    {
      "id": 123,
      "agentId": 100,
      "topic": "会话主题",
      "created": "2026-01-01T00:00:00Z",
      "modified": "2026-01-01T00:00:00Z"
    }
  ]
}
```

---

### 2.5 查询会话消息列表

```
GET /api/v1/chat/{conversationId}/messages?index=&limit=10
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `conversationId` | Long | 是 | 会话 ID（路径参数） |
| `index` | Long | 否 | 分页游标（上一页最后一条 index） |
| `limit` | Integer | 否 | 返回数量，默认 10 |

**响应**

```json
{
  "code": "0000",
  "message": "success",
  "data": [
    {
      "id": "msg-uuid",
      "role": "USER",
      "type": "CHAT",
      "text": "用户消息",
      "time": "2026-01-01T00:00:00Z",
      "attachments": [],
      "finished": true
    },
    {
      "id": "msg-uuid-2",
      "role": "ASSISTANT",
      "type": "CHAT",
      "text": "AI 回复",
      "think": "思考过程",
      "time": "2026-01-01T00:00:01Z",
      "finished": true,
      "componentExecutedList": []
    }
  ]
}
```

---

### 2.6 查询会话信息

```
GET /api/v1/chat/conversation/{id}
```

**响应**

```json
{
  "code": "0000",
  "message": "success",
  "data": {
    "id": 123,
    "uid": "conversation-uuid",
    "agentId": 100,
    "topic": "会话主题",
    "summary": "会话摘要",
    "variables": {},
    "agent": {
      "agentId": 100,
      "name": "智能体名称",
      "type": "ChatBot",
      "icon": "https://..."
    },
    "created": "2026-01-01T00:00:00Z"
  }
}
```

---

### 2.7 更新会话主题

```
POST /api/v1/chat/conversation/{conversationId}/update
Content-Type: application/json
```

```json
{ "topic": "新主题" }
```

---

### 2.8 删除会话

```
POST /api/v1/chat/conversation/{id}/delete
```

---

### 2.9 会话文件列表

查询会话中产生的文件（通用型智能体场景）。

```
GET /api/v1/chat/{conversationId}/files
```

**响应**

```json
{
  "code": "0000",
  "message": "success",
  "data": {
    "files": [
      { "name": "output.py", "path": "/workspace/output.py", "size": 1024, "isDir": false }
    ]
  }
}
```

---

### 2.10 会话文件访问

```
GET /api/v1/chat/{conversationId}/file/{filePath}
```

返回二进制流（图片、文档等）。

---

## 3. MCP 版本说明

平台 `mcp-proxy` 支持两种 MCP 协议：

| 协议 | 实现库 | 说明 |
|------|--------|------|
| **SSE** | rmcp 0.10 | 传统 SSE + POST message |
| **Streamable HTTP** | rmcp-soddygo 1.5.0 | 单端点双向流（较新规范） |

支持的 MCP 服务安装方式：

| 方式 | 说明 |
|------|------|
| `NPX` | npm 包管理器启动 |
| `UVX` | Python uv 包管理器启动 |
| `SSE` | 远端 SSE MCP 服务 |
| `STREAMABLE_HTTP` | 远端 Streamable HTTP 服务 |
| `COMPONENT` | 平台内部组件 |

---

## 4. 调用流程

```
1. 创建会话
   POST /api/v1/chat/conversation/add         → conversationId

2. 发送消息
   POST /api/v1/chat/{conversationId}          → SSE 流式回复

3. 查询历史
   GET  /api/v1/chat/conversation/list         → 会话列表
   GET  /api/v1/chat/{conversationId}/messages  → 消息列表

4. 停止执行
   POST /api/v1/chat/{conversationId}/stop
```

---

## 5. 数据结构速查

**附件（AttachmentDto）**

```json
{ "fileUrl": "https://...", "fileName": "file.pdf", "mimeType": "application/pdf" }
```

**消息角色（role）**: `USER` | `ASSISTANT`

**智能体类型（type）**: `ChatBot`（问答型） | `TaskAgent`（通用型）
