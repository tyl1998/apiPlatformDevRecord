# Nuwax 智能体平台 — 第三方接入接口文档

> 仅包含 API Key 鉴权接口，适用于第三方系统对接。
> 源码：`ChatApiController.java`（`/api/v1/chat`）

---

## 1. 基础信息

| 项目 | 值 |
|------|-----|
| 基础路径 | `/api/v1/chat` |
| 鉴权方式 | API Key |
| 协议 | HTTP/HTTPS |
| 数据格式 | JSON |
| 字符编码 | UTF-8 |

### 鉴权

所有接口在请求头携带 API Key：

```
Authorization: Bearer ak-xxxxxxxxxxxxxxxxxxxxxxxx
```

- API Key 格式：`ak-xxx` 或 `ck-xxx`
- **每个 API Key 绑定一个智能体**（`targetId = agentId`），创建会话时必须传入绑定的智能体 ID
- API Key 由平台管理员在**智能体详情页**创建（不是 `/more-page/api-key` 页面，那个是 OpenAPI Key）

### 通用响应结构

```json
{
  "code": "0000",
  "displayCode": "0000",
  "message": "success",
  "data": {},
  "tid": "trace-id"
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `code` | String | 业务状态码，`0000` 成功 |
| `message` | String | 错误描述 |
| `data` | Object | 业务数据 |

---

## 2. 对接前准备

### 2.1 管理员操作

1. **创建智能体**：配置模型、提示词、组件（知识库/插件等），并**发布**
2. **创建 API Key**：在智能体详情页 → API Key 管理 → 创建，记录返回的 `accessKey` 和绑定的 `agentId`（即 `targetId`）
3. **确认模型可用**：智能体绑定的模型必须有可用额度（检查模型的 API 连接配置是否正确）

### 2.2 信息交付

管理员需告知第三方以下信息：

| 信息 | 说明 | 示例 |
|------|------|------|
| API Key | `ak-xxx` 格式 | `ak-xxxxxxxxxxxxxxxxxxxxxxxx` |
| Agent ID | Key 绑定的智能体 ID | `26` |
| 接口地址 | 平台 STG/PROD 地址 | `http://stgstplatform.***.net:8089` |

> **注意**：当前平台没有提供接口让第三方自行查询 Key 绑定的 Agent ID，必须由管理员告知。

---

## 3. 接口列表

### 3.1 创建会话

在与智能体对话前，需先创建会话。

```
POST /api/v1/chat/conversation/add
```

**请求体**

```json
{
  "agentId": 26
}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `agentId` | Long | 是 | 智能体 ID（**必须与 API Key 绑定的 agentId 一致**） |
| `variables` | Map | 否 | 会话变量（智能体配置了变量组件时需要） |

**响应**

```json
{
  "code": "0000",
  "message": "success",
  "data": 57
}
```

> `data` 为会话 ID（`conversationId`），后续所有接口都需要。

---

### 3.2 发送消息（SSE 流式）

核心接口。向智能体发送消息，以 SSE 流式接收回复。

```
POST /api/v1/chat/{conversationId}
Content-Type: application/json
Accept: text/event-stream
```

**请求体**

```json
{
  "conversationId": 57,
  "message": "你好"
}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `conversationId` | Long | 是 | 会话 ID（**路径参数和 body 都要传**） |
| `message` | String | 是 | 用户消息内容 |
| `attachments` | List | 否 | 附件列表 |
| `attachments[].fileUrl` | String | 是 | 文件 URL |
| `attachments[].fileName` | String | 否 | 文件名 |
| `attachments[].mimeType` | String | 是 | MIME 类型 |

> **重要**：`conversationId` 必须同时放在路径和请求体中。只放路径不放 body 会返回 `4000 conversationId is required`。

**SSE 响应**

每条事件格式（`data:` 后无空格，按 SSE 规范兼容即可）：

```
data:{"requestId":"xxx","eventType":"MESSAGE","data":{"text":"你"},"completed":false,"error":null}
```

| eventType | 说明 | data 结构 |
|-----------|------|-----------|
| `MESSAGE` | 文字 token 流 | `{"text": "内容", "think": "思考过程"}` |
| `PROCESSING` | 工具执行状态 | `{"type": "Knowledge", "status": "EXECUTING", "name": "名称"}` |
| `FINAL_RESULT` | 执行完成（成功或失败） | 见下方说明 |
| `HEART_BEAT` | 心跳保活（10s） | 忽略 |

**`FINAL_RESULT` 成功示例**：

```json
{
  "eventType": "FINAL_RESULT",
  "completed": true,
  "data": {
    "success": true,
    "error": null,
    "outputText": "完整回复文本",
    "completionTokens": 458,
    "promptTokens": 3851,
    "totalTokens": 4309
  }
}
```

**`FINAL_RESULT` 失败示例**（模型调用异常等）：

```json
{
  "eventType": "FINAL_RESULT",
  "completed": true,
  "error": "错误描述",
  "data": {
    "success": false,
    "error": "错误描述",
    "outputText": "完整错误信息",
    "completionTokens": 0,
    "promptTokens": 0,
    "totalTokens": 0
  }
}
```

> **错误判断方式**：以 `FINAL_RESULT` + `data.success = false` 为准。平台不会发送 `eventType: ERROR` 的事件。

**客户端示例**

```javascript
const response = await fetch('/api/v1/chat/57', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'Authorization': 'Bearer ak-xxx'
  },
  body: JSON.stringify({ conversationId: 57, message: '你好' })
});

const reader = response.body.getReader();
const decoder = new TextDecoder();
let fullText = '';

while (true) {
  const { done, value } = await reader.read();
  if (done) break;
  const chunk = decoder.decode(value);
  for (const line of chunk.split('\n')) {
    if (!line.startsWith('data:')) continue;
    // data: 后可能有空格也可能没有，都兼容
    const jsonStr = line.startsWith('data: ') ? line.slice(6) : line.slice(5);
    if (!jsonStr.trim()) continue;
    const event = JSON.parse(jsonStr);
    switch (event.eventType) {
      case 'MESSAGE':
        fullText += event.data.text;
        break;
      case 'FINAL_RESULT':
        if (event.data.success) {
          console.log('完成:', fullText);
        } else {
          console.error('失败:', event.data.error);
        }
        break;
      case 'HEART_BEAT':
        break;
    }
  }
}
```

---

### 3.3 停止会话

```
POST /api/v1/chat/{conversationId}/stop
```

**响应**

```json
{ "code": "0000", "message": "success", "data": null }
```

---

### 3.4 查询历史会话列表

```
GET /api/v1/chat/conversation/list?agentId=26&lastId=&limit=10&topic=
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `agentId` | Long | 是 | 智能体 ID |
| `lastId` | Long | 否 | 分页游标（上一页最后一条 ID） |
| `limit` | Integer | 否 | 返回数量，默认 10 |
| `topic` | String | 否 | 按主题模糊搜索 |

**响应**

```json
{
  "code": "0000",
  "message": "success",
  "data": [
    {
      "id": 57,
      "agentId": 26,
      "topic": "会话主题",
      "created": "2026-09-11T08:35:03.000+00:00",
      "modified": "2026-09-11T08:35:03.000+00:00"
    }
  ]
}
```

---

### 3.5 查询会话消息列表

```
GET /api/v1/chat/{conversationId}/messages?index=&limit=10
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `conversationId` | Long | 是 | 会话 ID（路径参数） |
| `index` | Long | 否 | 分页游标（上一页最后一条 index） |
| `limit` | Integer | 否 | 返回数量，默认 10 |

**响应**

```json
{
  "code": "0000",
  "message": "success",
  "data": [
    {
      "id": "msg-uuid",
      "role": "USER",
      "type": "CHAT",
      "text": "用户消息",
      "time": "2026-09-11T08:35:03.000+00:00",
      "attachments": null,
      "think": null,
      "finished": false,
      "finishReason": null,
      "componentExecutedList": null
    },
    {
      "id": "msg-uuid-2",
      "role": "ASSISTANT",
      "type": "CHAT",
      "text": "AI 回复",
      "think": null,
      "time": "2026-09-11T08:35:05.000+00:00",
      "finished": true,
      "finishReason": "stop",
      "componentExecutedList": []
    }
  ]
}
```

---

### 3.6 查询会话信息

```
GET /api/v1/chat/conversation/{id}
```

**响应**

```json
{
  "code": "0000",
  "message": "success",
  "data": {
    "id": 57,
    "agentId": 26,
    "topic": "会话主题",
    "variables": {},
    "agent": {
      "agentId": 26,
      "name": "智能体名称",
      "type": "ChatBot",
      "icon": "https://..."
    },
    "created": "2026-09-11T08:35:03.000+00:00"
  }
}
```

---

### 3.7 更新会话主题

```
POST /api/v1/chat/conversation/{conversationId}/update
Content-Type: application/json
```

```json
{ "topic": "新主题" }
```

---

### 3.8 删除会话

```
POST /api/v1/chat/conversation/{id}/delete
```

---

## 4. 完整调用流程

```
前提：管理员已创建智能体、发布、创建 API Key 并告知 agentId

1. 创建会话
   POST /api/v1/chat/conversation/add
   Body: {"agentId": 26}
   → 返回 conversationId (如 57)

2. 发送消息（SSE 流式）
   POST /api/v1/chat/57
   Body: {"conversationId": 57, "message": "你好"}
   → SSE 流：MESSAGE 事件（逐 token）→ FINAL_RESULT 事件

3. 继续对话（复用同一 conversationId）
   POST /api/v1/chat/57
   Body: {"conversationId": 57, "message": "下一个问题"}
   → SSE 流同上

4. 查询历史会话
   GET /api/v1/chat/conversation/list?agentId=26

5. 查询会话消息
   GET /api/v1/chat/57/messages?limit=50

6. 停止生成（可选）
   POST /api/v1/chat/57/stop
```

---

## 5. 已知限制

| 项目 | 说明 |
|------|------|
| Key-Agent 绑定查询 | 当前无接口让第三方自行查询 Key 绑定的 agentId，需管理员告知 |
| `modelId` 模型切换 | 需智能体开启 `allowOtherModel`，否则传入无效；值为数字 ID 非模型名 |
| `ERROR` 事件 | 文档曾描述有 `ERROR` eventType，实际不会发送，错误以 `FINAL_RESULT` + `success=false` 落地 |
| SSE 格式 | `data:` 后无空格，解析时需兼容 |

---

## 6. 数据结构速查

**附件（AttachmentDto）**

```json
{ "fileUrl": "https://...", "fileName": "file.pdf", "mimeType": "application/pdf" }
```

**消息角色（role）**: `USER` | `ASSISTANT`

**智能体类型（type）**: `ChatBot`（问答型） | `TaskAgent`（通用型）
