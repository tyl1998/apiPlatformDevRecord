# 接口自动化平台 — 前端交互设计文档

> 版本: v2.6
> 状态: Draft
> 基于: API_AUTOMATION_SPEC.md v1.9

### 变更记录


| 版本 | 变更内容                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| v1.0 | 初始版本                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| v1.1 | 新增：登录/鉴权与权限可见性、项目管理与成员管理、项目 Dashboard、测试套件；导航按「接口模式 / 仓库模式 / 通用」分组；仓库模式拆为**仓库用例(上报式)** 与 **CI 任务**；**路由全面 deep-link 化**；DAG 编辑器补撤销重做/minimap；执行态首选 SSE                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| v1.2 | **CI 任务改为自研 Runner 执行 (不依赖 Jenkins)**：任务表单改为 镜像/执行模式(常驻·强隔离)/脚本/依赖缓存/凭据脱敏/覆盖率采集方式；执行详情增加**阶段进度 + SSE 实时日志**(实时/重连/结束态、断线按 offset 续传)；新增 **Worker 池管理**(注册 Token、在线状态、draining 下线)；明确**一项目=一仓库、一仓库=多任务**，仓库为单例视图且 CI 任务继承其 SCM；报告监控统一查 `ExecutionIndex`，统计跳转改用**统一执行入口** `executions/by-index/:indexId`                                                                                                                                                                                                                                                                                                        |
| v1.3 | 新增全局约束**1.4 执行结果就地可见原则**：执行结果一律在当前页以抽屉就地呈现，禁止强制切 Tab/跳页；所有可执行资源列表须有「最近结果」列且点击可重开该次结果；「重新执行」原地刷新当前面板；失败详情不依赖 Toast 传递。据此修正：3.1.1 接口列表补「最近结果」列与执行反馈细则；3.4.1 场景列表补「上次执行」可点开与结果落点；3.5.3 流程执行明确结束后原地展开报告抽屉；3.6.1 报告详情「重新执行」定义为原地刷新；4.3 执行反馈流程改为失败即展开结果面板                                                                                                                                                                                                                                                                                                      |
| v1.4 | 重构为全局层 + 项目层：登录默认进入数据看板，新增项目管理卡片页和系统管理页；项目内执行记录仅做统一明细，接口详情内常驻执行历史；导入支持 cURL；主题和语言移入系统管理，Header 仅保留可选快捷入口。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| v1.5 | **依据审查报告全面修订**：1.4 拆分为「就地摘要 + 完整详情」两段式结果模型；新增**统一执行生命周期**(queued/running/cancelling/cancelled/succeeded/failed/timed_out) 与取消语义；「重新执行」改为**新执行不可变 + A/B 对比**；定时调度改为**多目标对象**选择(流程/测试套件/CI 任务)；**测试套件补全编辑器、成员预览、执行详情与失败重跑闭环**；新增 **Secret 权限边界**(掩码/二次认证/审计)；新增 **删除依赖扫描与历史保留策略**；「保存并执行」拆为**两阶段反馈**并引入版本冲突处理；导入预览补**冲突检测/字段级 diff/逐项策略**；修正项目切换、调度失败跳转、看板下钻与 URL 状态边界；会话过期草稿恢复；导航仅展示已启用能力；**五类空状态**；新增**响应式断点与窄屏策略**；对齐 Quiet Console：删除内容区 1440px 居中、明确 antd 组件边界、收敛持续动效。 |
| v1.6 | **接口管理重构 + 接口用例并入接口管理**：接口创建/编辑从抽屉改为**全屏工作台**(请求构建 Tab + 内联响应 + 发送后快捷保存为用例)；右侧栏改为分段切换 `[调试记录                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| v1.7 | **移除应用内「MCP 对话式接口创建」**（原 3.1.4 删除，批量导入顺延为 3.1.4）；3.1.2 明确「AI 生成」对话式创建不提供前端入口（低优先级/后置）；新增 **3.10「平台 MCP 对外暴露（Server 模式，规划中）」**——平台将自身能力作为 MCP Server 对外暴露（创建接口/接口用例/场景/DAG 编排），**暂不对外暴露**，待相关能力全部完成后统一提供；前端届时仅提供最小管理入口（暴露地址 / Token / 工具清单 / 启停开关），不做对话式创建 UI。 |
| v1.8 | **场景支持数据库步骤 + 数据源支持 SQL 定义**：数据源新增 **SQL 定义（命名查询）**（3.9：命名 SQL 列表、参数化 `{{变量}}`、测试运行、供场景/流程复用）；场景步骤从「仅接口用例」扩展为 **接口用例 / 数据库步骤**（3.4.2：数据源 + 已定义 SQL 或现场编写、参数引用前序用例输出、结果断言、查询结果 JSONPath 提取为场景变量），并支持**后置 SQL 校验**（仅断言落库结果、不参与数据传递，典型如创建用户后查库确认）；流程数据库节点同步支持引用已定义 SQL（3.5.1）。 |
| v1.9 | **撤回「场景管理」模块**（3.4 保留为撤回说明，导航不再出现入口）：场景与 3.5 流程编排解决同一个问题，但场景步骤只**引用**用例、请求参数不能在场景页编辑，一条「先生券、再把券号发给用户」的链路要求跨接口工作台与场景页来回配变量；把入参绑定与前置/后置脚本搬进场景页则等于重做一遍流程编排。多步骤链路统一用流程；「接口执行后查库确认」用流程里的数据库节点表达，**后置 SQL 校验**一并撤回；批量回归由 3.13 测试套件承担，成员为**接口用例与流程**、各自独立执行且不互传变量。 |
| v2.0 | **3.11 仓库用例全节重写（P4 落地）**：四个页面各自一条真实路由（用例树 / 接入指引 / 上报记录 / **新增 3.11.4 未匹配诊断**），不再用 `?tab=`；接入指引改为**三步** —— **撤销**原第 ③ 步「在 `http_req.py` 封装请求」与第 ④ 步「CI 里跑 `apitest-run` 并上报」，包名与前缀改为 `apitrack-sdk` / `APITRACK_*`；用例树补「两个计数口径不同」的说明与**未归位用例**分组；上报记录页把 scope 与**三种对账状态**（已对账 / 不参与对账 / 分支不匹配）作为主列；未匹配诊断页**明确不做**待处理计数、红点、「全部处理」与「补登记接口」按钮，只留「关联到已登记接口」与「标为外部依赖」，每行三样读时标注；趋势页新增默认关闭的「含仓库上报」开关。 |
| v2.1 | **3.11.4 恢复单行「登记接口」+ 接口导入新增「路径清单」格式（2026-08-30）**：仓库模式团队为了覆盖率把仓库里已有的接口清单再抄一遍，这道摩擦挡住的是接入而不是误登记。未匹配诊断页每行新增「登记接口」——弹窗确认**可编辑的路径模板**（把 `/users/abc` 改成 `/users/{username}` 再存，补通用归一只认 id 形状段的盲区），建最小接口（无 headers/body）并按匹配语义归位历史记录，同形已登记 409 提示改用「关联」；**批量补登记/「全部处理」仍不做**。接口导入弹窗新增第三格式「路径清单（批量粘贴）」（每行 `METHOD 路径 [名称]`），与登记同一套最小接口语义。同轮后端 `/repo-cases` 覆盖统计分子分母改为按归一形状去重计数（缺陷修复，记录在 `issue_fix/`），树上的接口行仍是行口径。**用例树补详情抽屉（2026-08-30 同日）**：点用例行就地展开。第二轮改版（用户看过后定的取舍）：**不展示请求步骤与涉及的接口**（树已回答归属、请求记录 run 明细页已有；用户想要的 allure 式用例内步骤 SDK 采集不到，明确不做），抽屉只留身份事实清单（case_key/文件/标签/最近执行，`.detail-facts` 标签列固定布局）+ **参数化场景**（records distinct param_id 的 chips，仅参数化用例显示）；并修复树的列宽随展开跳动的缺陷（grid-fixed 固定列宽 + 截断单元格补 title 悬停）。**上报记录降噪过滤（同日，两处口径）**：run 明细内「只看有变动的记录」复选（未匹配 + 本轮新增覆盖——first_seen_run_id 判定；「消失」无记录行、由 diff 列汇总，空态专门措辞）+ run 列表两个复选（用例有变动=新增或消失>0 / 有未匹配，OR 语义，updated 不算变动）。**悬停提速**：截断单元格的原生 title（浏览器 ~1 秒延迟不可调）换成 `Tip` 原语（antd Tooltip，100ms）。 |
| v2.2 | **CI 执行详情页落地（P4.5-11/13，2026-09-01）**：`pipeline-runs/:id` 四 tab（`?tab=log\|cases\|report\|artifacts`，真实路由参数不是 tab 条状态）。阶段进度四段（clone/cache/script/report）文本式，当前段 accent 下边线，不画轨道；实时日志复用项目 SSE 通道，`pipelineLog` 水位事件驱动 `?offset=` 增量拉取，自动滚底只在用户本就在底部附近时生效；**cases tab 的 timeline gantt**：泳道按 thread、bar 用语义色（通过/失败/跳过本来就是时间轴要回答的问题）、零宽 case 画最小 1px 线（跳过用例 duration=0，画 0 宽会消失）、**全部行有时间戳才默认时间轴**（junit 混 allure 时半张时间轴比没有更误导，自动降级列表）；**报告 tab**：allure-results 包的平台自渲染（suite 分组树 + steps 嵌套 + 参数化 + 失败摘要/堆栈 + 附件 inline 预览），**不 vendor allure 官方 SPA**——v2.1 在 3.11.2 说过「allure 式用例内步骤 SDK 采集不到，明确不做」，这条不推翻：树上的抽屉仍然没有步骤，步骤出现在 **CI run 的报告视图**里（数据来自 allure-results 包，不是 SDK 采集）；产物 tab 短活直链下载。执行记录页 runner 行从不可点改为跳转（`openParent` 与 `?parent=` 深链两处）。 |
| v2.3 | **3.12 CI 任务全节落地（P4.5-11，2026-09-01）**：3.12.1 任务列表（顶部 Runner 池读数「在线/槽位/在途/空闲/排队」+ 行内 `[▶执行][历史][编辑][删除]`；「排队中」写明是**跨项目的全局数**——认领按分区取全平台最早的一条）；3.12.2 新建/编辑改为**抽屉**（配置无子状态、不需要被分享成链接；能分享的是它的执行）；**3.12.2 的「覆盖率采集三选一」撤销**——proxy 捕获在 8.10 明确不做，落地形状是一个「注入 SDK 上报变量」开关 + 一句「这是唯一能回写用例树的路径」；进程档的提示语写成**信任声明**而不是「轻量模式」，选中时用 `.form-error` 而不是 `.form-note`（它不是补充说明，是一个要被读到的取舍）；**3.12.4「Worker 池管理」改名 Runner 池并落在系统设置里紧挨执行器面板的相邻 tab**（不是独立路由）——「执行器（平台内部 BullMQ worker）」与「Runner（自托管，跑仓库代码）」是最容易被当成重复功能的一对，同页相邻 tab 是「挨着放并写明区别」最直接的形状；「新增 Worker」的实质是**签发注册 Token + 给部署命令**，平台永不反向连接那台机器。**导航新增「仓库模式」分组**（1.2 早已如此规划）：仓库用例从 P4 时的「编排与回归」组移出、与 CI 任务并列。**趋势页开关从「含仓库上报」扩成「含仓库执行」**：一个开关同时管 ingest 与 runner，不给第二个复选框（那两者对用户是同一件事）。执行记录页补「只看勾选来的」复选（判据是 `case_filter IS NOT NULL`，只在 kind=runner 下出现；勾选执行不加第五个触发源值）。看板的「CI 任务数」从「未启用」占位接真值，**覆盖率口径一个字不改**。 |
| v2.4 | **仓库模式收成一个导航项 + 任务配置面收窄（P4.5-14，2026-09-01 验收反馈）**：3.11 与 3.12 合并为**一个导航项、五个标签**（任务 / 用例树 / 上报记录 / 未匹配诊断 / 凭据），路由前缀 `repo`（`/repo`=任务、`/repo/tree`、`/repo/runs`、`/repo/unmatched`、`/repo/credentials`）——五块共享同一个前提（测试代码在用户的仓库里），v2.3 把它们劈成两项后「为什么 CI 任务要先绑仓库」变成每次都要重新解释的问题。**3.11.2「接入指引」页撤销**，三步说明与上报 Token 并入新的**凭据**页，同页另一块是新的**项目级 Git 拉取凭据池**（命名凭据、多任务共用、只写不读、被引用时拒绝删除并列出任务）——原先凭据挂在仓库行上，而仓库行只由首次上报创建，导致「先跑一次 CI 才能配 CI」。**3.12.2 的「代码来源只读」撤销**：Git 地址移到任务上并可编辑，凭据从池子里选，「拉取方式」这个词从任务表单消失（它是凭据的属性）。**四组配置删除**：任务级 Secret（代价：Runner 侧日志脱敏失去词表，敏感值写进 env 会原样进日志，这是显式取舍）、报告格式与报告路径（永远 allure，目录从命令行的 `--alluredir` 解析并只读回显，解析不到只提示不拦保存——它可能写在仓库的 `pytest.ini` 里）、产物路径、默认任务（勾选执行的兜底改成「单任务自动选、多任务让用户选」）。**执行命令改用 Monaco + shell 高亮**（不做补全：shell 没有 `ctx` 那样的契约可补）。任务抽屉补一句工作目录说明：两档都是一次性目录、跑完删除，跨执行复用的只有按任务 + key 文件哈希分桶的依赖缓存。 |
| v2.6 | **3.10 拆成两个方向，本期只做「平台当 MCP Server」（P5 规划，2026-09-03）**：原 3.10 把「注册外部 MCP Server 供平台调用」（平台当 Client）与「平台自身能力对外暴露」（平台当 Server）写在同一节，前者还带 Server 列表 / 工具预览 / 注册弹窗三张图 —— 两者共享名字但方向相反、不共享任何一行代码。**本期只做后者**：新增项目层导航项 `MCP`（`/projects/:projectId/mcp`，「接口资产」组下），页面内容是启停开关 + 端点地址 + Token 列表（名字 / 前缀 / **scope** / 最后使用 / 吊销）+ 签发弹窗 + 工具清单只读表；明文 Token 只在签发弹窗里出现一次（照 4.6）。**Token 带 `read` / `write` 两种 scope**，默认只发只读；签发 `write` 时弹窗必须写明「写权限跟着签发人走，签发人被降权它就写不动了」。**前者（平台当 Client）整节移出当前范围**，原三张图与「MCP 工具」流程节点（3.5.1 的节点类型表、4.7 的 AI 辅助面板）一并挂起，不在导航中出现。**v1.7 的「不做对话式创建 UI」这一条本期仍然成立**，但它的理由从「能力没做完」变成「写入走 proposal-first、由人确认」——站内助手（人物 + 聊天窗 + 新手教程）整体后置到 MCP 之后，前置是用户注册、用户权限与站内通知（计划 9.8）。 |

---

## 1. 整体布局与导航

### 1.1 布局框架

```
┌─────────────────────────────────────────────────────────────┐
│                       顶部 Header                             │
│  [Logo]  [当前层级/项目选择器]                         [用户菜单] │
├───────────┬─────────────────────────────────────────────────┤
│           │                                                   │
│  左侧      │              右侧内容区域                         │
│  导航      │                                                  │
│  Sidebar   │    ┌──────────────────────────────────────┐     │
│           │    │                                       │     │
│ 【全局】     │    │         页面内容 (动态组件)            │     │
│ · 数据看板   │    │                                       │     │
│ · 项目管理   │    │                                       │     │
│ · 系统管理   │    │                                       │     │
│ ─────────── │    │                                       │     │
│ 【当前项目】 │    │                                       │     │
│ · 项目概览   │    │                                       │     │
│ ▸接口模式   │    │                                       │     │
│ · 接口管理  │    │                                       │     │
│ · 环境管理  │    │                                       │     │
│ · 测试套件  │    │                                       │     │
│ · 流程编排  │    │                                       │     │
│ · Mock 服务 │    │                                       │     │
│ · 数据源    │    │                                       │     │
│ · MCP       │    │                                       │     │
│ ▸仓库模式   │    │                                       │     │
│ · 仓库模式  │    │                                       │     │
│ ▸通用       │    │                                       │     │
│ · 报告监控  │    │                                       │     │
│ · 定时调度  │    │                                       │     │
│ · 项目设置  │    │                                       │     │
│           │    └──────────────────────────────────────┘     │
│           │                                                   │
├───────────┴─────────────────────────────────────────────────┤
│                     Footer (可选, 版本号/状态)                 │
└─────────────────────────────────────────────────────────────┘
```

- 左侧导航层级不超过 3 级，选中态高亮 + 指示条
- **Shell 全宽 (遵循 Quiet Console)**: 内容区不设最大宽度、不做整页居中，避免宽屏死区与顶部边界断裂；阅读宽度通过**内容子元素**约束（正文/表单/详情用 `max-width`，表格、DAG、日志等工作区使用可用全宽）。
- 面包屑在内容区顶部，反映当前层级路径

### 1.2 导航结构

```
全局层 (无项目上下文，登录后的默认入口)
├── 数据看板
│   ├── 全部可见项目的指标聚合
│   ├── 按项目/标签/时间筛选
│   └── 指标下钻到项目或资源详情
├── 项目管理
│   ├── 项目卡片 / 列表视图
│   ├── 创建 / 查看 / 编辑 / 归档 / 恢复 / 删除
│   └── 搜索、状态、标签筛选
├── 系统管理
│   ├── 我的偏好：主题、语言、登录后恢复上次页面
│   ├── 通知与会话（后续）
│   └── 用户管理（仅系统管理员）
└── 登录 / 登出

项目层（由项目管理或数据看板下钻进入）
├── [← 返回主页]（唯一的全局返回入口，固定回 `/dashboard`）
│
├── 【接口模式 · 平台托管】────────────────────────────
│   ├── 接口管理
│   │   ├── 接口列表 (树形: 按模块/标签分组)
│   │   ├── 接口工作台 (请求构建/发送/响应/调试记录/用例)
│   │   └── 批量导入
│   ├── 环境管理
│   │   └── 环境变量编辑
│   ├── 测试套件
│   │   ├── 套件列表
│   │   └── 套件详情 (编辑/批量执行/报告)
│   ├── 流程编排
│   │   ├── 流程列表
│   │   ├── DAG 编辑器 (可视化编排)
│   │   ├── 执行历史
│   │   └── 定时任务
│   ├── Mock 服务
│   │   ├── Mock 规则列表
│   │   └── 规则编辑器
│   ├── 数据源
│   │   ├── 数据源列表
│   │   └── 连接配置 / SQL 查询
│   └── MCP（平台作为 Server 对外暴露，v2.6）
│       ├── 启停开关 / 端点地址
│       ├── Token（read / write scope，签发 / 吊销）
│       └── 工具清单（只读）
│
├── 【仓库模式 · 代码仓库】──── 一个导航项，五个标签（P4.5-14）────
│   ├── 任务 (自研 Runner 执行；`/repo`，第一个标签)
│   │   ├── 任务列表 (含 Runner 池状态)
│   │   ├── 新建/编辑任务 (抽屉：Git 地址+凭据/镜像/命令/缓存)
│   │   └── 执行详情 (阶段进度 + 实时日志 SSE；独立路由)
│   ├── 用例树 (系统 → 接口 → 接口用例)
│   ├── 上报记录
│   ├── 未匹配诊断
│   └── 凭据 (Git 拉取凭据池 + 上报 Token + 三步接入)
│
├── 【通用 · 两模式共享】───────────────────────────────
│   ├── 执行记录 / 报告监控 (统一展示 接口模式 + 仓库模式)
│   │   ├── 执行报告
│   │   └── 趋势分析
│   └── 定时调度
│       ├── 调度列表
│       └── Cron 配置器
│
└── 项目设置
    ├── 基本信息
    ├── 成员与角色
    ├── 默认环境与执行策略
    ├── 告警渠道
    └── 仓库绑定 (本项目唯一仓库: Git 地址/凭据/跟踪分支)
```

> **两种执行模式 (务必区分)**: 「接口模式」测试资产在平台内、由内部 Worker 执行、平台能力 (Mock/数据源/单步调试) 全生效；「仓库模式」测试代码在 Git 仓库，「仓库用例」经上报把用例挂到「系统→接口→用例」树 (接口下有用例=已覆盖)，「CI 任务」由平台自研 Runner 在沙箱中拉代码跑脚本 (不依赖 Jenkins)。二者仅在「仓库用例树 + 报告监控」层归一，其余各自独立。详见 Spec 1.4 / 2.10。
>
> **MCP 不属于这条轴（v2.6）**：它不是第三种执行模式，也不是接口模式的一项执行能力——它是**创建与读取资产的另一个入口**（平台当 MCP Server 被外部驱动），产出的仍然是接口模式的资产，仍由内部 Worker 执行。见 3.10。

> **基数约束**: **一个项目 = 一个仓库**（仓库为单例视图，非列表）；**一个仓库 = 多个 CI 任务**（任务列表挂在该仓库下，共享同一份 SCM 配置）。

> **默认路由**: 首次登录或重新登录默认落到全局 `/dashboard`；用户显式启用“恢复上次页面”时，才恢复最后一个仍有权限的 canonical URL。不得根据最近项目隐式跳到项目概览。

> **权限可见性**: 全局导航和项目导航分别依据系统权限与项目角色渲染；只读用户隐藏项目内创建/执行/删除入口，「项目设置 > 成员与角色」仅项目管理员及以上可见。

> **导航只展示已启用能力 (遵循验收口径)**: 主导航只出现「当前项目已启用且当前角色可访问」的能力。尚未交付的功能不形成空页面 —— 集中到「能力中心」或完全隐藏；已交付但尚未配置的模块保留入口，并用有行动指引的空状态引导配置。看板指标同样只对已启用能力显示数字（未启用显示「未启用」），不得展示误导性 0。

> **使用说明分层（确认）**: 工作台、抽屉和编辑器必须保留与当前操作直接相关的高频短提示、
> 字段约束、状态、错误和必要示例，不做“无提示化”；ctx API 全表、变量完整传播链、脚本库契约、
> 阶段生命周期等长篇说明后续集中到独立“使用文档”页面，避免长期操作疲劳。内容源以
> `API_AUTOMATION_SPEC.md` 为准；页面可保留一行摘要、字段 tooltip 与文档入口，但不复制整段长文
> 形成多份漂移文档。

> **两层导航互斥 (强约束)**: 全局层侧栏只出现全局导航项；项目层侧栏**只出现该项目的导航项**，不得重复罗列数据看板 / 项目管理 / 系统管理。项目层通过侧栏顶部一个「返回主页」按钮回到全局层，返回目标固定为 `/dashboard`。

> **项目内导航项唯一职责**: 项目层不得出现两个语义重复的入口。「项目概览」只展示该项目的聚合指标与最近失败，「执行记录」才是该项目全部执行明细的列表。二者渲染不同内容，任何一个都不能是另一个的副本。

> **接口用例不设独立入口 (v1.6)**: 接口用例（接口请求快照 + 环境 + 断言）不再作为独立导航模块，入口收敛到**接口管理 → 接口工作台的「用例」侧栏框**（见 3.1.3）。测试套件通过跨接口用例选择器弹窗引用用例，不提供独立的「全部用例」导航页。

### 1.2.1 列表检索一律走服务端

> **背景**: 前端对已加载数组做 `filter` 只能命中当前页数据，用户输入关键字却搜不到未加载的记录，是一种看起来能用、实际不可信的检索。


| 规则               | 要求                                                                                       |
| -------------------- | -------------------------------------------------------------------------------------------- |
| **检索发请求**     | 关键字、状态、标签、时间等筛选一律作为 query 参数发给后端，由 SQL 过滤全量数据后返回       |
| **模糊匹配口径**   | 关键字对名称与描述做**大小写不敏感的子串匹配**（`ILIKE %kw%`），不是前缀匹配、不是精确相等 |
| **输入防抖**       | 关键字输入 300ms 防抖后发起请求，避免逐字符打接口                                          |
| **筛选进 URL**     | 筛选条件写入 URL query，刷新和分享后结果一致                                               |
| **状态取值受约束** | 状态筛选只能使用数据库枚举中真实存在的值，界面必须解释其业务含义，不得直接暴露英文原值     |

**项目状态口径 (来自 `projects.status` 的 CHECK 约束，非前端自定义)**


| 取值       | 界面文案 | 含义                                     |
| ------------ | ---------- | ------------------------------------------ |
| `active`   | 进行中   | 正常项目，可编辑、可执行、参与调度与统计 |
| `archived` | 已归档   | 只读保留历史，不参与调度；可恢复为进行中 |

### 1.2.2 操作按钮：图标与文字的边界

> **背景**: 表格行内每行重复三四个文字按钮，会把操作区撑到换行，行高随之参差，列表读起来是乱的。但把所有按钮都换成图标同样有害：页面级主操作失去了可读的名字。


| 场景                                       | 形式                                | 理由                                                                       |
| -------------------------------------------- | ------------------------------------- | ---------------------------------------------------------------------------- |
| **表格行内操作**                           | 纯图标                              | 同一列重复出现，列标题与上下文已说明含义；图标能保证操作单行排列、行高一致 |
| **页面级主操作**（创建、导入、保存、运行） | 图标 + 文字                         | 每页只出现一次，没有重复上下文可依赖，必须自报其名                         |
| **危险操作**（删除）                       | 图标（行内）/ 图标 + 文字（页面级） | 一律配`danger` 色，且必须二次确认                                          |

**硬性要求**

- 每个纯图标按钮必须同时提供 `aria-label` 和 tooltip，含义不可仅靠图形传达。
- **操作按钮不得因视口变窄而隐藏**。曾有 `@media (max-width: 420px) { display: none }` 隐藏次级操作，这让用户在窄屏下彻底够不到功能——够不到的控件比拥挤的行更糟。图标形态本身已足够紧凑，应对窄屏。
- 行内操作区 `flex-wrap: nowrap`，宁可裁剪也不换行。

### 1.3 页面通用元素


| 元素          | 说明                                                   |
| --------------- | -------------------------------------------------------- |
| 面包屑        | 内容区顶部，显示`项目名 > 模块 > 子页`                 |
| 标题栏        | 页面标题 + 右侧操作按钮组（创建、刷新、筛选切换）      |
| 筛选区        | 可折叠的筛选条件，支持关键字、状态、标签等多维过滤     |
| 表格/卡片     | 列表页默认表格，可切换卡片视图；操作列固定在右侧       |
| 分页器        | 表格底部，支持跳页和每页条数切换                       |
| 抽屉/侧边面板 | 详情/编辑/创建使用右侧抽屉，避免跳页打断操作流         |
| 空状态        | 列表为空时展示引导性空状态，附带"创建"快捷入口         |
| 反馈          | 加载骨架屏/Spin，成功 toast，失败展开结果面板 (见 1.4) |

### 1.4 执行结果就地可见原则 (全局约束)

> **背景**: "点击执行 → 被切到另一个 Tab / 跳到执行历史页 → 再点开某条记录才看到结果"是一条反直觉的路径：用户刚发起的动作，结果却落在别处，排查时还会丢失当前筛选与滚动位置。本原则对**所有触发执行的入口**统一收口。
>
> **同时明确**: "结果必须就地可见"不等于"所有复杂结果都塞进一个窄抽屉"。"就地立即反馈"与"复杂详情需要完整页面"不是二选一，而是**两段式**：先就地给摘要，再由用户主动进入稳定详情页。

**适用范围**: 接口、接口用例、测试套件、流程、CI 任务 — 凡是可以"执行"的资源，一律遵守。


| 规则                 | 要求                                                                                                                                                                             |
| ---------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **结果就地呈现**     | 执行结束后，结果在**当前页面**就地呈现，**不得**自动切换左侧导航、Tab 或跳转路由                                                                                                 |
| **列表可见最近结果** | 所有可执行资源的列表页必须有「最近结果」列：`状态徽章 + 状态码或通过率 + 耗时`；未执行显示"未执行"占位                                                                           |
| **结果可重新打开**   | 点击「最近结果」单元格即重新打开该次执行的结果视图，无需去执行历史页找                                                                                                           |
| **两段式结果模型**   | 单接口与简单用例 →**完整结果抽屉**直接呈现；套件、流程、CI → 先在当前页展开**摘要面板/抽屉**(状态、进度、关键失败、少量子项)，再提供「打开完整详情」入口进入稳定详情路由 |
| **执行中就地反馈**   | 执行期间按钮进入 loading 态、行内显示进度，**不遮挡**整页；长耗时执行 (流程/套件/CI) 展示阶段进度                                                                           |
| **失败不依赖 Toast** | 失败**直接就地展开摘要面板**展示错误详情。Toast 3s 自动消失，不可作为错误详情的唯一载体 (与 9.1 错误分级一致)                                                                    |
| **历史页为可选入口** | 「执行历史」「报告监控」只用于**审查全量记录**，不作为查看"刚才那次执行结果"的必经路径                                                                                           |
| **资源内历史优先**   | 接口、用例、流程、套件、CI 任务详情内必须展示自身历史；例如接口详情只显示该接口执行，允许直接查看快照或重跑                                                                |

**统一执行生命周期 (所有可执行资源共用同一状态机)**:


| 状态                | 界面表现                         | 用户可做                                             |
| --------------------- | ---------------------------------- | ------------------------------------------------------ |
| `queued` 排队中     | 状态徽章「排队中」+ 队列位置     | 立即取消 (无需确认)                                  |
| `running` 执行中    | 呼吸点 (唯一环境动画) + 阶段进度 | 取消 (需确认，提示已发出的 HTTP 请求无法回滚)        |
| `cancelling` 取消中 | 状态徽章「正在取消」，按钮禁用   | 等待；取消请求失败或 Worker 失联时给出反馈并提供重试 |
| `succeeded` 成功    | 通过徽章 + 关键指标              | 查看详情 / 重跑                                      |
| `failed` 失败       | 失败徽章 + 错误摘要，直接展开    | 查看失败项 / 重跑                                    |
| `cancelled` 已取消  | 「已取消」徽章 (非失败)          | 查看已完成部分 / 重跑                                |
| `timed_out` 超时    | 「超时」徽章 + 超时点            | 查看已完成部分 / 重跑                                |

- **取消语义**: 排队中立即取消；运行中取消需确认；取消后**保留已完成节点/子项结果**，未开始项标记为 `cancelled` 而非 `failed`。
- **部分结果**: 因取消/超时/失败提前结束的执行，已完成子项结果照常可查，报告口径区分「失败」与「未执行/已取消」。
- **取消权限**: 仅项目管理员、任务发起者及具有执行权限的角色可取消；页面明确谁可以取消。

**完整结果抽屉结构** (接口/简单用例，宽度响应式，不写死 520px):

```
┌────────────── 执行结果抽屉 (窄屏自动全屏) ──────────────┐
│ <资源名称>                        [状态徽章] [X] │
├──────────────────────────────────────────────────┤
│ ┌────────┐ ┌────────┐ ┌────────┐                │
│ │ 状态码 │ │ 耗时   │ │ 执行时间│   ← 关键事实前置 │
│ └────────┘ └────────┘ └────────┘                │
│ ── REQUEST ──   (方法、URL、Query、Headers、Body)  │
│ ── ERROR ──     (失败时才出现, 错误信息)          │
│ ── RESPONSE ──  (状态、Headers、Body、截断信息)   │
│ ── 断言结果 ──  (用例/流程: 逐条通过与失败对比)    │
│ [重新执行] [打开完整详情] [查看全部记录]           │
└──────────────────────────────────────────────────┘
```

**复杂结果摘要面板** (套件/流程/CI，在当前页就地展开，不是完整报告):

```
┌────────────── 执行摘要 (就地展开) ──────────────┐
│ <资源名称>                  [状态徽章] [取消] [X] │
├──────────────────────────────────────────────────┤
│ 总状态 · 通过率(通过/总数) · 耗时 · 触发方式·环境  │
│ 阶段进度条 (拉取→执行→汇总) 或 子项进度           │
│ 关键失败 (最多前 5 条，可展开请求/断言摘要)        │
│ [打开完整详情 →]   [重新执行]  [查看全部记录]      │
└──────────────────────────────────────────────────┘
```

- **「打开完整详情」**: 由用户**主动**进入稳定详情路由 (见第 6 章)，例如 `test-suites/:id/executions/:execId`、`flows/:id/executions/:id`、`pipeline-runs/:id`，这些路由在刷新、分享、前进后退时状态一致。
- 完整详情页包含执行时间线/子项表、请求快照、失败原因、筛选、重跑与导出，不受窄抽屉高度限制。

**「重新执行」语义 (重跑身份不可变)**:

- 原执行记录 (执行 A) **保持不可变**：其编号、时间、结果与分享 URL 不因重跑而改变。
- 重跑**生成新的执行 B** (`ExecutionIndex`)，在 A 的面板内跟踪 B 时明确显示「由 #A 重新执行，生成 #B」。
- 新执行创建后**显式将 URL 切换为 B**，或保留 A 页面并给出「查看新执行 B」入口。
- 摘要面板与完整详情页均提供「返回原执行」与「对比 A/B」入口；同一资源同一参数的重跑可直接做逐项对比，不需要用导出 JSON 代替基本对比流程。
- 重跑沿用原执行的环境与输入参数；若需改参数，引导回资源编辑页而不是在报告里改。

> **深链一致性**: 结果抽屉/摘要面板由 URL query 驱动 (`?run=:executionId`，见 6.2 b)，完整详情为独立稳定路由，因此"就地展示"不牺牲可分享、可刷新、可前进后退的能力。

---

## 2. 技术栈


| 类别        | 选型                                 | 用途                            |
| ------------- | -------------------------------------- | --------------------------------- |
| 核心框架    | React 18 + TypeScript                | UI 构建                         |
| UI 组件库   | Ant Design 5.x                       | 仅行为组件，见下方**antd 边界** |
| 可视化编排  | React Flow                           | DAG 流程编排画布                |
| 多语言      | react-i18next                        | 界面框架 + 提示文案翻译         |
| HTTP 客户端 | axios (封装)                         | 与 API Service 通信             |
| 状态管理    | zustand / React Context + useReducer | 全局状态与页面级状态            |
| 路由        | React Router v6                      | 页面路由，嵌套路由              |
| 表单        | Ant Design Form + react-hook-form    | 表单验证与提交                  |
| 代码编辑器  | Monaco Editor                        | 脚本断言编辑、JSON/YAML 编辑    |
| 图表        | ECharts / Recharts                   | 趋势分析图表                    |
| 日期处理    | dayjs                                | 日期格式化与时区处理            |
| 请求去重    | ahooks useRequest                    | 请求缓存、轮询、防抖            |

> **antd 使用边界 (遵循 Quiet Console)**: 保留 antd 的 **Modal、Drawer、Form、Select、message** 等承载焦点陷阱/校验/门户逻辑的行为组件；**不使用 antd `Table`、`Card`、`Statistic`、`Tag`**，避免回到通用后台模板视觉。Shell、导航、表格、按钮、输入、状态徽章、HTTP method 标签、执行条、瀑布、空状态与指标读数一律使用项目共享原语 (`apitest-web/src/ui.tsx` 与 `design-system.css`)，颜色与主题统一来自现有 token (`theme.ts`)。

---

## 3. 各模块交互设计

### 3.0 登录与项目管理 (新增)

#### 3.0.1 登录页

```
┌────────────── 登录 ──────────────┐
│           接口自动化平台          │
│                                   │
│  账号: [______________________]   │
│  密码: [______________________]   │
│  ☑ 记住我                         │
│         [   登 录   ]             │
│                                   │
│  (预留 SSO / OIDC 登录入口)       │
└───────────────────────────────────┘
```

- 未登录访问任意页面统一重定向到登录页；登录成功后写入 JWT 并跳转到全局「数据看板」。只有用户在系统管理中显式开启“恢复上次页面”时才恢复上次 URL。
- 401 (token 失效) 由 axios 拦截器统一跳登录；403 (无权限) 展示无权限占位页。

#### 3.0.2 全局数据看板

```
┌──────────────────────── 数据看板 ────────────────────────┐
│ 项目: [全部项目 ▼]  标签: [全部 ▼]  时间: [近 7 天 ▼]     │
│                                                            │
│  项目  12     接口  248     通过率  96.2%     完成率  74% │
│  覆盖率  待接入    接口用例  未启用    CI 任务  未启用     │
│                                                            │
│ ┌──────────── 项目质量概览 ─────────────────────────────┐ │
│ │ 订单系统    42 接口  98.1%  31/42 已执行   [进入项目] │ │
│ │ 用户中心    28 接口  91.4%  22/28 已执行   [进入项目] │ │
│ └──────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────┘
```

- 看板只展示跨项目的聚合和趋势，不能展示全部执行明细表来替代项目执行记录。
- “通过率”仅基于有结果的执行计算；无样本显示 `—`。
- 未交付能力（CI、调度、覆盖率）显示“未启用”或“待接入”，不显示误导性的 0。
- 指标和项目行均可下钻：项目指标进入项目内资源页，项目行进入项目概览。

**指标下钻规则 (逐项定义)**:


| 指标      | 计算口径                                    | 无样本             | 下钻 URL                                                                              |
| ----------- | --------------------------------------------- | -------------------- | --------------------------------------------------------------------------------------- |
| 项目数    | 当前角色可见的 active 项目                  | `—`               | `/projects?status=active`                                                             |
| 接口数    | 项目内已登记接口总数 (含未执行)             | `—`               | `/projects/:pid/endpoints`                                                            |
| 通过率    | 有结果的执行中通过占比 (非全量时注明样本数) | `—`               | `/projects/:pid/reports?from=&to=`                                                    |
| 完成率    | 已至少执行一次的可执行资产 / 可执行资产总数 | `—`               | `/projects/:pid/overview`                                                             |
| 覆盖率    | 已覆盖接口数 / 已登记接口数                 | 「待接入」         | `/projects/:pid/repo/tree?onlyUncovered=1`                                           |
| 用例数    | 项目内接口用例总数                          | 未启用则「未启用」 | `/projects/:pid/endpoints?case=all`（进入接口管理；用例均在接口工作台的「用例」框内） |
| 仓库任务数 | 项目内仓库任务总数（含停用的；P4.5-11 起为真值） | 未启用则「未启用」 | `/projects/:pid/repo`                                                             |

- 下钻必须**保留看板的项目、标签与时间范围**，作为目标列表的初始筛选。
- 指标未启用时不可点击；部分数据接入时在 tooltip 明确覆盖范围。
- 所有「某次执行」跳转统一走 `executions/by-index/:indexId` (见 6.2 d)。

#### 3.0.3 项目管理 (全局)

```
┌──────────────────────────────────────────────────────────────┐
│ 项目管理                                        [+ 创建项目]   │
│ 搜索:[________]  状态:[全部▼]  标签:[全部▼]  [卡片][列表]     │
│ ┌──────────────────┐  ┌──────────────────┐                  │
│ │ 订单系统  ACTIVE  │  │ 用户中心  ACTIVE  │                  │
│ │ 核心交易系统      │  │ 会员服务          │                  │
│ │ 3 环境 · 42 接口  │  │ 2 环境 · 28 接口  │                  │
│ │ 近7天 96%         │  │ 近7天 88%         │                  │
│ │ [进入] [编辑] [...]│ │ [进入] [编辑] [...]│                  │
│ └──────────────────┘  └──────────────────┘                  │
└──────────────────────────────────────────────────────────────┘
```

- **一个被测系统 = 一个项目**。卡片为默认，列表为高密度替代视图；点击「进入」显式进入项目上下文。
- 「创建 / 编辑项目」包含名称、系统类型、负责人、描述、标签、可选关联仓库。
- 卡片菜单按权限提供详情、编辑、归档/恢复、删除。归档后项目只读并退出调度。
- **项目删除 (危险操作，见 4.4)**: 删除前进行依赖扫描并展示**资源统计**（接口/用例/流程/套件/调度/CI 任务数量）、执行历史与报告的处理策略（默认保留历史记录与资源名称/环境快照）、数据保留期及**不可恢复**说明；输入**项目名**确认后才可提交；被引用或仍有关联仓库时默认禁止硬删除，先要求归档或解绑。

#### 3.0.4 系统管理

- 一级全局页面，不能只藏在导航栏右下角或图标 tooltip 中。
- “我的偏好”至少包括主题（浅色/深色/跟随系统）、语言（中文/English）、登录后恢复上次页面开关。
- 系统管理员额外看到用户管理和系统级默认设置；普通用户只看到个人偏好。
- Header 的主题/语言快捷按钮可选，但设置页面是唯一完整、可发现的配置入口。

#### 3.0.5 项目 Dashboard (概览)

```
┌──────────────────── 概览: 订单系统 ────────────────────┐
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐  │
│  │近7天执行 │ │ 成功率   │ │ 失败告警 │ │ 待处理   │  │
│  │  1,240   │ │  96.2%   │ │    5     │ │    2     │  │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘  │
│  ── 近期执行 ──            ── 成功率趋势 (迷你图) ──    │
│  [执行列表: 用例/流程/流水线, 状态, 耗时]              │
└─────────────────────────────────────────────────────────┘
```

- 进入项目的落地页，聚合用例/流程/流水线的近期执行与关键指标，点击卡片下钻到对应模块。

#### 3.0.4 项目设置 — 成员与角色

```
┌──────────── 项目设置 > 成员与角色 ────────────┐
│ [+ 添加成员]                                    │
│ ┌────────────────────────────────────────────┐ │
│ │ 成员   │ 邮箱            │ 角色        │ 操作 │ │
│ │ 张三   │ zhang@x.com    │ 项目管理员▼ │ [移除]│ │
│ │ 李四   │ li@x.com       │ 开发者   ▼ │ [移除]│ │
│ │ 王五   │ wang@x.com     │ 只读用户 ▼ │ [移除]│ │
│ └────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────┘
```

- 角色下拉即时变更 (项目管理员/开发者/只读用户)，联动后端 `UserProjectRole`。
- 「添加成员」：搜索平台用户 -> 选择角色 -> 确认。
- 其它设置 Tab：基本信息、默认环境与执行策略、告警渠道、仓库绑定 (本项目唯一仓库；上报 Token 与 CI 任务在各自页面管理，不进设置)。

---

### 3.1 接口管理

#### 3.1.1 接口列表页

```
┌──────────────────────────────────────────────────────────────┐
│ [面包屑]  项目名 > 接口管理                                    │
├──────────────────────────────────────────────────────────────┤
│ 接口管理                                        [+ 创建接口]   │
│                                                              │
│ ┌────────────┐  ┌───────────────────────────────────────────┐│
│ │ 左侧树形    │  │  搜索: [____________]  标签: [▼]  方法: [▼]││
│ │            │  │  ┌───────────────────────────────────────┐││
│ │ 📁 用户模块 │  │  │ 名称 │方法│ 路径 │用例数│最近结果│ 操作 │││
│ │  ├ GET /users│ │  │获取用户列表│GET│/users│ 2 │✅200·82ms│[▶][编辑][删除]│││
│ │  ├ POST /users││  │创建用户   │POST│/users│ 0 │❌500·1.2s│[▶][编辑][删除]│││
│ │  └ GET /users/:id││获取用户详情│GET│/users/:id│ — │ 未执行 │[▶][编辑][删除]│││
│ │ 📁 订单模块 │  │  └───────────────────────────────────────┘││
│ │  ├ GET /orders││                              分页: < 1 2 3 > ││
│ │  └ POST /orders││                                           ││
│ │            │  │                                              ││
│ └────────────┘  └───────────────────────────────────────────┘│
└──────────────────────────────────────────────────────────────┘
```

**交互行为**:

1. **树形导航**: 左侧树按项目/模块/标签组织。点击节点选中，右侧表格联动过滤。支持拖拽调整分组。
2. **搜索与筛选**: 支持按接口名称、URL 关键词实时搜索（300ms 防抖），按 HTTP 方法、标签多选过滤。
3. **卡片/表格切换**: 视图切换按钮，表格适合批量查看，卡片展示接口摘要信息（名称、方法、最近执行状态）。
4. **批量操作**: 勾选多行后可批量删除、批量添加标签、批量导出。**批量删除遵循 4.4**：先做依赖扫描，结果按「可删除 / 被引用 / 无权限 / 失败」分组逐项展示，允许只提交可安全删除的项；接口被用例、流程节点、Mock 或仓库用例引用时默认禁止硬删除。
5. **快捷执行 (遵守 1.4)**: 行内"执行"按钮**进入该接口工作台并自动发送**（页面保持在工作台内，结果就地呈现在响应面板），或行内执行后刷新该行「最近结果」。两种方式均不跳离当前模块。
6. **最近结果列**: 展示 `状态徽章 + 状态码 + 耗时`，取该接口最近一次执行；点击单元格重新打开该次结果（工作台响应面板 / 结果抽屉）；从未执行显示"未执行"。该列让"刚才跑没跑、结果如何"在列表上直接可见，无需进执行历史。
7. **用例数列**: 展示该接口已保存的用例数量（`0` 表示暂无用例）；点击单元格进入该接口工作台并定位到「用例」框（见 3.1.3）。用例数由后端随列表返回（`caseCount`），未接入时显示 `—`，不显示误导性数字。

#### 3.1.2 接口工作台 (请求构建 · 发送 · 响应 · 调试记录 · 用例)

> 接口创建/编辑使用**全屏工作台**而非抽屉，行为对齐 API 客户端的工作方式：编辑请求 → 发送 → 就地读响应。响应**内联在请求区右侧**而不是遮住它，因为「把响应和产生它的请求放一起对比」是核心循环。

**布局**:

```
┌──────────────────────────────────────────────────────────────────┐
│ [GET] 获取订单列表 [未保存]          [切换接口◉] [删除] [保存]      │
├──────────────────────────────────────────────────────────────────┤
│ 请求行: [GET▼][https://api.example.com/orders/:id?page=1][环境▼]  │
│         [发送] [⇩保存为用例] [复制cURL]                            │
│ ┌─────────────────────────────┬─────────────────────────────────┐│
│ │ params │ headers │ body │ auth  │  内联响应面板                ││
│ │                             │  │  ┌─────────────────────────┐ ││
│ │  请求配置区 (Key-Value 表格, │  │  │ ✅ 200 · 82ms · 1.2KB   │ ││
│ │  Body 按类型切换, JSON 校验)  │  │  │  Body │ Headers │ Request│ ││
│ │                             │  │  │  { "code": 0, ... }     │ ││
│ │                             │  │  └─────────────────────────┘ ││
│ └─────────────────────────────┴───┼─────────────────────────────┘│
│   右侧栏 (分段切换, 同一框内二选一) │                              │
│   ┌ [调试记录 | 用例 (3)] ───────┐ │                              │
│   │ 调试记录: 该接口执行历史列表   │ │                              │
│   │ 用例:     该接口的用例列表     │ │                              │
│   └─────────────────────────────┘ │                              │
└──────────────────────────────────────────────────────────────────┘
```

**交互要点**:

- **全屏而非抽屉**: 创建/编辑接口在工作台内完成，不弹右侧抽屉；响应内联展示，不覆盖请求区。
- **请求配置 Tab**: `params`(Query+Path) / `headers` / `body` / `auth`。Key-Value 动态表格支持批量粘贴解析；Body 按 Content-Type 切换 JSON/Form/Text；JSON 提供格式化与语法校验。
- **发送即就地响应**: 点击「发送」后按钮进入 loading，完成后**就地更新响应面板**（状态徽章、状态码、耗时、响应大小、截断提示；Body/Headers/Request 子 Tab；JSON 支持原始/格式化）。**发送总是运行当前屏幕上的内容**：存在未保存修改时先自动保存（遵循 4.5 两阶段反馈），再执行。
- **调试记录（侧栏，原「执行历史」改名）**: 记录该接口的**临时验证**执行；点击某条记录恢复该次结果快照。命名「调试记录」是为了与「用例」明确分工：调试 = 临时验证，不留回归语义；用例 = 固化回归项（见 3.1.3）。
- **结果快照可回填调试 (「替换当前请求」)**: 调试记录打开的某次结果（内联响应面板或放大抽屉）提供**「替换当前请求」**操作：将该次执行的请求快照（方法 / URL / Query / Headers / Body）回填到请求区，**替换当前输入参数**，便于立刻改参重发。回填保留当前认证配置（执行快照已脱敏、不含认证）；当前输入存在未保存修改时，先确认「替换会丢失未保存修改」再执行。
- **发送后快捷保存为用例**: 响应面板顶部出现「保存为用例」入口，与请求行的「保存为用例」等价（见 3.1.3）。
- **变量与 Secret**: 变量引用 `{{var}}` 高亮、hover 预览；secret 类型 hover 永不显示真实值（见 4.6）；认证头中的 Token/密码字段默认掩码。
- **未保存离开确认**: 修改未保存时切换接口 / 返回列表 / 关闭页面前弹确认（nav guard + beforeunload）。
- **对话式创建不提供前端入口**: 应用内「AI 生成」对话式创建接口不作为当前交互能力（原 3.1.4 已移除）。外部 AI / 工具经**平台暴露的 MCP** 创建接口、接口用例与 DAG（见 3.10）。**v2.6 更新了这条的理由**：不是「能力没做完」，而是写入走 **proposal-first**——工具产草稿、人在**原编辑器里**确认，所以草稿的落点是这个工作台本身，不是一个独立的对话面板。

#### 3.1.3 接口用例（侧栏用例框）

> **用例 = 接口请求快照 + 环境 + 断言** 的固化产物，**完全快照隔离**：保存为用例时固化当时的请求配置与环境，接口后续修改**不影响**已保存的用例；用例也不设独立导航模块，全部在接口工作台内管理与执行。

**用例框（右侧栏）**:

- 右侧栏内分段切换 `[调试记录 | 用例]`，**同一框内二选一显示**，不新增常驻空间、不缩窄主工作台。
- 「用例」Tab 展示该接口的用例列表：**名称、标签、断言数、最近结果**。
- 操作 `[⋯]`：**编辑**（名称/标签/断言表）、**删除**（遵循 4.4 依赖扫描，被测试套件引用时提示并禁止硬删除）；`[+ 新建用例]` 以当前工作台内容为起点创建。

**保存为用例流程（先预填再编辑）**:

1. 工作台发送验证后，点击请求行「保存为用例」或响应面板快捷入口。
2. 弹出保存弹窗：名称、标签、目标环境（默认当前环境）；**断言表按最近一次响应的状态码预填 `$.status = 200`**，可继续添加 JSONPath 断言（如 `$.body.data.token exists`）。
3. 确认保存 → 用例出现在该接口的用例框；原工作台内容不受影响。

**用例加载与执行**:

- 点击用例框中的用例 → 用例快照**替换主工作台请求区**，顶部显示标识 `用例: <名称> · 快照 <日期>` 与 **[返回调试]**（还原点击前的草稿）。
- 加载后可修改请求并「发送」，改动**不自动落盘**；如需留存改动，用「保存为用例」另存新版本。
- 用例执行结果就地展示（遵守 1.4）；「最近结果」可重新打开该次结果。
- 深链：`endpoints/:id?case=:caseId` 直接打开该用例（见第 6 章）。

**用例的跨模块引用**: 测试套件（3.13）通过**跨接口用例选择器弹窗**引用用例（按接口 / 标签 / 名称检索，列表按所属接口分组），不提供独立的「全部用例」导航页。

#### 3.1.4 批量导入

```
┌──────────────────────── 全屏 Modal ────────────────────────────┐
│ 批量导入接口                                          [X]       │
├────────────────────────────────────────────────────────────────┤
│                                                                 │
│  导入方式:  [文件上传]  [URL 导入]  [粘贴文本]                    │
│                                                                 │
│  ┌─ 文件上传 ──────────────────────────────────────────────┐   │
│  │                                                         │   │
│  │  支持格式: OpenAPI 3.0 / Swagger 2.0 / Postman v2.1     │   │
│  │                                                         │   │
│  │  ┌──────────────────────────────────────────────┐       │   │
│  │  │            📁  拖拽文件到此处                   │       │   │
│  │  │            或 点击选择文件                      │       │   │
│  │  └──────────────────────────────────────────────┘       │   │
│  │                                                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  上传完成: swagger.json (已解析 12 个接口)                       │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ ☑ 检测结果   接口名称    方法   路径            目标分类    │  │
│  │ ☑ [新增]     getUserList  GET    /users          用户模块  │  │
│  │ ☑ [变更△]    createUser   POST   /users          用户模块  │  │
│  │ ☐ [已存在]   deleteUser   DELETE /users/:id      用户模块  │  │
│  │ ...                                                       │  │
│  │ 冲突策略: [跳过已存在 ▼] (全局)  支持逐项覆盖               │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│                              [确认导入(10个)]  [取消]            │
└─────────────────────────────────────────────────────────────────┘
```

- 解析后展示接口预览表格，默认全选，可单独取消勾选不需要导入的接口。
- **冲突检测 (对齐 Spec)**: 以 `method + 标准化 URL` 判断与库内已有接口是否冲突，预览表逐项标示**新增 / 已存在 / 变更**，并提供**检测结果、差异摘要、处理策略**列。
- **处理策略**: 全局选择「跳过 / 更新 / 另建」后允许单项覆盖；「更新」前展示**字段级 diff**（方法、路径、Headers、Body、认证差异）；「另建」要求填写新的名称或版本标识。
- **导入结果**: 完成后展示新增、更新、跳过、另建、失败的**逐项结果**，失败项可单独重试。
- ~~MCP 增强: 导入后可一键触发 "AI 补充断言和示例数据"。~~ **撤销（v2.6）**：平台不调模型。要让 AI 补断言，由外部 agent 经 MCP 的只读工具读走接口定义、再用写工具回写（计划 9.0 边界 16）。
- 支持批量修改目标分类。

---

### 3.2 环境管理

```
┌──────────────────────────────────────────────────────────────┐
│ 环境管理                                                     │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  [Dev]  [Staging]  [Prod]  [+ 新建环境]                      │  ← Tab 切换
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ 变量列表                                           操作 │   │
│  │ ┌──────────────┬─────────────────┬────────────────┐  │   │
│  │ │ 变量名        │ 值               │ 类型/说明        │  │   │
│  │ ├──────────────┼─────────────────┼────────────────┤  │   │
│  │ │ BASE_URL     │ https://api.dev  │ string   [编辑] │  │   │
│  │ │ API_TOKEN    │ ●●●●●●●●         │ secret   [编辑] │  │   │
│  │ │ TIMEOUT      │ 30000            │ number   [编辑] │  │   │
│  │ │ + 添加变量   │                  │                 │  │   │
│  │ └──────────────┴─────────────────┴────────────────┘  │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
│  [批量导入变量]  [导出]  [JSON 编辑模式]                        │
└──────────────────────────────────────────────────────────────┘
```

**交互要点**:

- Tab 切换环境，每个 Tab 右侧有编辑/删除 icon。**删除环境遵循 4.4**：环境可能被用例、流程、调度、项目默认设置引用，删除前先做依赖扫描并展示引用方；被引用时默认禁止硬删除，提示先迁移引用或归档。
- 变量值默认为密码遮蔽 (secret 类型)。**Secret 揭示遵循 4.6**：普通用户只看到「已配置」和掩码；仅持有独立 secret 权限的管理员可短时揭示，并要求重新认证，揭示后自动恢复掩码。
- 支持双击单元格直接编辑。
- "JSON 编辑模式"切换到全屏 Monaco Editor 批量编辑。
- 内置变量 (`$timestamp` 等) 以只读徽章展示，不可编辑删除。

---

### 3.3 接口用例（并入接口管理）

> 接口用例不再作为独立模块与独立导航入口（见 1.2）。用例的定义、创建（保存为用例）、编辑、执行与**快照隔离**语义统一收敛到 **3.1.3 接口用例（侧栏用例框）**，本节不再重复。
>
> - 用例被测试套件（3.13）引用时，通过**跨接口用例选择器弹窗**选取（按接口 / 标签 / 名称检索）。
> - 用例执行记录经统一执行入口 `/executions/by-index/:indexId` 查询（见 6.2 d）。

---

### 3.4 场景管理（已撤回，v1.9）

> **本模块不再交付**，导航中不出现「场景管理」入口。
>
> 场景曾按「按顺序串已有接口用例 + 数据传递映射 + 数据库步骤 + 后置 SQL 校验」实现并上线过
> 一轮，实测后撤回。原因是它与 3.5 流程编排解决同一个问题，却把上下文切开了：场景步骤只
> **引用**用例，请求参数属于用例、不能在场景页编辑，于是一条「先生券、再把券号发给用户」的
> 链路要求先去接口工作台给生券用例配好提取变量、回到场景、再进发券用例把 `{{券号}}` 写进
> 请求体——「谁产出、谁消费」跨了多个页面。若把入参绑定和前置/后置脚本都搬进场景页，它就
> 成了流程编排的第二套实现。
>
> **替代路径**：
> - 多步骤业务链路 → **3.5 流程编排**。同一张画布上看到请求节点、数据库节点、脚本节点与
>   条件节点，变量的产出与消费在一处可见。
> - 「接口执行后查库确认写入」→ 流程里在请求节点后接一个**数据库节点**，不再有单独的
>   「后置 SQL 校验」位置。
> - 批量回归（一次跑一组）→ **3.13 测试套件**，成员为接口用例与流程，彼此独立执行。

---

### 3.5 流程编排 (DAG 编辑器) — 核心交互

```
┌──────────────────────────────────────────────────────────────┐
│ 流程: 订单处理流程                        [保存] [执行] [历史]  │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────┐  ┌────────────────────────────────────────┐   │
│  │ 节点面板  │  │                                        │   │
│  │          │  │          React Flow 画布                 │   │
│  │ 🔌 API   │  │                                        │   │
│  │   调用    │  │   ┌───────┐    ┌───────┐    ┌───────┐ │   │
│  │          │  │   │ 获取   │    │ 校验   │    │ 创建   │ │   │
│  │ 🗄 数据库 │  │   │ 订单   │───▶│ 库存   │───▶│ 支付   │ │   │
│  │          │  │   └───┬───┘    └───┬───┘    └───┬───┘ │   │
│  │ 🔀 条件  │  │       │  ❌        │  ❌        │      │   │
│  │          │  │       ▼            ▼            ▼      │   │
│  │ 🔁 循环  │  │   ┌───────┐   ┌───────┐   ┌───────┐  │   │
│  │          │  │   │ 发送   │   │ 库存   │   │ 发送   │  │   │
│  │ 📜 脚本  │  │   │ 缺货   │   │ 不足   │   │ 通知   │  │   │
│  │          │  │   │ 通知   │   │ 通知   │   └───────┘  │   │
│  │ ⏱ 等待  │  │   └───────┘   └───────┘              │   │
│  │          │  │                                        │   │
│  │ 📦 子流程 │  │                                        │   │
│  └──────────┘  └────────────────────────────────────────┘   │
│                                                              │
├──────────────────────────────────────────────────────────────┤
│  底部状态栏: 节点数: 7  |  连线数: 8  |  最后保存: 14:30      │
└──────────────────────────────────────────────────────────────┘
```

#### 3.5.1 DAG 编辑器交互细节

**拖拽创建节点流程**:

1. 从左侧节点面板拖拽节点类型到画布，画布出现新节点卡位
2. 松开后自动弹出该节点的配置面板（右侧抽屉）
3. 从节点输出端口拖出连线到目标节点输入端口建立边
4. 点击连线可配置条件表达式（条件节点）/ 变量映射（数据传递）

**节点配置面板** (选中节点后右侧抽屉):

```
┌───────────────── 节点配置抽屉 ──────────────────┐
│ 节点: 获取订单信息                    [X] [删除节点]│
├─────────────────────────────────────────────────┤
│                                                  │
│  节点名称: [获取订单信息____________]             │
│                                                   │
│  (根据节点类型展示不同配置表单)                     │
│                                                   │
│  API 调用节点:                                     │
│  接口: [GET /orders/:id ▼]  |  [使用自定义接口]     │
│  Headers 覆盖:  [+ 添加]                           │
│  Body 覆盖:  [Monaco Editor]                       │
│  超时: [30____] 秒  重试: [3____] 次               │
│                                                   │
│  ── 失败处理 ──                                    │
│  重试策略: [指数退避 ▼]                            │
│  失败节点: [发送通知 ▼]  (跳转到指定节点)           │
│                                                   │
│  [删除节点]                        [应用配置]       │
└───────────────────────────────────────────────────┘
```

**不同节点类型的配置差异**:


| 节点类型 | 特殊配置项                                          |
| ---------- | ----------------------------------------------------- |
| API 调用 | 接口选择器 + 请求覆盖 + 超时/重试                   |
| 数据库   | 数据源选择器 + SQL 编辑器(可引用已定义 SQL, 见 3.9) + 参数绑定 + 结果映射     |
| 条件     | 条件表达式编辑器 (if/else if/else 多分支)           |
| 循环     | 循环类型(遍历数组/按次数) + 循环变量 + 最大迭代次数 |
| 脚本     | Monaco Editor (JavaScript/Python 切换)              |
| ~~MCP 工具~~ | **撤销（v2.6）**：它属于「平台当 Client 去调外部 MCP Server」，与 3.10 的暴露方向相反，整体移出当前范围 |
| 等待     | 延时(毫秒) 或 条件/变量等待                         |
| 子流程   | 流程选择器                                          |

#### 3.5.2 变量预览与调试

```
┌─────────────── 变量面板 (底部, 可折叠) ───────────────┐
│  ▾ 变量上下文                           [自动刷新: 开] │
│                                                       │
│  流程输入变量:                                        │
│  ┌───────────────────────────────────────────────┐   │
│  │ order_id: "ORD-2024-001"                      │   │
│  │ user_id:  1001                                │   │
│  └───────────────────────────────────────────────┘   │
│                                                       │
│  节点变量:                                            │
│  ┌───────────────────────────────────────────────┐   │
│  │ 获取订单信息.order_status: "pending"            │   │
│  │ 获取订单信息.total_amount: 299.00               │   │
│  │ 校验库存.stock_qty: 150                         │   │
│  │ 校验库存.is_available: true                     │   │
│  └───────────────────────────────────────────────┘   │
│                                                       │
│  全局变量:                                            │
│  ┌───────────────────────────────────────────────┐   │
│  │ BASE_URL: "https://api.dev.example.com"        │   │
│  │ API_KEY:  "***"                                │   │
│  └───────────────────────────────────────────────┘   │
└───────────────────────────────────────────────────────┘
```

#### 3.5.3 流程执行与调试

- "执行"按钮 -> 环境选择弹窗 -> 确认后流程进入执行状态。
- 画布中每个节点实时展示执行状态: 等待中(灰) / 执行中(语义色 + 统一呼吸点) / 成功(绿, 勾图标) / 失败(红, 叉图标) / 已取消(灰, 空心图标)。**仅当前活动执行使用一个呼吸点**，多个节点不各自脉冲 (遵循 Quiet Console 动效预算，见 9.2)。
- 点击已执行节点高亮并展示请求/响应快照 + 变量输出。
- 支持单步调试模式: 逐个节点手动触发执行，每次执行后暂停，检查变量状态。
- **执行结束后的结果落点 (遵守 1.4)**: 执行完成时**留在 DAG 编辑器**，在画布右侧**就地展开执行摘要面板** (总体状态/耗时/节点通过数 + 执行时间线 + 关键失败 + 取消/超时语义)，**不跳转**到「执行历史」页。画布保持最终节点状态着色，点击时间线中的节点即高亮画布对应节点，形成"报告 ↔ 画布"双向联动。
- 摘要面板内「重新执行」按 1.4 重跑语义生成新执行（原执行不可变，标注「由 #A 重新执行，生成 #B」）；「打开完整详情」进入 `/flows/:id/executions/:id` 查看时间线、失败详情与完整日志；「查看全部记录」才进入「执行历史」页。
- 顶部 `[历史]` 按钮是审查全量历史的可选入口，不承担"看刚才那次结果"的职责。

---

### 3.6 报告与监控

> **归一点**: 接口模式与仓库模式在此汇合，统一查 `ExecutionIndex`。执行报告列表带「来源」列 (流程/套件/仓库用例上报/CI 任务) 与筛选；点击经统一执行入口跳转到对应详情 (平台执行详情 / 上报记录 / CI 执行详情)。趋势与失败分布统一口径聚合。

#### 3.6.1 执行报告详情

```
┌──────────────────────────────────────────────────────────────┐
│ 执行报告 #EX20240115-001                                      │
│ 流程: 订单处理流程  |  环境: Dev  |  触发: 手动                │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  总体状态: ❌ 失败  |  耗时: 3.2s  |  节点: 4/7 成功           │
│                                                              │
│  ── 执行时间线 ──                                             │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  ✅ 获取订单信息 (234ms)  ──── 14:30:01                 │  │
│  │  ✅ 校验库存 (156ms)      ──── 14:30:02                 │  │
│  │  ✅ 创建支付 (890ms)      ──── 14:30:03                 │  │
│  │  ❌ 发送通知 (timeout)    ──── 14:30:04  (重试 3 次失败) │  │
│  │  ⏸  库存不足通知 (未执行)                               │  │
│  │  ⏸  发送缺货通知 (未执行)                               │  │
│  └────────────────────────────────────────────────────────┘  │
│                                                              │
│  ── 失败详情 ──                                               │
│  节点: 发送通知                                               │
│  错误: Request timeout after 30000ms                         │
│  请求: POST https://api.example.com/notify                    │
│  重试记录: 3 次均超时                                         │
│  [🤖 AI 根因分析]                                             │
│                                                              │
│  [查看完整日志]  [导出报告 JSON]  [重新执行]                    │
└──────────────────────────────────────────────────────────────┘
```

**「重新执行」语义 (遵守 1.4 重跑身份不可变)**:

- 原执行记录 **A 不可变**：其编号、时间、结果与分享 URL 均保持不变。点击「重新执行」后按钮进入 loading，执行完成后**在原报告视图内就地跟踪新执行 B**，并明确显示「由 #A 重新执行，生成 #B」，**不跳转新页面、不打开新标签**，保证正在排查的上下文 (筛选、滚动位置、展开的失败节点) 不丢失。
- B 使用新的 `ExecutionIndex`；创建后**显式切换 URL 到 B**（或保留 A 并提供「查看新执行 B」入口），同时提供「返回原执行」与「对比 A/B」入口，可直接逐项对比两条执行，无需用导出 JSON 代替。
- 重跑沿用原执行的环境与输入参数；若需改参数，引导回资源编辑页而不是在报告里改。

#### 3.6.2 趋势分析

```
┌──────────────────────────────────────────────────────────────┐
│ 趋势分析                              时间: [最近 7 天 ▼]       │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  成功率趋势                                                   │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  (ECharts 折线图)                                       │  │
│  │  100% ┤          ╭──╮                                   │  │
│  │   90% ┤    ╭─────╯  ╰───╮                               │  │
│  │   80% ┤    │             ╰──────                         │  │
│  │       └────────────────────────────────────────────    │  │
│  │         Mon  Tue  Wed  Thu  Fri  Sat  Sun                │  │
│  └────────────────────────────────────────────────────────┘  │
│                                                              │
│  响应时间趋势 (P50/P90/P99)                                    │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  (ECharts 多线图)                                       │  │
│  │  500ms ┤                          ╭── P99               │  │
│  │  300ms ┤               ╭──────────╯    P90              │  │
│  │  100ms ┤───────────────╯               P50              │  │
│  └────────────────────────────────────────────────────────┘  │
│                                                              │
│  失败分布                                                     │
│  ┌──────────────────────┐  ┌──────────────────────────────┐ │
│  │ (饼图: 按接口)        │  │ (柱状图: 按错误类型)          │ │
│  │  POST /notify  40%   │  │  Timeout       ████████ 15次 │ │
│  │  GET /orders   30%   │  │  500 Internal  ████      8次 │ │
│  │  POST /pay     20%   │  │  401 Auth      ██        4次 │ │
│  │  Other         10%   │  │  Other         █         2次 │ │
│  └──────────────────────┘  └──────────────────────────────┘ │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

- 图表区域支持时间区间选择器（最近 1h / 24h / 7d / 30d / 自定义）。
- ~~MCP 趋势洞察: "AI 分析"按钮触发 LLM 扫描数据并生成自然语言洞察摘要。~~ **撤销（v2.6）**：那要求平台自己持有 LLM 凭据并调模型。新模型下是外部 agent 用 `list_executions` / 报告类只读工具读走数据后自己生成（计划 9.0 边界 16）。
- 图表支持 hover 查看具体数值，点击切片/柱跳转到对应执行报告列表。

---

### 3.7 定时调度

> **多目标对象**: 调度可针对**流程、测试套件、CI 任务**三类可执行资源（对齐 Spec：测试套件与 CI 任务均支持定时调度，CI 任务由平台 Scheduler 统一驱动）。接口用例是否支持直接调度由产品规格统一确认，当前不提供入口。资源详情内提供「创建定时任务」快捷入口，反向写入统一调度模块。
>
> **落地形状（P3-11 + P4.5-12，2026-09-01）——本节以下的独立页设计不做**：
> - **目标只有两类：测试套件与仓库任务**。流程不可被直接调度（P3 边界 1）：要定时跑一条流程，把它放进只含它的套件——那样才有报告与统计口径。
> - **没有独立的「定时调度」页**（P3-11 删除）。配置入口收进它的目标：套件详情页里是一个 section，仓库任务列表里是行内日历按钮开的抽屉，**两处是同一个组件**。因此下面那张带「目标类型」列的列表、以及「两步选资源」的创建弹窗**都不做**——从资源里进来的人已经回答了「跑哪个」。
> - 每条调度仍有：cron + 时区（服务端算「下五次」预览，与调度器认领同一份解析）、启停、**立即跑一次**（手动语义，不写触发历史）、**触发历史**抽屉（一个到期时刻一行：已触发 / 漏跑 / 触发失败；已触发的行可点进那次执行）。
> - **两处按目标类型分叉**：CI 目标**不显示**「触发时覆盖环境」（仓库里的脚本自己决定打哪个环境）；「变量覆盖」在 CI 目标下改称**触发参数**（同一列、同一条键形约束，但「变量」在仓库模式里没有对应物）。
> - **漏跑不补跑**，只留一行 `skipped` 证据（边界 2）；触发失败**不自动禁用**调度（边界 3）。列表顶部在「有调度超过宽限期还没被认领」时提示调度器可能没在运行——不建心跳表，用事实回答。

```
┌──────────────────────────────────────────────────────────────┐
│ 定时任务                                       [+ 创建调度]    │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│ ┌────────────────────────────────────────────────────────┐  │
│ │ 目标类型│目标名称      │Cron        │状态│下次执行 │上次结果│  │
│ │ 流程    │每日订单检查   │0 9 * * *   │🟢 │09:00   │ ✅ 通过 │  │
│ │ 测试套件│冒烟回归       │0 * * * *   │🔴 │--      │ ❌ 失败 │  │
│ │ CI 任务 │夜间隔离跑     │0 8 * * 1   │🟢 │下周一  │ ✅ 通过 │  │
│ │        │              │            │   │        │ [禁用][编辑][删除]│
│ └────────────────────────────────────────────────────────┘  │
│ 触发失败记录 (最近): #SC-1021 · 14:03 · 上游重试耗尽 → [查看执行] │
└──────────────────────────────────────────────────────────────┘
```

**调度列表**:

- 列包含**目标类型、目标名称、Cron、状态、下次执行时间、上次结果、触发失败原因**；「上次结果」点击打开该次统一执行详情，触发失败直接给出原因并链接对应执行。
- 筛选支持按目标类型、状态；搜索按目标名称。

**Cron 配置器** (创建/编辑调度弹窗):

```
┌─────────────── 创建定时任务 Modal ───────────────┐
│                                                   │
│  目标对象:                                        │
│  ① 资源类型: [流程 ▼]  (流程 / 测试套件 / CI 任务) │
│  ② 具体资源: [订单处理流程 ▼]                     │
│  目标环境: [Staging ▼]                            │
│                                                    │
│  调度方式:  [Cron 表达式 ▼]  [可视化配置 ▼]         │
│                                                    │
│  Cron 可视化配置:                                   │
│  ┌─────────────────────────────────────────────┐  │
│  │ 分钟: [0_____]  小时: [9_____]              │  │
│  │ 日:   [*_____]  月:   [*_____]  周: [1-5____]│  │
│  │                                             │  │
│  │ 预览: 每周一至周五上午 9:00 执行               │  │
│  └─────────────────────────────────────────────┘  │
│                                                    │
│  时区: [Asia/Shanghai ▼]                           │
│  日历排除: [中国法定节假日 ▼] (可选)                 │
│                                                    │
│  告警通知:                                         │
│  ☑ 执行失败时通知                                  │
│  通知渠道: [企业微信 ▼] [+ 添加渠道]               │
│                                                    │
│                        [创建]  [取消]               │
└────────────────────────────────────────────────────┘
```

- **目标对象两步选择**: 第一步选资源类型（流程/测试套件/CI 任务），第二步选该类型下的具体资源；资源列表服务端检索，避免大列表全量拉取。
- Cron 提供"可视化配置"和"表达式"两种模式，实时显示下一次执行时间和可读描述。
- 日历排除: 提供预设假期模板 + 自定义日期选择。
- **失败告警跳转 (遵循 6.2 d)**: 调度失败通知携带对应 `ExecutionIndex`，优先进入统一执行详情 `/executions/by-index/:indexId`；详情内展示触发来源与调度名称，并提供「查看调度配置」次级入口；仅当调度本身无法触发时，才进入调度诊断页。

---

### 3.8 Mock 服务

```
┌──────────────────────────────────────────────────────────────┐
│ Mock 服务                                      [+ 创建规则]   │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│ Mock 基础 URL: https://mock.example.com/project/xxx          │
│ [📋 复制]                                                    │
│                                                              │
│ ┌────────────────────────────────────────────────────────┐  │
│ │ 接口           │ 响应状态 │ 延迟  │ 模式    │ 状态  │ 操作 │  │
│ │ GET /users     │ 200      │ 0ms   │ 固定    │ 🟢   │ [编辑] │
│ │ POST /login    │ 200      │ 500ms │ 模板    │ 🟢   │ [编辑] │
│ │ GET /orders    │ 200      │ 0ms   │ 代理    │ 🔴   │ [编辑] │
│ └────────────────────────────────────────────────────────┘  │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

**Mock 规则编辑器**:

```
┌───────────── Mock 规则编辑: POST /login ─────────────┐
│                                                       │
│  模式:  [固定响应 ▼]  [模板响应 ▼]  [代理模式 ▼]        │
│                                                        │
│  ── 固定响应 ──                                        │
│  响应状态: [200___]                                    │
│  响应延迟: [0_____] ms                                 │
│  响应 Headers: [+ 添加]                                │
│  响应 Body:                                            │
│  ┌──────────────────────────────────────────────┐    │
│  │ {                                             │    │
│  │   "code": 0,                                  │    │
│  │   "data": {                                   │    │
│  │     "token": "mock-token-xxxx",              │    │
│  │     "user": { "id": 1, "name": "mock_user" } │    │
│  │   }                                           │    │
│  │ }                                             │    │
│  └──────────────────────────────────────────────┘    │
│                                                        │
│  ── 条件匹配 (可选) ──                                  │
│  触发条件: 请求 body.username = "admin" 时返回此响应     │
│  [+ 添加条件]                                          │
│                                                        │
│  Mock URL: POST /mock/project/xxx/login                │
│  [测试] [保存]                                          │
└────────────────────────────────────────────────────────┘
```

- 模板模式: Body 支持 `{{$randomInt}}`、`{{$uuid}}` 等内置 Mock 变量动态生成。
- 代理模式: 配置目标真实服务 URL，可选择修改请求或响应。
- "测试"按钮: 发起请求验证 Mock 是否正确响应。

---

### 3.9 数据源管理

```
┌──────────────────────────────────────────────────────────────┐
│ 数据源                                         [+ 创建数据源]  │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│ ┌────────────────────────────────────────────────────────┐  │
│ │ 名称     │ 类型      │ 主机:端口          │ 状态 │ 操作    │  │
│ │ 生产-用户库│ MySQL    │ db.example.com    │ 🟢  │ [测试][编辑][查询][删除] │
│ │ 测试-订单库│ PostgreSQL│ test-db.com       │ 🔴  │ [测试][编辑][查询][删除] │
│ │ 缓存-Redis │ Redis    │ redis.example.com │ 🟢  │ [测试][编辑][编辑][删除] │
│ └────────────────────────────────────────────────────────┘  │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

#### 数据源配置弹窗

```
┌─────────────── 创建数据源 Modal ───────────────┐
│                                                  │
│  名称: [测试用户库________________]               │
│  类型: [MySQL ▼]                                 │
│                                                   │
│  主机: [db.example.com________]                   │
│  端口: [3306___]                                  │
│  数据库: [test_db_______________]                 │
│  用户名: [admin________________]                   │
│  密码: [●●●●●●____________]   [👁 显示/隐藏]      │
│                                                   │
│  SSL: ☑ 启用  [证书配置...]                       │
│                                                   │
│  连接池:                                          │
│  最大连接数: [10___]  超时: [30____] 秒            │
│                                                   │
│  [测试连接]                        [保存] [取消]   │
└───────────────────────────────────────────────────┘
```

- "测试连接"按钮: 点击后显示 loading -> 成功/失败反馈。
- 密码字段默认遮挡。**揭示遵循 4.6**：仅持有 secret 权限的管理员可短时显示并要求重新认证，普通用户仅见掩码；更新密码时留空表示不修改。

#### SQL 查询面板

```
┌──────────── 查询: 测试用户库 ───────────────┐
│                                               │
│ ┌─────────────────────────────────────────┐  │
│ │  (Monaco Editor, SQL mode)              │  │
│ │  SELECT id, name, email                │  │
│ │  FROM users                            │  │
│ │  WHERE status = {{status}}             │  │
│ │  LIMIT 10                              │  │
│ └─────────────────────────────────────────┘  │
│                                               │
│  变量绑定: status = [active____]              │
│                                               │
│  [执行查询]  [格式化 SQL]  [导出结果]           │
│                                               │
│  ── 查询结果 ──                                │
│  ┌──────────────────────────────────────┐    │
│  │ id  │ name    │ email                │    │
│  │ 1   │ 张三    │ zhang@example.com    │    │
│  │ 2   │ 李四    │ li@example.com       │    │
│  │ ...  │ ...      │ ...                   │    │
│  └──────────────────────────────────────┘    │
│  返回 2 行  |  耗时: 45ms                      │
│                                               │
└───────────────────────────────────────────────┘
```

> **SQL 查询面板（手动查询）**: 数据源行内「查询」入口，执行后展示结果集；SQL 可引用**已定义 SQL**（见下）或现场编写。

#### SQL 定义（命名查询）

> 在数据源下维护一组**命名 SQL 查询**，供 SQL 查询面板与流程数据库节点复用，避免同一 SQL 多处粘贴漂移。

```
┌──────────── 数据源: 测试用户库 ───────────────┐
│ 定义 SQL: [+ 新建 SQL 定义]                  │
│ 名称      │ 参数      │ 最近校验 │ 操作         │
│ 查询用户  │ userId    │ ✅ 通过  │ [测试][编辑][删除] │
│ 清理测试单│ -         │ -       │ [测试][编辑][删除] │
└──────────────────────────────────────────────┘
```

**SQL 定义编辑器**:

```
┌──────── 编辑 SQL 定义: 查询用户 ────────┐
│  名称: [查询用户______]                  │
│  说明: [创建用户后按 id 查用户]           │
│  SQL:                                   │
│  ┌──────────────────────────────────┐   │
│  │ SELECT id, name, status          │   │
│  │ FROM users WHERE id = {{userId}} │   │
│  └──────────────────────────────────┘   │
│  参数:                                 │
│  ┌──────────────────────────────────┐   │
│  │ 参数名  │ 说明          │ 来源      │   │
│  │ userId  │ 用户 id       │ 流程变量  │   │
│  └──────────────────────────────────┘   │
│  [测试运行]  [保存]  [取消]              │
└─────────────────────────────────────────┘
```

- **命名 SQL 列表**: 名称、参数、最近校验状态；支持新建 / 编辑 / 删除（删除遵循 4.4 依赖扫描，被流程数据库节点引用时提示）。
- **参数化**: 使用 `{{变量}}` 语法（防注入，统一走参数绑定）；声明每个参数名、说明与来源（流程变量 / 前序节点输出 / 手动输入）。
- **测试运行**: 填参数试跑并预览结果集，校验 SQL 正确性。
- **复用**: 流程数据库节点（3.5.1）可引用已定义 SQL，也可现场编写。

---

### 3.10 MCP（平台作为 MCP Server 对外暴露）

> **两个方向，别混（v2.6）**：MCP 的每一条连接都有两头——**提供工具的一头（Server）** 与
> **使用工具的一头（Client）**。它们共享「MCP」这个名字，但方向相反、不共享任何一行代码。
>
> - **本节做的是「平台当 Server」**：平台把自身能力暴露成工具，让外部 AI / 工具（你已有的
>   agent 平台、IDE 里的助手、CI 脚本）来驱动平台创建接口 / 用例 / DAG。
> - **「平台当 Client」（注册外部 MCP Server、发现工具、在流程节点里调它）整节移出当前范围**
>   ——原 v1.7 版本的 Server 列表 / 工具预览 / 注册弹窗三张线框，以及 3.5.1 节点类型表里的
>   「MCP 工具」节点、4.7 的 AI 辅助面板，一并挂起，导航中不出现。
>
> 范围、边界决策与实施顺序见 `DEVELOPMENT_PLAN.md` 9.0–9.7。

**导航位置**：项目层「接口资产」组下的 `MCP` 项，路由 `/projects/:projectId/mcp`。
不进系统管理页——Token 是**项目级**的，而系统管理那三个 tab 全是平台级设施。

```
┌──────────────────────────────────────────────────────────────┐
│ MCP                                             [+ 签发 Token]│
├──────────────────────────────────────────────────────────────┤
│ 对外暴露  [ ●━ 已开启 ]                                        │
│ 端点地址  https://apitrack.internal/mcp              [复制]    │
│ 协议版本  2026-07-28                                          │
│                                                              │
│ ── Token ──                                                  │
│ ┌────────────────────────────────────────────────────────┐  │
│ │ 名称        │ 前缀        │ 权限     │ 最后使用   │ 操作  │  │
│ │ 需求同步机器人│ apimcp_7fK2 │ 读 · 写  │ 3 分钟前   │[吊销] │  │
│ │ IDE 助手     │ apimcp_a91X │ 读       │ 昨天      │[吊销] │  │
│ │ 旧脚本       │ apimcp_ee40 │ 读       │ 已吊销     │  —   │  │
│ └────────────────────────────────────────────────────────┘  │
│                                                              │
│ ── 可用工具 ──                                                │
│ ┌────────────────────────────────────────────────────────┐  │
│ │ 工具名               │ 说明                    │ 需要权限 │  │
│ │ list_endpoints       │ 列出接口                │ 读      │  │
│ │ get_execution        │ 读一次执行的请求/响应证据 │ 读      │  │
│ │ get_script_contract  │ 脚本可用的 ctx.* 契约    │ 读      │  │
│ │ create_endpoint      │ 创建或更新接口           │ 写      │  │
│ │ update_case          │ 改用例的校验与脚本       │ 写      │  │
│ └────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────┘
```

**「对外暴露」开关是服务端开关，不是隐藏入口**。关掉时端点对该项目**真的 404**，不是「藏起
按钮但接口还通」。默认关闭：一个默认开着的写入通道是安全默认值的反面。

**签发 Token 弹窗**：

```
┌─────────── 签发 MCP Token ───────────┐
│                                       │
│  名称: [需求同步机器人__________]      │
│                                       │
│  权限:  ☑ 读（接口 / 用例 / 执行证据 / 脚本契约）│
│         ☐ 写（创建与更新接口 / 用例 / DAG）│
│                                       │
│  ⚠ 勾了「写」之后：这把 Token 的写权限   │
│    跟着**你**走。你被降为只读成员或移出   │
│    项目后，它立刻写不动，读不受影响。     │
│                                       │
│  [签发]  [取消]                        │
└───────────────────────────────────────┘
```

- **默认只勾「读」**，「写」必须显式勾选。
- 那句警告不是免责声明，是**排查线索**：不写清的话，「为什么我的 Token 突然写不进去了」
  是一个查不到原因的问题。
- 签发成功后弹出明文，**只出现这一次**，照 4.6 的 secret 展示纪律（与仓库凭据页同款交互）。

**工具清单是只读的**，没有「测试运行」按钮。理由与 v1.7 那版的工具预览面板正好相反：那时
平台是调用方，得替用户验证「这个外部工具通不通」；现在平台是被调方——工具通不通由调用方
自己的连接结果回答，而在这里加一个「执行」按钮等于在管理页里内建一个没有上下文的调用器。

**明确不做的三样**：

- **对话式创建 UI**（v1.7 的决定继续成立，但理由更新了）：不是因为「能力没做完」，而是因为
  写入走 **proposal-first**——工具产草稿、人在原编辑器里确认，所以草稿的落点是**接口 / 用例
  编辑器本身**，不是一个独立的对话面板。站内助手（人物 + 聊天窗 + 新手教程）整体后置，
  前置是用户注册、用户权限与站内通知（计划 9.8）。
- **调用日志页**：写入与拒绝写审计日志，读调用不记（一次对话产生 3~10 次读调用，那个量级
  的读日志是噪音）。要看「谁改了什么」时看审计，不为它单开一页。
- **工具级开关**（逐个勾选「这个工具能不能用」）：权限的粒度是 scope 两档，再细一层会让
  「为什么模型说没有这个工具」多出一个排查维度，而收益只是把已经不含 execute / delete 的
  工具集再切一刀。

---

### 3.11 仓库模式 (上报式用例 + 自研 Runner 执行)

> **仓库模式的核心页面**。已有 pytest 仓库跑完后把结果报上来，平台把用例归位到「**系统 → 接口 → 接口用例**」树 —— 与接口模式同构，接口下**有用例=已覆盖，无用例=未覆盖**，一眼看清覆盖情况。平台只暴露**一个上报接口**，不持有、不执行、不重放测试代码；要它替你跑，用 3.12 的仓库任务。
>
> **一个导航项、五个标签**（v2.4，P4.5-14）：任务 (`/repo`) / 用例树 (`/repo/tree`) / 上报记录 (`/repo/runs`) / 未匹配诊断 (`/repo/unmatched`) / 凭据 (`/repo/credentials`)。各自一条真实路由而不是 `?tab=`：凭据页会被贴进别人的 CI 说明、上报记录会被贴进排查群，两者都要是一条能直接打开的地址。
>
> **为什么合并**（v2.3 曾是两个导航项）：五块共享同一个前提——测试代码在用户的仓库里、平台不拥有它。劈成两项之后「为什么 CI 任务要先绑仓库、另外几页不要求」变成每次都要重新解释的问题；而 P4.5-14 把 Git 来源搬到任务上之后，那个前置条件本身也消失了。**任务排在第一个**：它是唯一一个「做事」的标签，其余四个是「看结果」与「配一次就不动」。

#### 3.11.1 用例树 (系统 → 接口 → 接口用例)

```
┌──────────────────────────────────────────────────────────────┐
│ 仓库用例                                                      │
│ 已覆盖接口 41/50 · 接口覆盖率 82.0% · 仓库用例 36 · 未归位 2   │
│ ⓘ 接口后面的用例数是「关系数」，仓库用例总数按 case_key 去重，  │
│   两者相加不相等。此处覆盖率只在本页计算，看板口径不变。        │
│ 搜索:[用例名/case_key/文件路径]  □显示已消失的用例             │
├──────────────────────────────────────────────────────────────┤
│ ▾ POST  创建订单  /orders                          3 条用例   │
│     创建订单-正常   tests/order/test_create.py  smoke  ✅      │
│     创建订单-缺参   tests/order/test_create.py         ✅      │
│     创建订单-越权   tests/order/test_auth.py           ❌      │
│ ▾ GET   订单详情  /orders/{id}                     2 条用例   │
│ ▾ POST  登录      /login                           0 条用例   │
│     ⓘ 已覆盖，但没有专属用例 (请求都发生在 fixture 里)         │
├──────────────────────────────────────────────────────────────┤
│ 未归位用例 (2)                                     [展开]     │
│ 这些用例报上来了，但在本项目没有打到任何已登记的接口——常见于   │
│ 只调用了外部系统的用例。它们不属于任何接口节点，所以单列在这里。│
└──────────────────────────────────────────────────────────────┘
```

- 分页分在**接口**上，不在用例上：一个接口下的用例要一起显示才有意义，切页会让「这个接口有几条用例」随页码变。
- **两个计数口径不同，页面上必须写明**：接口后面的用例数是**关系数** (一条用例打三个接口就在三处各算一次)，「仓库用例总数」按 `case_key` 去重。
- `removed` (软删) 的用例默认隐藏，勾选后置灰显示，历史与最后结果仍读得通。
- **树底部两个并列分组**：**未归位用例** (有用例、一个接口都没打到) 与进入**未匹配诊断**的入口。两者是同一件事的两端 —— 一个是「有用例找不到接口」，一个是「有记录找不到接口」。
- **本页覆盖率不改看板**：`/dashboard` 的 `coverageRate` 本阶段一个字都不动，整套统计口径要重新设计一版，那是独立一件事。

#### 3.11.2 凭据 (上报 Token + Git 拉取凭据，就在本页，不进设置)

> **v2.4 改判（P4.5-14）**：本节原名「接入指引」，只有上报 Token 一块。现在是**两块凭据同页**，服务对象不同，标题上写清，否则会被读成重复功能：
> - **Git 拉取凭据**给**平台的 Runner**：clone 用户仓库用。项目级命名池，多个任务共用一把 key。
> - **上报 Token** 给**用户仓库里的 SDK**：跑完把结果 POST 回 `/ingest`。三步接入说明跟着它，因为那三步讲的就是这个 token 怎么用。
>
> 合并的理由是用户原话：「凭证管理应该在仓库模式下新增一个 tab 管理，你在接入指引那里我没看懂（这里不是上报的吗？跟跑任务不要耦合呀）」——两块**确实**不该耦合，但它们同类，放在一页并各自说明服务对象，比藏在两个不同名字的页面里更容易分清。

```
┌──────────── Git 拉取凭据 ─────────────── [+ 新建凭据] ┐
│ ⓘ 平台的 Runner 用它 clone 你的仓库。项目级共用：多个 │
│   任务引用同一把 key，换 token 只改这一处。公开仓库    │
│   不需要凭据——建任务时不选就行。                       │
│ 名称                  │ 方式         │ 被引用 │ 更新   │
│ 公司 GitLab 只读 token │ HTTPS+Token │   3    │ 09-01 🗑│
├──────────── 上报 Token ───────────────────────────────┤
│ ⓘ 给你仓库里的 apitrack-sdk 用。与上面的 Git 凭据无关。│
│ [名称____] [+ 创建 Token]                             │
│ order-ci   apitrack_a1b2…   最近使用 14:30  生效中 🗑 │
│                                                       │
│ ── 三步接入 ──                                        │
│ 第 1 步  pip install apitrack-sdk             [复制] │
│ 第 2 步  APITRACK_URL=https://…               [复制] │
│          APITRACK_TOKEN=apitrack_xxxx                │
│ 第 3 步  pytest                               [复制] │
│ 先看一眼会报什么: pytest --apitrack-dry-run   [复制] │
├──────────── 上报连通状态 ─────────────────────────────┤
│ Git 地址 git@…/order-api.git   状态 生效中            │
│ 最近上报 14:30   在册用例 36   上报次数 12            │
│ 跟踪分支 [main_______]                                │
│ ⓘ 只有这个分支上的「全量」上报才会把代码里已删掉的     │
│   用例标记为消失。                        [解绑仓库]  │
└───────────────────────────────────────────────────────┘
```

- **Git 凭据是项目级命名池，不是任务的一个字段**（v2.4）：一个项目通常只有一两把 key（「公司 GitLab 只读 token」），换 token 应该是一次编辑而不是 N 次。原先它挂在**仓库行**上，而仓库行只能由**首次上报**创建，于是配置顺序被锁成「先跑一次 CI 上报 → 才能配 clone → 才能让平台跑 CI」——用户来这里正是因为还没有 CI。
- **只写不读**：保存后任何接口都不回明文，只能整条替换。「拉取方式」（HTTPS token / SSH key）是**凭据的属性**，所以它出现在这里而不是任务表单里。**没有「不用凭据」这一档**：公开仓库由任务不引用任何凭据表达，一个空引用比一行「什么都没有的凭据」准确。
- **HTTPS token 是推荐的那一种**：可以精确到单仓库只读、可以随时吊销、不需要在 Runner 机器上配 SSH。平台**无法**验证凭据是不是只读，所以「请在 Git 平台侧只勾读取权限」必须写出来，而不是假设用户知道。
- **被引用时拒绝删除**并列出是哪几个任务在用（没有 `?force=true`）：强删的唯一后果是那几个任务静默退化成「按公开仓库 clone」，然后在下一次执行时以一条 clone 失败告诉用户——那不是一个用户会想要的选项。
- **三步，不是四步** (与原方案的差异在此显式记录)：原第 ③ 步「在 `http_req.py` 封装请求」与第 ④ 步「CI 里跑 `apitest-run` 并上报」**均已撤销**。前者等于让用户重写全部请求代码；后者在 pytest 场景下多余 (插件自动加载)，已降级为非 pytest 场景的备用入口 `python -m apitrack`。
- 包名与环境变量前缀是 `apitrack-sdk` / `APITRACK_*` (原拟 `apitest-sdk` / `APITEST_*`)。
- Token 明文**只在创建响应里出现一次**，之后只显示前缀；支持多 Token、吊销 (软删)、查看最近使用。
- **仓库绑定不在这里发生**：它由首次上报完成，所以这一页没有「创建仓库」的动作，只能改跟踪分支与解绑。`git_url` 刻意不可改 —— 它是上报的准入判据，在 UI 里改掉等于绕过那道校验换一个仓库进来。**解绑不影响仓库任务**（v2.4）：任务自带 Git 地址，它们回答的不是同一个问题（上报身份 vs 从哪 clone）。

#### 3.11.3 上报记录 (合并/去重/删除的可视化)

```
┌──────────── 上报记录 ───────────────────────────────────────┐
│ commit  │ 本次范围      │ 删除对账   │用例│ +/−/~   │未匹配│
│ a1b2c3d │ tests/order   │ 已对账     │ 36 │+2 −1 ~33│  0   │
│  main · apitrack-sdk 0.1.0 · #4821                          │
│  └ 这次按范围 tests/order 做了删除对账：该范围内代码里已不    │
│    存在的用例被标记为消失，范围外的用例一个都没动。          │
│ 9f0c1a2 │ tests/order   │ 不参与对账 │  8 │+0 −0 ~8 │  3   │
│  └ 这次不是范围内的全量 (用了 -k / -m / --lf 之类的筛选，或   │
│    者收集阶段有报错)，所以不删除任何用例。                   │
│ 7ab1e45 │ 整个仓库      │ 分支不匹配 │ 36 │+0 −0 ~36│  0   │
│  └ 这次是全量，但分支与仓库的跟踪分支不一致，所以没有执行     │
│    删除对账。                                                │
└──────────────────────────────────────────────────────────────┘
```

- 每条 run 展示 commit / 分支 / **本次范围 (`scope`)** / 删除对账状态 / 用例数 / diff / 未匹配数。
- **对账状态有三种，不是两种**：`已对账`、`不参与对账` (非全量)、`分支不匹配` (是全量但分支不对)。合成两种就会丢掉第三类，而「为什么这次没删」正是最常被问的那个问题。
- 展开一行看该次上报的请求明细 (method / 归一后路径 / `phase` / 用例 / 状态码 / 耗时)，未匹配的记录在此就带标记。

#### 3.11.4 未匹配诊断 (独立页面)

```
┌──────────── 未匹配诊断 ──────────────────────────────────────┐
│ 上报到了本项目没有登记的接口路径。这是一张按出现频次排序的     │
│ 诊断列表，不是待办清单——不需要清空它。                        │
│ ⓘ 这里的记录永远不计入覆盖率的分子，被前缀规则静默的也一样。   │
│ □看已静默的路径                                共 7 条路径    │
├──────────────────────────────────────────────────────────────┤
│ 路径              │次数│阶段分布            │打过它的用例│登记在│
│ POST /api/v2/user/│ 48 │setup 48 call 0 …   │ 创建订单 等│用户  │
│   login           │    │集中在 setup，多半是│  12 条     │中心  │
│                   │    │跨系统前置调用      │            │      │
│                   │    [登记接口] [关联接口] [标为外部依赖]    │
├──────────── 外部依赖前缀 ────────────────────────────────────┤
│ [/api/v2/user/] [备注____] [+ 添加规则]                       │
│ /api/v2/user/   用户中心的接口              2026-08-29   🗑   │
└──────────────────────────────────────────────────────────────┘
```

- **它是诊断视图，不是待办清单**。因此这一页刻意**没有**：待处理计数、红点、「全部处理」按钮。风险全部来自那个待办形状 —— 清单会被清空，批量清空会把别的系统的接口拉进本项目的分母。
- 保留三个动作：**登记接口** (2026-08-30 修订恢复：单行显式动作，弹窗里确认一个预填好的路径模板——可把 `/users/abc` 改成 `/users/{username}` 再存；建最小接口并按匹配语义归位历史记录，同形已登记时 409 让人改用「关联」。恢复的理由：仓库模式团队为了统计把仓库里已有的接口清单再抄一遍，这道摩擦挡住的是接入而不是误登记。**批量补登记/「全部处理」仍然不做**)、**关联到已登记接口** (安全——目标本来就在本项目、本来就在分母里；它解决的是 path 归一没匹配上这类技术缺陷) 与 **标为外部依赖** (顺手写成一条前缀规则)。
- 接口导入弹窗同步新增第三格式**路径清单（批量粘贴）** (每行 `METHOD /path [名称]`，`#` 注释；只建最小接口，进覆盖分母，要调试再补全——与「登记接口」同一套语义)。
- 每行三样都是**读时标注**而不是判定：出现频次 + 打过它的用例、`phase` 分布 (集中在 `setup` 的几乎一定是跨系统前置，这是唯一真正有判别力的信号)、**别的项目是否登记过这条路径** (只显示项目名，读取时才查，所以是活的)。标注错了没有后果——它们从不参与任何数字。
- 前缀规则支持多层 (`/api/v2/user/`)，命中即在这张列表里静默。**静默是读时算的**，删掉规则后那些记录重新出现，历史上报记录从未被改写。

### 3.12 仓库任务 (自研 Runner 执行)

> **不依赖 Jenkins**。任务由平台自研 Runner (执行 Worker) 在沙箱中运行：拉取仓库代码 → 跑用户写的命令 → 实时推日志 → 上传报告。**一条任务自己带 Git 地址与凭据引用**（v2.4，P4.5-14），因此建任务**不需要**先有一次上报——那正是用户来这里的场景：「我还没有 CI，让平台帮我跑第一次」。
>
> **它是 3.11 仓库模式的第一个标签**（`/repo`），不是独立导航项（v2.4）。
>
> **落地状态（v2.5）**：3.12.1 ~ 3.12.4 全部**已实现**。与原稿的取舍写在各小节里：编辑是抽屉不是独立路由（3.12.2）、覆盖率采集三选一撤销为一个 SDK 上报开关（3.12.2）、**代码来源从只读改为可编辑**（3.12.2，v2.4）、**四组配置删除**（3.12.2，v2.4）、**新增通知面板与串行开关**（3.12.2，v2.5）、**行内「定时」抽屉复用 3.7 的调度面板**（3.12.1，v2.5）、Worker 池改名 Runner 池并落在系统设置的相邻 tab（3.12.4）。

#### 3.12.1 任务列表

```
┌────────────────────────────────────────────────────────────┐
│ 仓库任务                                     [+ 新建任务]   │
│ [任务] 用例树  上报记录  未匹配诊断  凭据                    │
│ Runner 池: 在线 3/4  槽位 12  在途 4  空闲 8  排队 0        │
├────────────────────────────────────────────────────────────┤
│ 名称     │ Git 地址              │ ref  │沙箱│ 分区   │超时│ │
│ 冒烟     │ …/order-api.git       │ main │容器│ default│30m │▶│
│          │ 公司 GitLab 只读 token │      │    │        │    │ │
│ 全量回归 │ …/order-api.git       │ main │容器│ default│60m │▶│
│          │ 公司 GitLab 只读 token │      │    │        │    │ │
│ 夜间隔离 │ …/edge-tests.git      │ main │进程│prod-dmz│120m│▶│
│          │ 公开仓库（不用凭据）   │      │    │        │    │ │
│ 行内操作: [▶执行][📅定时][历史][编辑][删除]                  │
└────────────────────────────────────────────────────────────┘
```

- **Git 地址是一列，凭据名跟在它下面**（v2.4）：它回答的是「这条地址是用哪把 key 拉的」，分成两列会让两个本来要一起读的事实隔开。没引用凭据时写「公开仓库」而不是留空——空单元格读成「没配」。
- 顶部展示 **Runner 池实时状态**（在线/总槽位/在途/空闲/排队）。**「排队中」是跨项目的全局数**：认领扫描按分区取全平台最早的那条 run，所以「我前面排着几个」的真实答案就是全局的——这一点必须在页面上说出来，否则会被读成「本项目排队 N 个」。池状态 10 秒轮询，读的是**任何登录用户可读**的聚合接口（不是系统管理员专属的 Runner 清单），否则非管理员在自己的任务页上会吃 403。
- 池里没有在线 Runner 时把「触发会被直接拒绝」提前说清，并指向系统管理员该去哪里签发 Token——那条 `2004` 错误本身说不出这句话。
- 一个项目可建多个任务（冒烟/回归/夜间全量）。它们**可以指向不同仓库**（v2.4 的任务级 Git 地址允许这件事），但**用例树仍然只有一棵**——这是已知的松耦合，见开发计划 8.11 的「明确不做」。
- 「历史」跳执行记录页并预置 `?type=parent&kind=runner&keyword=<任务名>`，**不新建一个「任务历史」页**：那份数据就是 `ExecutionIndex`，再开一页等于第二个入口讲同一件事。
- **「定时」按钮开一个调度抽屉**（v2.5，P4.5-12）：里面就是 3.7 那个调度面板组件，与套件详情页里的**同一个**。为什么是抽屉而不是任务表单里的一段：一个任务可以有「每晚全量」与「每小时冒烟」两条调度，它有自己的列表、编辑与触发历史；塞进任务表单会变成表单里的表格。
- **删除是两段式**（v2.5）：任务被调度 / Webhook 触发器引用时，第一次确认后服务端回一份引用清单，第二个确认框把它们列出来、确认后一并删除。留着一个悬空的调度就是每天到点记一行失败的僵尸。
- **「默认任务」已删**（v2.4）：勾选执行的兜底改成「项目里只有一个任务就用它、多个让用户选」——「只有一个任务」数一下就知道，不需要一列去声明。

#### 3.12.2 新建 / 编辑任务

```
┌──────────────── 新建仓库任务 (抽屉) ──────────┐
│ 任务名称: [冒烟_________]  描述:[...]         │
│ ── 代码来源 ──                                │
│   Git 地址: [https://…/order-api.git________] │
│   拉取凭据: [公司 GitLab 只读 token ▼]        │
│   分支/ref: [main____]                        │
│ ── 运行环境 ──                                │
│   沙箱: (•)容器档(默认)  ( )进程档(信任声明)   │
│   镜像: [python:3.11-slim]  分区: [default▼]  │
│   ⓘ 两档的工作目录都是一次性的，跑完删除，所以 │
│     两次执行的报告不会互相污染。               │
│ ── 执行命令 (Monaco, shell 高亮) ──           │
│   ┌─────────────────────────────────────────┐ │
│   │ pip install -r requirements.txt          │ │
│   │ pytest -q --alluredir=allure-results     │ │
│   └─────────────────────────────────────────┘ │
│   ⓘ 报告目录: allure-results（从命令里的      │
│     --alluredir 解析）                        │
│ ── 依赖缓存 ──   缓存路径 / 缓存 key 文件      │
│ ── 环境变量 ──  ⓘ 明文，会进日志              │
│ ── 报告 ──                                    │
│   ☑ 注入 SDK 上报变量 (唯一能回写用例树的路径)  │
│ ── 通知 ── (v2.5)                              │
│   ☐成功通知  ☑失败通知                        │
│   渠道: [研发群 ▼]  标题/正文模版: [留空用默认] │
│ 超时: [30] 分钟                                │
│ ☑ 串行执行（同一时刻只跑一次）                  │
│   ⓘ 触发不会被拒绝，只是排队等前一次跑完        │
│ ☑启用                                          │
│ [取消] [保存并执行] [保存]                     │
└────────────────────────────────────────────────┘
```

- **抽屉而不是独立路由**（v2.3 取舍）：任务配置是一张长表单但它**没有子状态**，也不需要被分享成一条链接——能被分享的是它的**执行**，那才有独立路由（`pipeline-runs/:id`）。与数据源编辑器同款形状。
- **代码来源可编辑，v2.3 的「只读」撤销**（v2.4）：Git 地址在任务上（不再继承仓库行），凭据从 3.11.2 的项目级池子里选。原来那条「只读」规则的理由是「来源挂在仓库行上、改它会打断上报」——来源搬到任务上之后这个理由不成立了。**「拉取方式」这个词从任务表单消失**：它是凭据的属性，不是任务的选择。
- **沙箱两档，容器档是默认**。选进程档时的提示语是一个**信任声明**而不是「轻量模式」：脚本与 Runner 同一个文件系统，读得到平台下发给它的 clone 凭据，也拿不到出站白名单与资源限额。这一句用失败色而不是次要说明色——它不是补充信息，是一个必须被读到的取舍。
- **工作目录的说明是新加的一句**（v2.4，对应用户「进程档我理解每次工作目录的地方应该是不一致的吧？要不然可能会有报告污染」）：两档都是 `<data>/jobs/<job_id>/workspace`，跑完整目录删除，所以**不存在**跨执行的报告污染。唯一真的跨执行共享物是**依赖缓存**，且按任务 + key 文件哈希分桶——这是有意为之。这条是**文案补充而不是代码改动**：行为本来就对，缺的是把它说出来。
- **分区（Runner 标签）允许自由输入**，候选带「在线 N/M」后缀；选到一个没有在线 Runner 的标签时提前给出提示（保存不阻——标签可以先于 Runner 存在）。
- **执行命令用 Monaco + shell 语法高亮**（v2.4，对应用户反馈 3）。**不做补全**：脚本节点那边的补全喂的是平台自己的 `ctx` 契约（有单一来源），shell 命令没有等价物，硬做只能补一堆猜的 pytest 参数。Monaco 默认的 word-based 建议也一并关掉——它会把上一行的路径名当候选弹出来。
- **报告格式与报告路径两个控件删除**（v2.4，对应用户「报告格式配置是什么意思？我们就直接是 allure 呀」）：永远按 allure，目录**从命令行解析** `--alluredir[=| ]<dir>`（也认 `ALLURE_RESULTS_DIR=`），只读回显。解析不到就提示「跑完不会有报告」——**提示而不是拒绝保存**：它可能写在用户仓库的 `pytest.ini` 的 `addopts` 里，那份文件平台看不见，拿一条猜测去否决保存是错的。junit 解析器在 Runner 侧代码保留但不再有入口（删一个能跑的解析器换不来任何东西）。
- **产物路径删除**（v2.4）：allure-results 的打包上传是平台自己的行为（报告视图的数据源），与那一列从来无关；它只是一个没人填的框。
- **任务级 Secret 删除**（v2.4，对应用户「创建任务不要有 Secret，搞这么多配置干嘛呢」）。**代价必须记下而不是当成零成本**：Runner 侧的日志脱敏（原文逐行替换）随之失去词表输入，敏感值写进环境变量就会原样出现在日志里。这是用户在「少一层配置」与「平台替我遮日志」之间的显式取舍；脱敏机制保留（clone 凭据仍走它），只是不再有任务级词表喂它。环境变量那一栏的说明句因此改写成「明文，会进日志」。
- **「覆盖率采集三选一」撤销**（v2.3）：proxy 零侵入捕获在开发计划 8.10 里明确不做。落地形状是一个「注入 SDK 上报变量」开关，配一句「这是唯一能回写用例树的路径」——allure 解析出的用例名与 `case_key` 之间没有稳定映射，用它回写会把树写脏。
- **通知面板**（v2.5，P4.5-12）：与套件详情页那一块**形状相同**（成功 / 失败开关 + 渠道多选 + 可覆盖标题与正文模版），只有占位符词表换成 CI 语境（`taskName` / `runNumber` / `status` / `passedCount` / `failedCount` / `skippedCount` / `total` / `duration` / `gitRef` / `commitSha` / `trigger` / `finishedAt`）。文案要说清三件事：**「中断（Runner 失联）」与「超时」都算失败**（那两种恰恰最需要有人知道）、**主动取消不通知**（用户动作不是执行结果）、渠道在「告警通知」页维护（这里只是选）。
- **串行执行开关**（v2.5，P4.5-12，对应用户「同一任务并发跑会因为共享测试数据导致 case 不稳定」）：**默认开**。文案的关键是「触发**不会**被拒绝，只是排队」——研发的流水线在等结果，一个报错会被它当成失败。关掉时提示语换成另一句（「同一任务可以并发跑多次，上限是 Runner 空闲槽位；只在这个仓库的用例确实互不干扰时才关」），因为这两种状态下用户需要知道的事不同。

#### 3.12.3 执行详情与实时日志

```
┌──────── 执行详情: 冒烟 #128 ────────────────────┐
│ 🔵 执行中 | main@a1b2c3 | 已用 38s | Worker: w-03│
│ [取消执行]                                       │
│ ── 阶段 ──                                       │
│  ✅ 拉取代码 (2s)  ✅ 依赖(缓存命中, 1s)          │
│  🔵 执行脚本 ...                                  │
│ ── 实时日志 ──          🟢 实时  [暂停滚动][下载] │
│ ┌──────────────────────────────────────────────┐ │
│ │ + pytest -q --junitxml=junit.xml             │ │
│ │ tests/test_order.py ....                     │ │
│ │ ...                                          │ │
│ └──────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────┘

执行完成后:
┌──────── 执行详情: 冒烟 #128 ────────────────────┐
│ ✅ 成功 | 42s | exit 0 | Worker: w-03            │
│ 测试报告: 通过 48 / 失败 2 / 跳过 1  通过率 94%   │
│  [用例结果表: 用例│结果│耗时│错误摘要]            │
│  [完整日志] [下载报告] [重新执行]                 │
└──────────────────────────────────────────────────┘
```

- **实时日志**：运行中通过 **SSE** 订阅增量日志（前端不直连 Worker，由平台代理）。状态角标：`🟢 实时 / 🟡 重连中 / ⚪ 已结束`；断线自动重连并按 offset 续传；支持暂停自动滚动、关键词搜索。
- 日志超阈值时前端提示"日志过大已截断，请下载完整日志"。
- **阶段进度**（拉取代码 / 依赖 / 执行脚本 / 上传报告）让排队与冷启动耗时可见，避免用户以为卡住。
- **被串行挡住的排队 run 要说出「在等谁」**（v2.5，P4.5-12）：概要行多一句「等 #N 跑完后开始（这个任务开着串行执行）」。不说的话，一条一直排队的 run 与「分区没有在线 Runner」在界面上长得一模一样，而两者的处置动作完全不同——一个是等着，一个是去起一台机器。
- **生命周期与取消 (遵循 1.4)**: 状态按 `queued → running → cancelling → succeeded/failed/cancelled/timed_out` 推进；排队中可立即取消，运行中取消需确认（提示已发出的 HTTP 请求无法回滚，可通过补偿脚本清理）；取消/超时后**保留已完成阶段与用例结果**，未开始阶段标记为「已取消」而非失败。取消请求失败或 Worker 失联（心跳超时判定）时给出明确反馈并允许重新入队。
- 执行结束后自动切换为历史日志模式；case 级结果复用「报告监控」详情 UI。
- 若采集了覆盖率，同步进入「仓库用例」树与覆盖度视图。

#### 3.12.4 Runner 池管理 (系统管理员，原「Worker 管理」)

> **落在系统设置里紧挨执行器面板的相邻 tab，不是独立路由**（v2.3）。本节原稿把它叫「Worker 池」，与平台内部的 BullMQ 执行器**撞了同一个词**——两者是两张表两个概念，也是最容易被当成重复功能报上来的一对，所以两个面板同页并列且各自写明区别：
>
> - **执行器**：平台内部的 BullMQ worker，跑平台自己的接口/流程/套件，直连 Redis 与 Postgres，掉线只影响调度；
> - **Runner**：自托管机器，跑用户仓库里的代码，只需要出站 443，掉线要判 `aborted` 并回收租约。

```
┌──────────── Runner 池 ────────────────────────────────────┐
│ 在线 3/4    可用槽位 12    判离线阈值 90s                   │
│ ── 已注册的 Runner ──                                      │
│ Runner        │ 分区    │ 沙箱            │槽位│ 最后心跳 │  │
│ 在线 r-01     │ default │ process,container│ 4 │ 12:01:33│下线│
│ 下线中 r-02   │ default │ process,container│ 4 │ 12:01:30│恢复│
│ 离线 r-03     │prod-dmz │ process          │ 4 │ 09:12:04│下线│
│ ── 注册 Token ──   [名称___] [分区 tags] [签发 Token]      │
│ 名称    │前缀   │分区    │已注册│最后使用 │状态  │        │
│ 机房A   │a1b2c3…│default │ 2   │12:01:33│有效  │[吊销]  │
└────────────────────────────────────────────────────────────┘
```

- 「签发 Token」生成 **注册 Token** 并给出部署命令（一键复制）；Runner 主动外连注册，**自托管机器无需开放任何入站端口**，可部署在客户内网以测试内网服务。明文只出现这一次，界面上要写明这一点，否则丢了会被当成 bug。
- Token 的分区标签是「**这张 Token 允许声明的分区**」：注册时校验子集，所以一台机器不能自称在它无权服务的网段。
- 状态**三态而不是两态**：`draining` 的机器仍在线（它要跑完手上的活），只是不再领新任务——把它显示成「在线」会让人以为下线没生效。离线是心跳算出来的事实，不可人工编辑；重新注册（升级重启）会自动回到在线。
- 吊销是软删并**即时生效**：那张 Token 名下的机器下一次心跳就会被拒，它们手上在跑的任务会被判为已中止。确认框里要说出这个影响面（含台数）。

---

### 3.13 测试套件 (新增)

> **闭环要求**: 套件必须具备完整的「创建 → 配置 → 成员预览 → 执行 → 结果 → 失败重跑」闭环。路由已预留 `test-suites/:id` 与 `test-suites/:id/executions/:execId`。

#### 3.13.1 套件列表

```
┌──────────────────────────────────────────────────────────────┐
│ 测试套件                                       [+ 创建套件]    │
│ 搜索:[____] 标签:[smoke ▼] 状态:[全部▼]                       │
│ 套件名称   │ 组成方式  │ 数量 │ 上次结果      │ 操作            │
│ 冒烟回归   │ 标签:smoke│ 动态 │ ✅ 20/20     │ [▶执行][编辑]   │
│ 下单主流程 │ 手动选取  │ 8   │ ❌ 6/8       │ [▶执行][编辑]   │
└──────────────────────────────────────────────────────────────┘
```

- 「上次结果」列可点击，打开该次执行摘要（就地）或完整执行详情路由。

#### 3.13.2 套件编辑器 (`/test-suites/:id`)

```
┌───────────────── 编辑套件: 冒烟回归 ─────────────────┐
│  套件名称: [冒烟回归______]  标签: [smoke] [+添加]   │
│                                                       │
│  ── 组成方式 ──                                       │
│  (•) 按标签动态命中: [smoke ▼] [+ 条件]              │
│  ( ) 手动选取:      [+ 选择用例/流程] [8 项已选]      │
│                                                       │
│  成员预览 (保存时固化 / 执行时实时计算):               │
│  ┌──────────────────────────────────────────────┐   │
│  │ 类型 │ 名称          │ 标签    │ 状态         │   │
│  │ 用例 │ 登录-正常      │ smoke   │ 已启用       │   │
│  │ 用例 │ 登录-密码错误  │ smoke   │ 已启用       │   │
│  │ 流程 │ 下单主流程     │ smoke   │ 已归档 ⚠    │   │
│  │ ...                                        │   │
│  │ (动态命中: 命中 21 项, 较上次 +1/-0)        │   │
│  └──────────────────────────────────────────────┘   │
│                                                       │
│  ── 执行策略 ──                                       │
│  模式: [串行 ▼] / [并行 ▼]  并发度: [4____]           │
│  ☑ 失败即停 (fail-fast)   ☐ 全部执行                  │
│  环境: [Staging ▼]  超时: [30] 分钟                   │
│  ── 触发方式 ──                                       │
│  手动 / 定时调度 / Webhook/API / CI 回调 (对齐 Spec)  │
│                                                       │
│                    [保存] [保存并执行] [取消]          │
└───────────────────────────────────────────────────────┘
```

**交互要点**:

- **成员选择与去重**: 手动选取用「**跨接口用例选择器**」弹窗（按接口 / 标签 / 名称检索，列表按所属接口分组；用例即接口工作台内保存的用例快照，见 3.1.3），同一用例/流程在套件内去重并提示重复来源。成员各自独立执行，**成员之间不传变量**——需要传值的链路建成一条流程。
- **动态命中语义**: 按标签动态命中的套件在编辑页展示**实时成员预览**；保存时选择「保存时固化成员」或「执行时实时计算」——前者固化清单与版本，后者执行前展示最终命中清单及**相对上次的变化**（新增/移除项），由用户确认后再跑。
- **子项失效处理**: 被删除、归档、禁用或失去权限的子项，执行前给出拦截或跳过提示；已归档子项保留在历史结果中并标注。
- **执行策略校验**: 并行并发度 > 0 且有上限；fail-fast 与全部执行互斥；执行策略修改需确认，避免误改影响已有调度。

#### 3.13.3 执行与结果

- 点击 `[▶]` 执行：行内进入 loading，页面不跳转；执行结束就地展开**执行摘要面板**（总数、通过、失败、跳过、已取消、耗时、触发方式、环境，见 1.4 复杂结果摘要），并提供「打开完整详情」进入 `/test-suites/:id/executions/:execId`。
- 执行过程按生命周期状态推进（queued → running → cancelling → succeeded/failed/cancelled/timed_out）；运行中可取消（遵循 1.4 取消语义）。

#### 3.13.4 套件执行详情 (`/test-suites/:id/executions/:execId`)

```
┌──────── 套件执行详情: 冒烟回归 #42 ─────────┐
│ 通过 18 / 失败 1 / 跳过 1 / 取消 0 | 42s    │
│ 触发: 定时调度 · 环境: Staging               │
│ ┌───────────────────────────────────────┐  │
│ │ 子项     │ 类型 │ 结果   │ 耗时 │ 操作  │  │
│ │ 登录-正常 │ 用例 │ ✅ 通过 │ 234ms│[快照] │  │
│ │ 下单-缺参 │ 用例 │ ❌ 失败 │ 312ms│[快照] │  │
│ │ 清理-旧单 │ 流程 │ ⏭ 跳过 │ -    │[原因] │  │
│ │ ...                                   │  │
│ │ 筛选: [全部▼] [仅失败] [仅跳过]         │  │
│ └───────────────────────────────────────┘  │
│ [仅重跑失败项] [重新执行] [导出 JUnit/JSON] │
└────────────────────────────────────────────┘
```

- 子项列表支持**按结果筛选**（通过/失败/跳过/取消/未执行）、查看请求快照、跳回源用例/流程、仅重跑失败项。
- 列表「上次结果」和执行后的摘要面板都可进入完整执行详情；「仅重跑失败项」沿用原执行参数与策略，创建新执行（遵循 1.4 重跑身份不可变）。

---

## 4. 通用交互模式

### 4.1 表单交互模式


| 场景           | 交互方式                                                                  |
| ---------------- | --------------------------------------------------------------------------- |
| Key-Value 编辑 | 动态表格，每行 Key/Value/类型/删除，底部 [+ 添加]                         |
| JSON 编辑      | Monaco Editor，支持格式化和语法校验                                       |
| 下拉多选       | Ant Design Select mode="multiple"，支持搜索                               |
| 密码输入       | Input.Password，默认遮挡；**揭示遵循 4.6**（仅管理员短时显示 + 重新认证） |
| 变量引用       | `{{xxx}}` 语法输入时自动补全 + 实时高亮                                   |
| 标签输入       | Ant Design Select mode="tags"，支持新建标签                               |
| 拖拽排序       | 步骤/节点顺序调整使用 dnd-kit 实现                                        |
| 文件上传       | Ant Design Upload + Dragger，拖拽区域 + 文件图标 + 进度条                 |

### 4.2 列表通用操作

```
列表页通用交互:
├── 顶部: 搜索框 (300ms 防抖) + 多条件筛选器 (可折叠)
├── 工具栏: [创建] [批量操作] [视图切换 表格/卡片] [刷新]
├── 表格:
│   ├── 排序: 点击列标排序 (↑↓ 指示)
│   ├── 行选择: 复选框多选 -> 批量操作栏浮现
│   ├── 行操作: 固定在右侧的操作列，最多展示 3 个按钮，更多收起到 [...]
│   ├── 快捷筛选: 列头筛选器 (文本/枚举/标签)
│   └── 空状态: 区分五类 (见 9.3)，仅可创建且模块已启用时显示创建入口
└── 底部: 分页器 + 总数
```

- **行选择为会话级状态**：批量选中的行**不进入 URL**（只影响当前页面会话），筛选变化或离开页面时清空并提示，避免超长 URL 与危险操作上下文被分享。

### 4.3 执行反馈流程

```
触发执行 (手动/定时/Webhook/CI 回调)
    │
    ▼
┌─────────────────────────────────────┐
│  环境选择弹窗 (如果是手动触发)        │
│  选择目标环境 -> [确认执行]          │
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│  执行中状态 (统一生命周期，见 1.4)    │
│  ─ queued: 徽章「排队中」+ 队列位置   │
│  ─ running: 行内 Spin + 唯一呼吸点    │
│    详情页: 顶部进度条 + 阶段进度       │
│  ─ cancelling: 「正在取消」          │
│  ─ 取消: 排队中立即取消; 运行中需确认  │
│    (提示已发出的 HTTP 请求无法回滚)    │
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│  执行完成 (遵守 1.4: 结果就地呈现)    │
│  ─ 单接口/简单用例: 就地完整结果抽屉   │
│  ─ 套件/流程/CI: 就地执行摘要        │
│     面板 + 用户主动「打开完整详情」    │
│  ─ 无论成败: 刷新列表「最近结果」列    │
│  ─ 成功: 摘要展示通过率/耗时/响应     │
│          附 Toast 绿色 "执行成功(234ms)"│
│  ─ 失败: 摘要直接展开错误详情         │
│          (不靠会消失的 Toast 传递)    │
│  ─ cancelled/timed_out: 保留已完成部分│
│      未开始项标记为取消而非失败        │
│  ─ 页面不切 Tab、不跳路由             │
│  ─ 状态实时更新 (通过轮询或 SSE)     │
└─────────────────────────────────────┘
```

> **反面示例 (禁止)**: 执行完成后自动切到「执行报告」Tab 或跳转执行历史页。用户刚发起的动作，结果必须落在他当前所在的位置；被迫离开再点回来，等于让人为一次点击付两次导航成本。

### 4.4 危险操作 (删除/归档统一规则)


| 规则         | 要求                                                                                                                  |
| -------------- | ----------------------------------------------------------------------------------------------------------------------- |
| **依赖扫描** | 删除/归档前先执行依赖扫描，展示引用类型、数量与受影响资源列表；被引用资源默认**禁止硬删除**，优先归档或要求先迁移引用 |
| **历史保留** | 历史执行始终保留资源名称与环境快照，不依赖当前资源继续存在；删除不追溯改写历史报告                                    |
| **批量结果** | 批量删除结果分为「可删除 / 被引用 / 无权限 / 失败」四类逐项展示，允许只提交可安全删除的项                             |
| **确认强度** | 常规资源二次确认；项目永久删除输入**项目名**确认并说明资源统计、数据保留期与不可恢复性                                |
| **权限**     | 删除按角色权限控制，只读用户不显示删除入口；归档/删除动作写入审计日志                                                 |

### 4.5 保存并执行：两阶段反馈与并发冲突

适用：接口、用例、流程、CI 任务等提供「保存并执行」/「保存并测试」的入口。


| 规则                    | 要求                                                                                                               |
| ------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| **两阶段反馈**          | 操作反馈拆为「保存结果」与「执行结果」两个明确阶段，分别提示，不让用户误以为执行失败等于内容没保存                 |
| **保存成功 + 执行失败** | 保留已保存状态，提供「重试执行」，不重复保存                                                                       |
| **幂等**                | 触发执行携带幂等键 (`Idempotency-Key`)，网络重试不产生重复执行；提交按钮在请求期间禁用，防重复点击                 |
| **并发编辑冲突**        | 使用版本号或 ETag 检测多人同时编辑；保存时冲突则提供「重新加载 / 对比差异 / 另存副本」三种选择，不静默覆盖他人修改 |

### 4.6 Secret 与敏感信息权限边界

适用：环境 secret、数据源密码、MCP Token、CI 凭据、接口认证头等所有敏感字段。


| 规则               | 要求                                                                                               |
| -------------------- | ---------------------------------------------------------------------------------------------------- |
| **默认掩码**       | 普通用户对 secret 只看到「已配置」和掩码 (●●●●)，复制按钮默认不可用                            |
| **hover 永不泄露** | 变量引用 hover 预览对 secret 类型**永不显示真实值**，只显示掩码与说明                              |
| **短时揭示**       | 仅持有独立 secret 权限的管理员可短时揭示，并要求**重新认证**；揭示后自动恢复掩码（固定时长后关闭） |
| **审计**           | 查看、复制、更新、吊销 secret 均写入审计日志（操作者、时间、目标）                                 |
| **不落明文**       | 页面缓存、URL、日志、执行快照、本地草稿与浏览器历史不得保存 secret 明文；执行快照中敏感头/字段脱敏 |

### 4.7 AI 产物的确认模式（原「MCP AI 辅助通用模式」）

> **v2.6 改写**。原文给的是一个「🤖 按钮 → 抽屉里的对话面板 → 确认应用」的通用面板，前提是
> **平台自己调模型**。那个前提在 P5 被推翻：平台不调模型，模型调平台（计划 9.0 边界 16）。
> 所以本节保留的只有原文最后一行那个真正的约束——**AI 的产物必须由人确认才落库**——并把它
> 变成一条实现级规则；那个对话面板本身撤销。

**规则：AI 产出草稿，人在原编辑器里确认，不新增写库路径。**

外部 agent 经 MCP 工具产出的内容（一段响应校验脚本、一组断言、一份接口定义），**不由 agent
写库**，而是作为草稿回到前端，塞进**它本来就该出现的那个编辑器**：

| 产物 | 落点 | 现成的受控入口 |
| --- | --- | --- |
| 响应校验脚本 | 用例编辑器的响应脚本区 | `ResponseScriptEditor` 的 `{ scripts, onChange }` |
| 声明式断言 | 用例编辑器的断言区 | `AssertionEditor` 同款受控签名 |
| 接口定义 | 接口工作台的请求区 | 与「替换当前请求」（3.1.2）同一条回填路径 |

三条推论：

- **落点不是一个独立的对话面板**。草稿出现在用户本来就在看的那个编辑器里，处于**未保存**
  状态，用户改它、弃它、或按原本那个保存按钮存它——所以权限、审计、未保存离开确认全部是
  现成的那一套，**一条新的写库路径都不需要**。
- **「确认应用」不是一次提交，而是一次填充**。填完之后仍要走正常保存，这与 1.5 定的
  「保存并执行两阶段反馈」不冲突。
- **正确性由「再跑一次」回答，不由信任回答**。响应脚本每个 `ctx.assert` 调用的通过/失败会
  逐条出现在执行证据里（3.1.3 的用例执行结果），所以生成的脚本对不对，点一次运行就看到。

**对话入口本身后置**：聊天窗（含站内人物与新手引导）整体排在 MCP 之后，前置是用户注册、
用户权限与站内通知（计划 9.8）。在那之前，MCP 的消费方是平台外部的 agent / IDE / CI，
草稿通过它们回到编辑器。

---

## 5. 状态管理

### 5.1 状态分层

```
┌──────────────────────────────────────────┐
│           全局状态 (zustand store)         │
│  ─ 当前项目 (projectId)                   │
│  ─ 用户信息 (user)                        │
│  ─ 当前项目角色与权限 (role, permissions) │
│  ─ i18n 语言偏好 (language)               │
│  ─ 全局通知 (notifications)               │
├──────────────────────────────────────────┤
│      URL 状态 (useSearchParams) ★可深链     │
│  ─ 列表: 筛选条件, 分页, 排序 (不含选中)   │
│  ─ 抽屉/弹窗/Tab 开合 (?drawer/?edit/?tab) │
│  ─ 画布定位 (?node), 时间区间 (?from/?to)  │
├──────────────────────────────────────────┤
│         页面级状态 (useReducer)            │
│  ─ 编辑: 表单数据, 脏标记, 校验状态        │
│  ─ 编排: 画布节点, 连线, 选中状态         │
│  ─ 列表: 批量选中行 (会话级, 不进 URL)     │
├──────────────────────────────────────────┤
│       组件级状态 (useState)                │
│  ─ 纯 UI 瞬态: 加载态, hover, 折叠动画     │
└──────────────────────────────────────────┘
```

> **原则**: 凡是"希望能被外部深链/统计跳转直达、或需要刷新/前进后退保持"的状态 (列表筛选、分页、抽屉、Tab、画布定位)，一律放 **URL query (`useSearchParams`)**，不要只放 `useReducer/useState`。纯 UI 瞬态才用组件级状态。
>
> **URL 状态边界**: URL 只承载**可分享/可恢复**的导航状态（筛选、分页、排序、当前资源、抽屉开合）。**批量选中行、未提交草稿、编辑态不进入 URL**：前者为会话级状态（筛选变化或离开页面清空并提示），避免超长 URL 与危险操作上下文随链接扩散；草稿见 6.1 会话过期恢复。

### 5.2 请求状态管理 (ahooks useRequest + URL 同步)

```typescript
// 列表查询参数来自 URL，保证可深链/刷新保持/统计跳转
const [sp, setSp] = useSearchParams();
const params = {
  keyword: sp.get("keyword") ?? "",
  status:  sp.get("status") ?? undefined,
  page:    Number(sp.get("page") ?? 1),
};

const { data, loading, error, refresh } = useRequest(
  () => api.getEndpoints(projectId, params),
  {
    refreshDeps: [projectId, sp.toString()], // URL 变即重查
    debounceWait: 300,
    cacheKey: `endpoints-${projectId}-${sp.toString()}`,
    onError: (err) => message.error(err.message),
  }
);

// 交互回写 URL (而非本地 state)，外部即可通过链接复现该视图
const onFilter = (next) => setSp({ ...Object.fromEntries(sp), ...next, page: "1" });
```

---

## 6. 路由设计

> **设计目标：全站可深链 (deep-link)**。每个可点资源都有稳定 canonical URL，列表筛选/分页与抽屉/弹窗态编码进 query，便于后续「统计 → 直达具体页」的跳转。

```typescript
// React Router v6 路由结构 (deep-link 友好)
const routes = [
  { path: "/login", element: <LoginPage /> },
  { path: "/403",   element: <Forbidden /> },   // 无权限深链落地
  { path: "/404",   element: <NotFound /> },    // 资源不存在/已删除
  {
    path: "/",
    element: <AuthGuard><AppLayout /></AuthGuard>,
    children: [
      { index: true, element: <Navigate to="/dashboard" replace /> },
      { path: "dashboard", element: <GlobalDashboard /> },
      { path: "projects", element: <ProjectListPage /> },
      { path: "system/:tab?", element: <SystemManagement /> },
      { path: "users", element: <UserManagement /> },
      {
        path: "projects/:projectId",
        element: <ProjectLayout />,
        children: [
          { index: true, element: <Navigate to="overview" replace /> },
          { path: "overview", element: <ProjectDashboard /> },
          // ── 接口模式 ──
          { path: "endpoints",       element: <EndpointList /> },     // ?keyword&method&tags&page  ?drawer=create|edit&id=
          { path: "endpoints/:id",   element: <EndpointDetail /> }, // ?run=:executionId&case=:caseId&status=&environment=&from=&to=
          { path: "environments",    element: <EnvironmentManager /> },// ?env=dev  (Tab 由 query 定位)
          { path: "test-suites",     element: <TestSuiteList /> },
          { path: "test-suites/:id", element: <TestSuiteEditor /> },
          { path: "test-suites/:id/executions/:execId", element: <SuiteExecutionDetail /> },
          { path: "flows",           element: <FlowList /> },
          { path: "flows/:id",       element: <FlowDAGEditor /> },     // ?node=  定位/高亮画布节点
          { path: "flows/:id/executions", element: <ExecutionHistory /> },
          { path: "executions/:id",  element: <ExecutionDetail /> },   // ?node=  高亮失败节点
          { path: "mocks",           element: <MockList /> },
          { path: "mocks/:id",       element: <MockRuleEditor /> },
          { path: "data-sources",    element: <DataSourceList /> },
          { path: "data-sources/:id",element: <DataSourceDetail /> },  // ?tab=config|query
          { path: "mcp",             element: <McpPage /> },           // v2.6：平台作为 MCP Server 的管理页（开关 / Token / 工具清单）
          // ↓ 撤销（v2.6）：那是「平台当 Client 去调外部 MCP Server」，整节移出当前范围
          // { path: "mcp-servers",     element: <MCPServerList /> },
          // { path: "mcp-servers/:id", element: <MCPServerDetail /> },
          // ── 仓库模式（P4.5-14 起是一个导航项、五个标签，路由前缀 `repo`） ──
          { path: "repo",            element: <CiTaskList /> },        // 任务：第一个标签，所以它是索引路由
          { path: "repo/tree",       element: <RepoCaseTree /> },      // ?endpoint=&repo=&status=&onlyUncovered=1  定位树节点
          { path: "repo/runs",       element: <IngestRunList /> },
          { path: "repo/unmatched",  element: <IngestUnmatched /> },
          { path: "repo/credentials",element: <RepoCredentials /> },   // 上报 Token + 三步接入 + 项目级 Git 凭据池（原 `repo-cases/onboarding` 撤销）
          { path: "repo-cases/:caseId", element: <RepoCaseTree /> },      // 2026-08-30 落地为**抽屉**而非独立路由：点树上用例行就地打开（身份/涉及接口/请求步骤），不打断树的浏览状态（用户确认）
          { path: "ingest-runs/:id", element: <IngestRunDetail /> },
          { path: "ci-tasks",        element: <CITaskList /> },           // 撤销：任务并入 `repo`（P4.5-14）；新建/编辑是**抽屉**，`ci-tasks/new` 与 `ci-tasks/:id` 两条路由此前已撤销
          { path: "ci-tasks/:id/runs", element: <CIRunList /> },          // 撤销：任务历史 = 执行记录页 `?type=parent&kind=runner&keyword=<任务名>`，不为同一份 ExecutionIndex 开第二个入口
          { path: "pipeline-runs/:runId", element: <PipelineRunPage /> },  // ?tab=log|cases|report|artifacts（P4.5-13）
          { path: "workers",         element: <WorkerPool /> },           // 撤销：Runner 池落在 `/system` 的相邻 tab（3.12.4），且它是**系统级**而不是项目级
          // ── 通用 ──
          { path: "reports",         element: <ReportDashboard /> },   // ?source=api|repo|ci&status=failed&endpoint=&from=&to=&page=
          { path: "reports/trends",  element: <TrendAnalysis /> },      // ?range=7d&metric=coverage|success|latency
          { path: "schedules",       element: <ScheduleList /> },       // ?drawer=create|edit&id=
          { path: "executions/by-index/:indexId", element: <ExecutionResolver /> }, // 统一执行入口, 按来源重定向
          { path: "settings/:tab?",  element: <ProjectSettings /> },    // tab=basic|members|env|alert|repository
        ],
      },
    ],
  },
  { path: "*", element: <NotFound /> },
];
```

### 6.1 路由交互规则

- 登录后的默认路由为 `/dashboard`。仅当“恢复上次页面”开启且 URL 可访问时，登录后恢复该 URL；上次项目不能作为默认跳转依据。
- 项目 ID 从 URL 参数获取；全局页面不显示项目选择器。
- **项目切换 (遵循 1.1/1.2 两模式约束)**: 切换项目时只保留**目标项目中可访问的模块级路径**（如 `endpoints`、`reports`），**不保留资源 ID、编辑态、执行 ID 与 `?run=` 等资源级参数**（具体资源 ID 不能跨项目复用）。目标项目未启用该模块或当前角色无权限时，回退到项目概览并说明「该项目未启用此模块」或「当前角色无权限」。切换前存在未保存内容时先触发离开确认。
- 当前选中的导航项、面包屑均由 URL 路径推导，保证任意深链进入时高亮/层级正确。
- 详情/编辑页通过 `:id` 参数定位，页面内支持未保存离开确认 (beforeunload + 路由守卫)。
- **会话过期 (401) 处理**: 401 由 axios 拦截器统一处理时，先保存 **canonical URL 与「非敏感」草稿**（localStorage，不含 secret/token/密码），再跳转登录页；登录成功后恢复原页面与草稿并提示「内容已恢复，请重新提交」。secret、Token、密码与敏感请求头**不得持久化**到本地草稿。
- **鉴权守卫**: `AuthGuard` 校验登录态 (未登录跳 `/login`)；进入项目校验成员身份，无权跳 `/403`；资源不存在/已删除跳 `/404`；页面内按角色控制操作按钮 (`usePermission`)。

### 6.2 深链接与统计跳转约定 (为后续统计下钻预留)

**a) URL 参数分工**


| 用途                   | 承载方式    | 示例                                                     |
| ------------------------ | ------------- | ---------------------------------------------------------- |
| 定位资源               | path param  | `/endpoints/:id`、`/executions/:id`                      |
| 列表筛选/分页/排序     | query param | `?status=failed&tags=smoke&sort=-created_at&page=2`      |
| 抽屉/弹窗/Tab/画布节点 | query param | `?drawer=create`、`?edit=:id`、`?tab=result`、`?node=n3` |

**b) 抽屉/弹窗即路由 (关键)**

创建/编辑/详情抽屉、结果抽屉、配置弹窗、Tab 一律由 query 驱动，进入路由时按 query 自动打开，关闭时清除 query：

```typescript
// 统一用 useSearchParams 驱动抽屉/弹窗，可被外部深链直接打开
const [sp, setSp] = useSearchParams();
const editing = sp.get("edit");                 // /endpoints?edit=ep_123 → 直接打开编辑抽屉
const openCreate = () => setSp({ drawer: "create" });
```

**c) 列表状态同步 URL**

列表页筛选、分页、排序不再只放 `useReducer`，而是**双向绑定 `useSearchParams`**：URL → 初始化查询；交互 → 回写 URL。这样统计可把筛选编码成链接，且支持刷新/前进后退/分享。**批量选中行不进入 URL**（会话级状态，见 5.1）。

**d) 规范化跳转目标 (统计模块统一按此拼 URL)**


| 统计/来源                   | 点击跳转目标 URL                                                                                  |
| ----------------------------- | --------------------------------------------------------------------------------------------------- |
| 接口卡片 / 某接口指标       | `/projects/:pid/endpoints/:id`                                                                    |
| 接口覆盖(仓库用例)          | `/projects/:pid/repo/tree?endpoint=:endpointId`                                                  |
| 未覆盖接口清单项            | `/projects/:pid/repo/tree?onlyUncovered=1`                                                       |
| 全局项目指标                | `/projects?status=active&sort=-last_execution_at`                                                 |
| 失败执行数                  | `/projects/:pid/reports?status=failed&from=&to=`                                                  |
| 某次执行 (任意来源)         | `/projects/:pid/executions/by-index/:indexId` (**统一入口**，内部按 source_type 重定向到具体视图) |
| CI 执行详情                 | `/projects/:pid/pipeline-runs/:id`                                                                |
| 覆盖率趋势点                | `/projects/:pid/reports/trends?metric=coverage&range=7d`                                          |
| 告警通知                    | 通知携带上述 canonical URL，点击直达                                                              |
| 调度失败 (已触发但执行失败) | `/projects/:pid/executions/by-index/:indexId` (进入该次执行详情，含「查看调度配置」次级入口)      |
| 调度本身无法触发            | `/projects/:pid/schedules?schedule=:scheduleId` (调度诊断，展示漏跑/重复记录)                     |

> **原则**: 统计/告警一律只认 `executions/by-index/:indexId` 这一个执行入口，由平台按来源解析重定向；各来源的具体详情路由供内部跳转使用，统计侧无需感知。

---

## 7. API 交互层

### 7.1 请求封装

```typescript
// api/client.ts
const httpClient = axios.create({
  baseURL: "/api/v1",
  timeout: 30000,
});

// 请求拦截: 注入 JWT Token
httpClient.interceptors.request.use((config) => {
  const token = getToken();
  if (token) config.headers.Authorization = `Bearer ${token}`;
  return config;
});

// 响应拦截: 统一错误处理
httpClient.interceptors.response.use(
  (res) => {
    if (res.data.code !== 0) {
      // 业务错误: 1002 跳登录, 1003 提示无权限
      handleBusinessError(res.data);
      return Promise.reject(res.data);
    }
    return res.data.data;  // 直接解包 data 字段
  },
  (err) => {
    // 网络/超时错误
    return Promise.reject(err);
  }
);
```

### 7.2 实时状态 (执行进度)

执行状态使用短轮询 (2s 间隔) 或 SSE (Server-Sent Events) 实现:

```typescript
// 轮询模式
const { data: executionStatus } = useRequest(
  () => api.getExecution(executionId),
  {
    pollingInterval: 2000,
    pollingWhenHidden: false,  // 页面不可见时暂停
    pollingErrorRetryCount: 3,
    ready: executionStatus === "running",  // 非运行中停止轮询
  }
);
```

---

## 8. i18n 国际化交互

### 8.1 语言切换

```
顶部 Header 语言切换器:
[中文 ▼]  ->  下拉菜单: 简体中文 / English
```

- 切换即时生效，无需刷新页面。
- 语言偏好存 localStorage + 同步到服务端。
- 接口返回的动态数据（执行结果、错误信息）不翻译。
- 仅翻译: 界面框架文案、按钮、提示、表单标签、校验消息。

### 8.2 翻译文件组织

```
src/locales/
├── zh-CN/
│   ├── common.json       # 通用: 确定、取消、保存、删除等
│   ├── endpoint.json     # 接口管理模块
│   ├── flow.json         # 流程编排模块
│   ├── ...
│   └── index.ts          # 汇总导出
├── en/
│   ├── common.json
│   └── ...
└── config.ts             # 语言配置
```

---

## 9. 错误处理与用户反馈

### 9.1 错误分级展示


| 错误级别 | 展示方式                     | 场景                       |
| ---------- | ------------------------------ | ---------------------------- |
| 轻量提示 | Toast (右上角, 3s 自动消失)  | 操作成功、状态变更         |
| 警告提示 | Alert (页面顶部横幅, 可关闭) | 部分失败、数据过期         |
| 错误详情 | Modal / 抽屉                 | 执行失败详情、导入错误列表 |
| 字段错误 | Form.Item 红色提示文字       | 表单校验失败               |
| 全局错误 | ErrorBoundary 降级页面       | 渲染崩溃                   |

### 9.2 加载状态

> **动效预算 (遵循 Quiet Console)**: 全局只允许**一个**环境动画 —— 执行中的呼吸点。首次加载使用**稳定骨架**，占位不做持续扫光/脉冲；过渡动画统一 120ms；`prefers-reduced-motion` 开启时完全关闭非必要动画。


| 状态     | 展示                                    |
| ---------- | ----------------------------------------- |
| 首次加载 | 骨架屏 (Skeleton)，静态占位，无扫光动画 |
| 请求中   | Spin 覆盖区域、按钮 loading 态、进度条  |
| 局部刷新 | 表格行内 Spin，不用 shimmer 扫光        |
| 执行中   | **唯一呼吸点** + 进度百分比/阶段进度    |

### 9.3 空状态

> **空状态区分五种语义**，只有「具备创建权限且模块已启用」时才显示创建按钮；只读用户隐藏创建操作。


| 类型       | 触发条件                                 | 展示                                   |
| ------------ | ------------------------------------------ | ---------------------------------------- |
| 首次为空   | 模块已启用且当前可创建，但还没有任何数据 | 插图 + 「创建第一个…」引导 + 创建按钮 |
| 筛选无结果 | 有数据但当前筛选不命中                   | 「没有符合条件的记录」+**[清除筛选]**  |
| 无创建权限 | 有数据查询权限但无创建权限               | 仅说明数据为空，**不显示创建按钮**     |
| 模块未启用 | 该能力尚未交付或项目未启用               | 「该能力未启用」+ 跳转/联系入口        |
| 加载失败   | 请求异常                                 | 错误摘要 +**[重试]**                   |

```
┌────────────────────────────────┐
│                                │
│        📄 (插图)               │
│    暂无接口用例                 │
│    创建第一个用例开始测试吧     │
│                                │
│        [创建用例]               │
│                                │
└────────────────────────────────┘
```

### 9.4 响应式与窄屏策略

> **断点与行为对齐 Quiet Console**，保证 320px 起无整页横向滚动（仅代码/表格区域可内部滚动）。


| 断点       | 行为                                                                         |
| ------------ | ------------------------------------------------------------------------------ |
| ≥940px    | 完整侧栏 + 顶栏，多栏布局                                                    |
| 700–940px | 侧栏收为可折叠的顶部导航条；环境/快捷入口折叠                                |
| 560–700px | 关键读数两列、断言逐条堆叠；抽屉/弹窗切为全屏                                |
| <560px     | 表格保留主列与操作入口，次要字段进行详情；复杂编辑器降级为只读或提示转桌面端 |

- **抽屉与弹窗**: 窄屏下抽屉切为全屏（不带固定宽度，不写死 520/600/750px）；宽屏下仍可指定合理宽度。
- **表格**: 按断点裁剪列，主列（名称、状态、操作）始终可见；其余字段折叠进行内「详情」。
- **复杂工作区**: DAG、SQL、日志等复杂编辑区在小屏定义明确策略——只读查看 / 简化编辑 / 提示转桌面端，不因窄屏损坏流程编辑完整性。
- **滚动**: 整页不出现横向滚动；代码、日志、表格区域允许内部滚动。

---

## 10. 性能优化策略


| 策略       | 实现                                     |
| ------------ | ------------------------------------------ |
| 代码分割   | React.lazy + Suspense 按路由懒加载       |
| 虚拟列表   | react-window 处理大列表渲染 (执行历史等) |
| 缓存       | ahooks useRequest 缓存 + 手动失效        |
| 防抖节流   | 搜索输入 300ms 防抖，滚动监听节流        |
| 图片/文件  | 懒加载 + 压缩                            |
| React Flow | 大流程使用节点折叠、视口裁剪             |
| Bundle     | Tree shaking + Gzip/Brotli               |

---

## 11. 关键交互流程总览

```
用户登录
    │
    ▼
选择/创建项目 (一个系统 = 一个项目) → 项目概览 Dashboard
    │
    ├──【接口模式】
    │    ├──▶ [接口管理] 导入/创建接口 → 接口工作台 (请求构建/发送/响应/调试记录)
    │    │         ├──▶ 发送验证后「保存为用例」(请求快照+环境+断言, 完全快照隔离)
    │    │         ├──▶ [环境管理] 配置多环境变量
    │    │         │         └──▶ [测试套件] 跨接口用例选择器组合用例/流程 → 批量执行
    │    │         └──▶ [流程编排] DAG 拖拽 → 连线 → 配置 → 执行 → 调试
    │    ├──▶ [Mock 服务] 为接口创建 Mock 规则
    │    ├──▶ [数据源] 添加数据库连接 → 在流程引用
    │    └──▶ [MCP] 开启暴露 → 签发 Token(读/写) → 外部 AI/工具经 /mcp 读资产、产草稿 → 人在编辑器里确认
    │
    ├──【仓库模式】一个导航项、五个标签（任务 / 用例树 / 上报记录 / 未匹配诊断 / 凭据）
    │    ├──▶ [凭据] 取上报 Token → pip install apitrack-sdk → 配两个环境变量 → 原样跑 pytest
    │    │         ├──▶ 用例归位「系统→接口→用例」树 (接口下有无用例=覆盖情况)
    │    │         └──▶ [未匹配诊断] 按频次看未登记路径 (诊断视图, 不是待办清单)
    │    └──▶ [任务] 新建任务(Git 地址+凭据/镜像/命令/缓存) → 自研 Runner 沙箱执行 → 实时日志 → 报告自渲染
    │
    └──【通用】[报告监控] 两模式统一报告+趋势   [定时调度] Cron
```
