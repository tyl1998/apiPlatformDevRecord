# 接口自动化平台 — 技术规格说明书 (Spec)

> 版本: v1.9  
> 状态: Draft
> 目标: 定义接口自动化平台的功能范围、架构设计、数据模型与技术选型

### 变更记录

| 版本 | 变更内容 |
|------|---------|
| v1.0 | 初始版本 |
| v1.1 | 1) 新增「项目与成员管理 / RBAC」完整落地（User/Role/Membership/ProjectSetting/AuditLog）；2) 新增「测试套件」批量执行；3) 明确「接口模式 vs 仓库模式」两种平行执行范式 (1.4)；4) 仓库模式拆为**仓库用例(上报式)** 与 **CI 任务** 两个独立能力：上报式经**唯一上报接口**把用例归位到「系统→接口→用例」树，含 commit 全量快照的合并/去重/删除对账；5) 修复执行引擎、定时调度重复触发、DAG 循环语义、Python 沙箱、敏感信息加密、大 payload 存储、MCP 连接状态、Webhook/触发安全等缺陷 |
| v1.2 | 1) **放弃 Jenkins 集成，改为自研 Runner (执行 Worker)**：默认常驻 Worker Pool (秒级启动 + 依赖缓存复用)，可选 Job per build 强隔离；Worker 主动外连协议 (register/claim/heartbeat/logs/artifacts/complete)，支持自托管内网部署；含容器沙箱、资源限额、出站白名单、secret 日志脱敏、孤儿任务恢复；移除 CIConnection/能力探测/config.xml 镜像/日志三级降级；2) 新增 **ExecutionIndex 统一执行索引** 并移除 `FlowExecution.executor_type` 矛盾字段；3) 补齐 CI 本地读模型 (`PipelineRunCase`/`PipelineRunArtifact`/`PipelineRunLogState`) 与上报 case 级结果 (`IngestCaseResult`)；4) 明确**一项目=一仓库、一仓库=多任务**基数约束，上报改为首次绑定+后续 git_url 校验 (防越权)；5) 新增零侵入 proxy 覆盖率采集 |
| v1.3 | 新增全局信息架构：登录后默认进入全局数据看板，项目管理与系统管理不再依附于项目工作台；明确数据看板与项目执行记录的职责边界；补全单接口就地历史、完整 HTTP 快照、cURL 导入和系统偏好设置需求。 |
| v1.5 | **撤回「场景组合」**：场景与流程编排解决同一个问题，但场景步骤只引用用例、请求参数不可在场景内编辑，导致「谁产出变量、谁消费变量」被切到多个页面；实现并实测后确认流程能更好地表达同一条业务链路，因此编排只保留流程一种模型。一并撤回**场景后置 SQL 校验**——「请求之后紧跟一步查库校验」就是流程里的一个数据库节点，不需要一个只能挂在末尾的特殊校验位。测试套件成员改为**接口用例与流程**，成员之间相互独立、不传变量。已产生的执行记录保留可读。 |
| v1.6 | **高级节点落地（P2-6）**：新增 2.2.3 记录循环 / 等待 / 子流程的已实现语义与保护性上限——循环为节点内迭代且迭代互相独立、子流程为引用而非快照、等待为有上限的同步短等待。明确整次执行共享固定并发预算且**容器节点不占额度**，嵌套不放大并发。3.2 的挂起-恢复模型标注为尚未实现。 |
| v1.7 | **P2-6 模型修订（用户验收后）**：并发与循环解耦——`RequestFlowNode.concurrency` 让「同一个接口发 N 个并发请求」直接在请求步骤上表达，不再绕道循环；循环改为**画布容器**（`LoopFlowNode.body` 内联子图，添加后往里加步骤、画布上框住它们），循环自身的并发指「N 次迭代在飞」，与请求并发正交。循环体内可以是任意节点包括子流程节点。2.2.3 相应改写。 |
| v1.8 | **P2-6 验收与缺陷统计**：2026-08-16 用户验收通过。开发周期共发现并处理 21 个缺陷（其中 2 个造成数据丢失：保存流程误删内联脚本、并发请求令牌记账失真），统计与教训见 `DEVELOPMENT_PLAN.md` 5.0.8「P2-6 缺陷统计」。并发不变式修正为「每个在飞的 HTTP 请求恰好持有一个令牌」；脏检查改为稳定序列化（jsonb 不保键序）；循环容器的尺寸持久化（`LoopFlowNode.size`）。遗留：流程「测试」5 处悬空脚本引用（用户选择不修复）、公共脚本「断言」类型退役（范围未确认）。 |
| v1.4 | 1) **数据源新增 SQL 定义（命名查询）**：数据源下可保存命名 SQL（名称/SQL/参数/说明），参数统一走 `{{variable}}` 绑定防注入，供 SQL 查询面板、场景数据库步骤、流程数据库节点复用；2) **场景支持数据库步骤与后置 SQL 校验**：场景步骤扩展为「接口用例 / 数据库步骤」两类，数据库步骤可引用已定义 SQL 或现场编写、参数引用前序用例输出、对查询结果断言、查询结果 JSONPath 提取为场景变量；场景末尾支持仅断言不传参的后置 SQL 校验（典型：接口执行后查库确认写入）；3) 流程数据库节点同步支持引用已定义 SQL。 |
| v1.9 | **仓库模式 2.10.1 落地定稿**：1) SDK 改为**零改动接入** —— `pip install apitrack-sdk` + 两个环境变量 + 原样跑 `pytest`，插件靠 `pytest11` entry point 自动加载、请求靠**传输层打桩**采集，**撤销**原「把请求集中封装进 `http_req.py` 并改用 `apitest_sdk.client`」的示例（那等于让用户重写全部请求代码，与零改动接入直接冲突）；2) `is_full_inventory` 收窄为**范围内的全量**，协议新增 `scope` —— 原写法把「指定了目录参数」当作非全量，会让按目录划分系统的仓库永远不对账；3) 用例↔接口改为**关系表**（一个用例挂它实际打过的每一个接口），只有 `call` 阶段的请求建立关系，`setup`/`teardown` 仍计入接口覆盖但不挂到用例节点；4) 对账**只看 inventory 不看 records**，关系累积不每轮重建；5) 未匹配区从待办清单改为**诊断视图**，**撤销**「从未匹配区补登记接口」的动作；6) 新增「未归位用例」分组。统计口径（覆盖率、看板、趋势）本阶段一律不动。 |

---

## 1. 项目概述

### 1.1 背景与目标

构建一个面向 API 测试与编排的自动化平台，覆盖接口管理、流程编排、断言验证、报告监控、定时调度、Mock 服务、MCP 工具集成、数据库源连接、接口用例与测试套件等核心能力。平台需同时服务技术用户（开发者）与非技术用户（业务/测试人员），提供可视化操作界面与灵活的扩展能力。

### 1.2 核心原则

| 原则 | 说明 |
|------|------|
| 稳定 | 核心引擎需具备重试、超时、事务保障，不丢任务、不误报 |
| 高效 | 单次 API 调用开销控制在毫秒级，支持并发执行 |
| 清晰 | 界面直观、日志完整、错误信息可追溯 |

### 1.3 UI/UX 设计原则

#### 1.3.1 多语言 (i18n)

- **初始支持**: 简体中文、英文 (可扩展)
- **实现方式**: react-i18next，翻译文件按语言拆分，按需加载
- **语言切换**: 用户设置中切换，偏好保存在本地并同步到服务端
- **动态内容**: 接口返回的动态数据（如执行结果）不翻译，仅翻译界面框架和提示文案

#### 1.3.2 简洁清晰的设计风格

- **布局**: 左侧导航 + 右侧内容区，导航层级不超过 3 级
- **色彩**: 克制使用色彩，主色不超过 2 种，大面积留白
- **信息密度**: 默认只展示关键信息，次要信息通过展开/悬停/抽屉展示
- **操作路径**: 核心操作（创建/执行/查看结果）不超过 3 次点击
- **反馈**: 每个操作都有明确的加载状态、成功/失败反馈
- **响应式**: 适配 1280px+ 桌面端，不优先考虑移动端

### 1.4 两种执行模式 (核心概念，务必区分)

平台提供**两条平行、互不替代**的测试执行范式，用户可在同一项目中按需选用，甚至混用：

#### 模式 A — 接口模式 (平台托管 / Platform-native)

- 测试资产 (接口、用例、流程、断言、参数) **全部定义并存储在平台内**。
- 由**平台内部 Worker 引擎**执行，可视化 / 低代码。
- 平台对执行过程**完全可见可控**：变量细粒度注入、Mock、数据源节点、MCP 节点、单步调试均生效。
- 结果实体：`FlowExecution` / `SuiteExecution`。

#### 模式 B — 仓库模式 (代码仓库，见 2.10)

- 测试逻辑在开发者的 Git 仓库中，平台**不拥有**测试代码。含两个独立能力：
  - **仓库用例 (上报式)**: 开发者装上平台 **SDK** (`pip install apitrack-sdk`) 并配两个环境变量，**测试代码一行不改**、`pytest` 命令不变；跑完后经**唯一的上报接口**上报。平台把用例归位到「**系统 → 接口 → 接口用例**」树 (与接口模式同构)，接口下有无用例即代表是否被覆盖。合并/去重/删除按 commit 快照对账，删除只在「范围内的全量 + 跟踪分支」两道闸门都成立时发生。
  - **CI 任务 (自研 Runner 执行)**: 平台自研轻量 Runner (执行 Worker) 拉取代码并在沙箱中运行用户自定义脚本，**不依赖 Jenkins**；默认常驻 Worker Pool (秒级启动、依赖可复用)，可选每次构建全新隔离环境。
- **平台内置能力 (Mock、数据源节点、MCP 节点、变量细粒度注入、单步调试) 对本模式不生效**。
- 结果实体：`IngestRun`/`IngestRecord` + `RepoTestCase` (上报式) 或 `PipelineRun` (触发式)。

#### 两模式对比

| 维度 | 接口模式 (平台托管) | 仓库模式 (代码仓库/CI) |
|------|---------------------|------------------------|
| 测试资产位置 | 平台数据库 | Git 仓库测试代码 |
| 编写方式 | 可视化 / 低代码 | 编写代码 |
| 执行者 | 平台内部 Worker | 上报式: 外部 CI (GitHub Action 等) / CI 任务: 平台自研 Runner Worker |
| 断言/数据 | 平台内定义、可见可管 | 代码内定义 (上报式可见被封装的请求) |
| 环境/变量 | 细粒度注入到每个请求 | 由 CI/仓库自身管理 |
| Mock/数据源/MCP/单步调试 | ✅ 生效 | ❌ 不介入测试内部 |
| 接口覆盖 | 执行即覆盖 | **上报式: 接口下有无仓库用例即覆盖情况** |
| 触发方式 | 手动/定时/Webhook/API | 上报式: CI 运行后上报 / CI 任务: 手动·定时·Webhook·API 触发自研 Runner |
| 结果实体 | FlowExecution/SuiteExecution | 仓库用例(RepoTestCase)+IngestRun / PipelineRun |
| 适用人群 | 测试/业务人员为主 | 开发者为主 |

> **归一原则**: 两种模式**仅在「报告与监控」层归一** (统一看通过率、趋势、告警、覆盖率)；定义、执行、调试三层各自独立。**不得**把 `IngestRun` / `PipelineRun` 混入 `FlowExecution` 模型，三者仅通过统一的执行索引 `ExecutionIndex` (`source_type: flow | scenario | suite | ingest | runner`) 在报表层聚合。

---

## 2. 功能规格

### 2.0 项目与成员管理 (新增)

> **核心定位**: 一个被测「系统」= 一个 **Project**。Project 是平台的资源隔离边界与权限边界，其下的接口、环境、数据源、MCP Server、用例、流程、测试套件、代码仓库均归属于该项目。

#### 2.0.1 项目管理

- **项目生命周期**: 创建 / 编辑 / 归档 (archive) / 删除。归档后只读保留历史，不再参与调度与执行。
- **系统级元信息**: 项目负责人 (owner)、系统类型、标签、描述、关联代码仓库地址 (衔接 2.10 仓库模式)。
- **项目级默认配置** (ProjectSetting): 默认环境、默认超时/重试策略、默认告警渠道、默认执行器 (内部 Worker / 外部 CI)。
- **资源归属**: 所有业务资源均带 `project_id`，跨项目不可见 (含 MCP Server，统一为项目级)。

#### 2.0.1.1 全局层、项目层与默认落点

- **登录后默认路由**: 首次登录或登录态失效后重新登录，一律进入全局「数据看板」；仅当用户主动选择“恢复上次页面”且该资源仍有访问权时，恢复上次 canonical URL。
- **全局层**: 数据看板、项目管理、系统管理。全局层不要求项目上下文，不展示项目内编辑表单。
- **项目层**: 仅在用户从项目管理或数据看板下钻到某项目后进入。接口、环境、用例、流程、CI 任务、项目执行记录等资源均在项目层。
- **项目切换**: 项目切换器只出现在项目层 Header；全局层不得因为上次项目选择而隐式进入项目。
- **系统管理**: 是独立页面而非侧栏角落按钮的集合，承载当前用户偏好和系统管理员能力。主题、语言等个人偏好必须在系统管理中可发现、可修改；可在 Header 保留快捷按钮，但快捷按钮只改变值，不替代设置页面。

#### 2.0.2 成员与角色 (RBAC 落地)

- **用户体系**: 平台维护 `User` 表 (账号、邮箱、密码哈希、状态、语言偏好)，支持本地账号，预留 OIDC/LDAP 对接。
- **角色定义** (系统内置四种):

  | 角色 | 作用域 | 权限概述 |
  |------|--------|---------|
  | 系统管理员 (admin) | 全局 | 管理所有项目、用户、全局配置 |
  | 项目管理员 (project_admin) | 项目 | 管理项目成员、设置、全部资源的增删改执行 |
  | 开发者 (developer) | 项目 | 资源的增删改与执行，不能管理成员/设置 |
  | 只读用户 (viewer) | 项目 | 只能查看与查看报告，不能创建/执行/删除 |

- **成员绑定**: 通过 `UserProjectRole` 将「用户 × 项目 × 角色」绑定，一个用户可在不同项目拥有不同角色。
- **权限校验**: API 层统一在项目资源入口做 `project_id + role` 校验；前端按角色做菜单/按钮可见性控制。
- **审计**: 所有变更类操作 (创建/修改/删除/执行/成员变更) 写入 `AuditLog`，记录操作人、对象、前后差异。

#### 2.0.3 全局数据看板

- **定位**: 登录后的默认统计页，回答“平台当前有哪些项目、各项目的测试资产和质量趋势如何”；不是某个项目的执行记录列表，也不承担单次执行排障。
- **统计范围**: 默认聚合当前用户有权限访问的全部 active 项目，支持按单项目、多项目、项目标签、时间范围筛选。
- **指标口径**:
  - 项目数：当前用户可见且 active 的项目数。
  - 接口总数：已登记 Endpoint 数。
  - 通过率：筛选窗口内已结束执行的 `success / (success + failed)`；无执行时显示 `—`，不显示 0%。
  - 完成率：已至少执行一次的可执行资产 / 已登记可执行资产；P0 仅计算 Endpoint，P1 起纳入用例、流程、套件、CI 任务。
  - 覆盖率：具备至少一个有效测试用例或仓库用例的 Endpoint / 已登记 Endpoint；P0 无用例能力时显示“待接入”，不得伪造百分比。
  - 接口用例、CI 任务、定时任务：展示总数和状态分布；未交付的阶段显示“未启用”，不得显示 0 误导用户。
- **下钻**: 所有指标必须链接到对应项目/资源的 canonical URL，并带入筛选条件。

#### 2.0.4 项目管理

- **页面定位**: 全局项目目录，使用卡片为默认视图；支持列表视图切换、关键词、状态和标签筛选。
- **卡片内容**: 项目名称、描述、负责人、状态、环境数、接口数、近 7 天通过率、最近执行时间，以及进入、编辑、归档/恢复、删除操作。
- **CRUD**: 有权限的用户可创建、查看、编辑、归档、恢复和删除项目；删除必须二次确认并说明历史数据处理策略。归档项目默认在筛选中隐藏，查看时只读。

### 2.1 接口管理

#### 2.1.1 接口注册

> **阶段归属**: 接口的增删改查属于 **P0**，不是后续阶段。接口是整个平台的最小可测单元，用例、流程、CI 都以它为基础，所以 P0 必须交付完整定义能力，而不是只能填 name/url 的简化版。

**P0 必须支持的接口要素**

| 要素 | 说明 |
| ------ | ------ |
| 基本信息 | 名称、描述、标签 |
| 请求行 | Method (GET/POST/PUT/PATCH/DELETE/HEAD/OPTIONS)、URL |
| 路径参数 | URL 中 `:id` 或 `{id}` 占位符，单独维护取值，替换时做 URL 编码 |
| Query 参数 | 键值对，执行时合并进 URL；与 URL 上已有的 query 共存 |
| 请求头 | 键值对，支持单行启用/停用而不必删除 |
| Body | `none` / `json` / `form` / `text`；JSON 需校验并可格式化 |
| 认证 | `none` / Bearer / Basic / API Key (header 或 query) |

**编辑形态 (强约束)**

- 接口新建与编辑必须是**独立路由页面**，不得使用弹窗。要素总量远超弹窗承载能力，弹窗一旦关闭即丢失全部输入。
- 编辑页需支持：未保存标识、离开前确认、关闭标签页前浏览器提示。
- 参数、请求头、Body、认证按页签组织，页签上显示已填数量，避免内容被折叠后无法察觉。

- 支持协议: HTTP/HTTPS (RESTful), GraphQL, WebSocket (只读)
- 接口分类: 按项目/模块/标签组织树形结构
- 列表检索: 关键字对**名称、路径、描述**做服务端模糊匹配，并支持按 Method 筛选
- 批量导入: 支持 OpenAPI 3.0 / Swagger 2.0 / Postman Collection v2.1 / 单条或多条 cURL。
- cURL 解析: 解析 method、URL、请求头、请求 body、`--data-raw`/`--data-binary`、`-u` Basic Auth；无法安全还原的 shell 表达式、文件引用和变量替换必须给出字段级警告，不得静默丢失。
- 导入预览必须标示新增、已存在、变更；按 `method + 标准化 URL` 检测冲突，支持全局跳过、更新、另建和逐项覆盖。

#### 2.1.1.1 接口工作台与执行历史

- **打开接口 = 打开它的编辑器**。不设只读详情页：查看请求长什么样和调整它是同一件事，拆成"详情页 + 编辑页"会让用户为了看一个请求头多点一次，也让两个页面都表达不完整。
- 工作台左侧是可直接编辑的定义区（请求行 + 参数/请求头/Body/认证页签），入参与请求头**无需二次点击即可见**。
- 工作台右侧是**运行记录卡片**，与定义区并排常驻：
  - 卡片可折叠为竖向导轨，折叠状态记忆到本地，编辑区随之变宽
  - 点击任一条记录，就地拉出完整快照抽屉，并把 `?run=<id>` 写进 URL
  - 执行历史只查询该 `endpoint_id`，支持状态筛选和分页
- 运行时若存在未保存修改，先保存再执行，保证跑的就是屏幕上这一份。
- 项目级「执行记录」仅用于跨接口、跨执行类型的审查和筛选，不能替代接口内执行历史。
- **执行记录必须可访问全量**: 执行记录是审计轨迹，不得只展示最近 N 条。列表走服务端分页，响应必须回传真实 `total`，前端提供页码与每页条数控制。任何"取一批就当作全部"的实现都不合格。
- **失败执行不得展示估算的阶段分解**: 响应时间瀑布图在没有真实分段数据时按总耗时估算。请求失败（连接未建立）时不存在可分摊的阶段，此时必须隐藏该图，不能显示编造的 DNS/TCP/TLS 数值。

#### 2.1.2 环境管理

- 多环境: 至少支持 dev / staging / prod 三级
- 变量系统: 全局变量 / 环境变量 / 接口级变量
- 变量引用语法: `{{variable_name}}`
- 内置变量: `$timestamp`, `$uuid`, `$randomInt`, `$randomStr`

#### 2.1.3 认证管理

- Basic Auth
- Bearer Token / API Key
- OAuth 2.0 (Client Credentials, Authorization Code)
- JWT (支持自定义 payload 生成)
- 自定义 Header 注入

#### 2.1.4 MCP 工具辅助生成接口用例

- **对话式创建**: 用户通过自然语言描述接口需求，通过 MCP 工具调用 LLM 自动生成完整的接口定义（URL、Method、Headers、Body、Auth）
- **智能补全**: 输入部分接口信息，MCP 工具推荐补全剩余字段
- **批量生成**: 一句话描述业务场景，MCP 工具批量生成一组相关接口用例（如"生成用户管理模块的 CRUD 接口"）
- **导入增强**: 导入 OpenAPI/Swagger 文档后，MCP 工具自动补充缺失的断言和示例数据
- **历史对话**: 保留对话上下文，支持多轮迭代修改接口定义

### 2.2 流程编排

#### 2.2.1 可视化编排

- 基于 DAG (有向无环图) 模型
- **画布编辑能力**：框选/平移模式切换、多选整组拖动、撤销与重做（⌘Z / ⇧⌘Z，历史记的是
  文档快照而非画布投影；输入框与脚本编辑器内的 ⌘Z 归它们自己）。
- **关于「循环节点」与 DAG 的关系**: 循环是**节点内部的迭代执行** (遍历数组/按次数)，不是图上的回边；编辑器的连线校验禁止产生图级环路，保证 DAG 语义成立。
- 节点类型:
  - **API 调用节点**: 执行 HTTP 请求
  - **数据库节点**: 执行 SQL 查询/执行/存储过程（引用已配置的数据源代理）
  - **条件节点**: 基于前序节点结果做 if/else 分支
  - **循环节点**: 遍历数组或按次数循环。见 2.2.3 的已实现语义与上限。
  - **脚本节点**: 执行自定义 JavaScript/Python 代码。JS 走 `isolated-vm` 进程内沙箱；**Python 走独立子进程/容器沙箱** (资源限额 + 网络禁用 + 只读文件系统)，两者隔离机制不同，见 3.3 技术选型
  - **MCP 工具节点**: 调用外部 MCP Server 提供的工具（如 LLM 调用、数据查询、消息推送等），通过 MCP Gateway 执行
  - **等待节点**: 固定延时或等待条件满足。**当前实现只做有上限的固定延时**，见 2.2.3。
  - **子流程节点**: 调用另一个已定义的流程
- 拖拽式编辑，实时保存

#### 2.2.3 高级节点：循环 / 等待 / 子流程（P2-6 已实现）

前四种节点（请求 / 数据库 / 脚本 / 条件）的语义见 2.3 与 4.x。这一节记录三种高级节点
**当前的实现语义与保护性上限**，与目标模型有差异的地方明确标注。

**等待节点**

- 固定延时，`waitMs` ≤ 30s。等待期间该节点**占用一个流程并发额度**，所以首版只做短等待。
- 「等待条件满足」以及不占槽位的长等待需要 3.2 的挂起-恢复模型，**尚未实现**。
- 取消运行或流程超时会立刻打断等待，不必等它睡完。

**子流程节点**

- 引用另一条同项目流程并原地跑一次。**引用而非快照**——与请求节点复制 endpoint 相反：
  请求节点快照是为了让流程独立于后续编辑，而子流程的价值是一份定义多处复用，所以改动
  必须影响所有调用方。单次运行的可复现性靠入队时把内层图连同脚本正文整份冻结来保证。
- 内层是**隔离作用域**：只能看到环境变量 + 内层流程自己的种子变量 + 显式入参。外层改
  变量名不会悄悄改变子流程读到的内容。
- 出参是**白名单**，且「未声明」与「声明为空」语义不同：未声明 = 内层产出全部带出，
  空 = 一个都不带（只要副作用）。

**循环节点（画布容器）**

- 迭代在**节点内部**，画布上不存在回边，因此外层图始终是 DAG，既有的分层并行、边条件、
  跳过传播、取消语义全部不变。
- **循环体是内联子图而非引用**：循环在画布上是一个**容器（虚线大框）**，被它框住的步骤是
  真正的画布子节点：可以把已有步骤**拖进框**、**拖出框**，**双击框内步骤直接编辑**，框上的
  `+` 也能直接往框里加一步。体内可以是任意节点，包括子流程节点——「循环内引用子流程」由此
  成立，不需要循环自己引用。体内步骤与流程**共享变量作用域**（直接读外层袋），不隔离。
- **连线不能跨越框**：框内每次迭代跑一遍、框外只跑一次，一条跨界的边没有可执行语义，编辑器
  当场拒绝。体内无连线时按数组顺序串成链执行。一个循环最多框住 10 步。
- **迭代互相独立**：每次迭代从同一份外层变量快照起步，读不到其他迭代写的值。这是循环
  并发可用的前提；跨迭代汇总应在循环之后放一个脚本节点读取出参完成。
- 两种模式：按次数、或遍历一个 JSON 数组变量（元素与序号各自暴露为一个变量名）。
- 出参按迭代序压实成 JSON 数组：声明 `orderId` 后，外层 `{{orderId}}` 是 `["A","B"]`，
  可直接整字段塞进后续请求体。未产出的迭代不占位，不会出现 `null` 空洞。

**请求步骤自身的并发（与循环正交）**

- `RequestFlowNode.concurrency`（≤ 10）：这个请求自己跑 N 次并发，提取按迭代序收集成
  JSON 数组，证据是该步骤下挂了 N 个子步骤行。**单步调试同样按并发跑**：并发 > 1 时返回
  容器步骤（N 条 HTTP 证据挂在它下面），而不是单条执行记录。
- **并发只属于请求**：它是「同一个接口发 N 个并发请求」的表达；数据库/SQL、脚本、等待
  并发跑没有价值，所以不给它们这个字段。一个步骤能选并发，用户就会下意识去选，而并发跑
  SQL 除了撞锁没有别的产出。

**并发预算（三种节点共同的约束）**

- 整次执行共享一个容量固定的令牌池（`FLOW_PARALLELISM`，默认 4 / 上限 10）。
  **只有叶子步骤取令牌，循环与子流程容器不取**——容器持令牌会死锁：容器占满额度后，它
  自己的内层图永远等不到令牌。
- 循环的 `concurrency` 与请求的 `concurrency`（均 ≤ 10）都是**局部信号量**，不抬高全局
  容量。因此嵌套三层不会把并发乘成 4×4×4，两个并发的循环也不会互相踩。它是形状声明，
  不是压测设施。

**其他上限**：迭代 ≤ 100（达上限直接失败，不静默截断）；嵌套深度 ≤ 3；嵌套总步骤数 ≤ 200。
子流程/循环的**跨流程循环引用**在保存、执行、入队三处检测，报错按流程名给出路径。

**执行证据**：容器节点内部的每一步都写入 `execution_steps`，用 `parent_step_id` 指回容器
行、`iteration` 标记第几次迭代——一次失败的循环必须能追到是里面哪个请求失败。
**父级计数只统计顶层步骤**，所以一个 20 次迭代的循环在 `total` 里是 1 步。

#### 2.2.2 MCP 工具节点

- **MCP Server 连接**: 在平台中注册外部 MCP Server（名称、URL、认证方式），通过 MCP Gateway 建立连接
- **工具发现**: 连接后自动发现 MCP Server 提供的工具列表及其输入 Schema
- **工具调用**: 选择工具并配置参数，参数支持引用流程上下文变量 `{{variable_name}}`
- **结果处理**: 工具返回结果可作为后续节点的输入数据继续流转
- **错误处理**: MCP 工具调用失败时支持重试和降级策略
- **MCP Server 管理**: 支持添加/编辑/删除 MCP Server 连接，查看连接状态和可用工具列表

#### 2.2.3 数据传递

- 节点间通过变量传递数据
- 支持 JSONPath / JMESPath 提取响应数据
- 支持字段映射与类型转换 (string / number / boolean / array / object)

##### 2.2.3.1 请求节点执行生命周期（确认稿）

请求节点统一采用以下阶段，不允许由页面 Tab 顺序或某一种脚本实现自行决定：

```text
已有环境/流程变量展开请求模板
→ 前置处理
→ 发送请求
→ 后置处理
→ 声明式校验
→ 节点成功后提交变量袋；节点失败则丢弃本节点临时变量
```

- **前置处理**：输入为变量展开和认证注入后的最终请求；可直接改写
  `ctx.request.method/url/headers/query/body/bodyText`，用于签名、加密、动态请求头等。
- **发送请求**：只发送前置处理结束后的 request，并以该最终请求生成请求快照。
- **后置处理**：统一承载简单字段提取与响应脚本。简单 JSONPath/Header/Status 映射走提取；
  遍历、聚合、格式转换、复杂关联校验与变量生成放进同一个响应脚本，避免为了“校验一次、
  提取一次”重复遍历响应。
- **声明式校验**：只保留状态码、Header、JSONPath、JSON Schema 等规则；自定义脚本从
  “校验”类别迁入后置处理的响应脚本。响应脚本可同时使用 `ctx.assert` 和
  `ctx.setVariable`，一次遍历同时完成业务判断和变量加工。
- **提交变量**：前置处理、字段提取和响应脚本写出的变量均先进入节点临时变量袋；只有请求、
  后置处理和声明式校验全部成功才提交到流程变量袋。失败、取消、跳过均不提交。

> **当前实现差异**：现执行器仍按“请求 → 脚本/声明式校验 → 提取”执行，脚本断言仍可写变量。
> 上述生命周期是已确认的后续重构目标；实现时允许忽略现有数据，不要求兼容旧脚本断言结构。

##### 2.2.3.2 `ctx.setVariable` 语义与传播范围

```js
ctx.setVariable("requestNonce", nonce);
// 脚本节点也可使用：return { requestNonce: nonce };
```

- `ctx.setVariable` 写入**当前节点临时变量袋**，不是修改当前请求模板，也不会触发第二次
  `{{variable}}` 展开。要影响当前请求必须直接改写 `ctx.request`。
- 同一节点内，后一个前置/后置脚本可通过 `ctx.variables.requestNonce` 读取前一个脚本写入值；
  后置处理也可读取前置处理生成的中间值，用于请求/响应关联校验。
- 当前节点整体成功后，变量提交到流程变量袋。后续依赖节点的请求、SQL、条件使用
  `{{requestNonce}}`，后续脚本使用 `ctx.variables.requestNonce`。
- “后续”指 DAG 依赖边上的后续 stage；同一并行 stage 的节点启动时读取同一份输入快照，
  不能互相读取。同 stage 写同名变量属于不确定输出，必须稳定失败。
- 失败分支不能读取失败节点未提交的临时变量。

##### 2.2.3.3 条件节点

- 条件节点支持扁平条件组：**全部满足（AND）**与**任一满足（OR）**，每个节点最多 20 条规则。
- 规则支持 `equals/notEquals/contains/exists/notExists/greaterThan/lessThan`。
- 条件节点有两个明确出口：满足（`on: true`）与不满足（`on: false`）；只激活命中链路，
  未命中的后继标记为 skipped。
- 执行证据逐条记录变量、操作符、期望值、实际值和单条结论，并记录最终 AND/OR 结论。

#### 2.2.4 错误处理

- 可配置重试策略: 固定间隔 / 指数退避 / 不重试
- 最大重试次数: 可配置 (默认 3 次)
- 超时控制: 可配置 (默认 30s)
- 失败降级: 可指定失败时的默认值或跳转到指定节点

### 2.3 断言与验证

#### 2.3.1 内置断言

- 状态码断言: `equals`, `notEquals`, `in`, `notIn`
- 响应头断言: 同上
- 响应体断言:
  - 精确匹配 / 包含 / 正则匹配
  - JSON 路径取值后比较
  - 数组长度 / 元素存在性

#### 2.3.2 Schema 校验

- 支持 JSON Schema (draft-07)
- 自动从响应体推断 Schema
- 校验结果详细到具体字段

#### 2.3.3 响应脚本（确认目标）

- 支持 JavaScript (沙箱执行)
- 注入 `ctx`: `ctx.response`（`.status` / `.headers` / `.body` / `.text`）、
  `ctx.variables` 读变量、`ctx.setVariable(名, 值)` 写回、`ctx.assert` 断言、
  `ctx.crypto` / `ctx.encoding` 签名与编码、`ctx.debug` 记录中间值
- 响应脚本属于请求节点的**后置处理**，不再属于“校验”Tab。它可在一次响应遍历里同时
  聚合数据、执行复杂断言并写入后续变量；声明式校验仍在后续独立执行。
- **在脚本内直接断言**，不靠返回值决定校验结论。无断言且无报错即通过；响应脚本的
  `return` 对象与 `ctx.setVariable` 均写入节点临时变量袋，节点成功后再提交。
- 示例:
  ```js
  ctx.assert.equal(ctx.response.status, 200);
  ctx.assert.greaterThan(ctx.response.body.data.length, 0);
  ctx.setVariable("token", ctx.response.body.data.token);
  ```
- 一个脚本多条断言时，默认第一条失败即中断；脚本内写 `ctx.assert.soft = true`
  改为收集全部失败再一起上报
- 每次 `ctx.assert` 调用（含通过的）逐条记录在结果里（`AssertionResult.checks`）；
  断言失败不再另生成重复的汇总文本——`checks` 明细是唯一信息源
- 响应脚本可带本地 `name`，同节点多个脚本按声明顺序执行，后者可读取前者写入变量。

> **当前实现差异**：当前产品仍显示“脚本断言”，`return` 仅作证据且脚本变量由
> `assertionVariables` 通道写回。后续重构时直接删除脚本断言类型，迁移为后置响应脚本，
> 现有数据可以忽略。

#### 2.3.4 脚本运行上下文与脚本库

- `ctx` 契约由服务端 `scriptContract` 单一来源按脚本类型提供：
  - assertion/响应脚本：`ctx.response + variables + setVariable + assert + crypto + encoding + debug`
  - hook/前置脚本：`ctx.request + variables + setVariable + assert + crypto + encoding + debug`
  - node/流程脚本节点：`variables + setVariable + assert + crypto + encoding + debug`
- 脚本库自身没有独立 ctx；库函数在调用方作用域执行，可通过参数或调用方 ctx 读取数据。
- 脚本库只允许顶层 `function`、`class`、`const/let/var` 声明（含箭头函数），禁止顶层调用、
  循环、分支及带副作用的静态初始化；库不能依赖另一个库，组合保持一层。
- Monaco 仅补全当前脚本已声明依赖的库符号，未声明依赖的符号不得提示，保证提示与运行时一致。

#### 2.3.5 MCP 工具辅助断言 (可选)

- **智能断言生成**: 输入自然语言描述，通过 MCP 工具调用 LLM 自动生成断言规则（如"确保返回的用户列表不为空且每个用户都有 id 字段"）
- **响应智能分析**: 接口返回不符合预期时，MCP 工具分析响应内容并给出可能的原因和建议
- **异常分类**: MCP 工具自动对失败原因进行分类聚合（如"鉴权失败"、"参数格式错误"、"服务端 500"）

### 2.4 报告与监控

#### 2.4.1 执行报告

- 每次执行生成完整报告
- 内容: 执行时间、总耗时、节点数、成功/失败数
- 每个节点的请求/响应快照：请求 Method、变量解析后的 URL、Query、Headers、Body；响应 Status、Headers、Body、Content-Type、截断标记、开始/结束时间和耗时。
- P0 的单接口执行记录也必须保存上述完整 HTTP 快照；请求或响应敏感 Header、环境 secrets、Authorization/Cookie 字段必须脱敏后再落库和展示。
- 响应 body 允许按大小阈值截断；截断记录必须标识原始字节数、保留字节数和对象存储引用（P0 未接对象存储时至少保留截断元数据）。
- 失败节点的错误堆栈与上下文

#### 2.4.2 趋势分析

- 成功率趋势图 (按小时/天/周)
- 响应时间趋势图 (P50 / P90 / P99)
- 失败分布统计 (按接口 / 按错误类型)

#### 2.4.3 告警通知

- 触发条件: 连续失败 N 次 / 成功率低于阈值 / 响应时间超过阈值
- 通知渠道: Webhook (通用) / Email / 企业微信 / 钉钉 / Slack
- 通知模板: 可自定义消息内容

#### 2.4.4 MCP 工具报告分析 (可选)

- **失败根因分析**: 执行失败时，MCP 工具自动分析请求/响应/日志，给出根因推测
- **趋势洞察**: MCP 工具定期扫描执行数据，发现异常模式（如"某个接口响应时间持续上升"）
- **优化建议**: 基于历史数据，MCP 工具推荐优化策略（如调整超时时间、增加重试、拆分流程）

### 2.5 调度与触发

#### 2.5.1 定时任务

- Cron 表达式调度
- 支持时区设置
- 支持一次性定时执行
- 日历排除 (节假日跳过)

#### 2.5.2 事件触发

- Webhook 接收: 提供唯一 URL，接收 POST 请求触发流程
- 参数注入: Webhook 请求体可作为流程输入变量
- **安全**: 每个 Webhook 配独立密钥，请求需携带 HMAC 签名 (如 `X-Signature`)，服务端校验；支持时间戳 + nonce 防重放

#### 2.5.3 程序化触发

- **API 触发**: `POST /api/v1/flows/:id/execute`，任何外部系统可通过 HTTP 请求触发流程执行
- **CLI 工具**: `api-auto run --flow <id> --env staging`，命令行触发，适合脚本集成
- 支持同步模式（等待执行结果返回）和异步模式（立即返回执行 ID）
- **幂等性**: 触发请求支持 `Idempotency-Key` 头，服务端在窗口期内对相同 key 去重，避免网络重试导致重复执行

### 2.6 Mock 服务

#### 2.6.1 自动 Mock

- 根据接口定义自动生成 Mock 端点
- 返回示例响应 (基于注册时的示例数据)
- 支持动态响应: 根据请求参数返回不同数据

#### 2.6.2 Mock 规则

- 固定响应: 返回预设的 JSON/XML
- 模板响应: 使用变量模板生成动态数据
- 代理模式: 转发到真实服务，可修改请求/响应

### 2.7 数据库源连接

#### 2.7.1 数据源管理

- 支持数据库类型: MySQL, PostgreSQL, SQL Server, Oracle, MongoDB, Redis
- 连接配置: 主机/端口/数据库名/用户名/密码/SSL，作为独立代理配置保存
- 连接池管理: 可配置最大连接数、超时时间、空闲回收策略
- 连接测试: 一键测试连接是否可用
- 敏感信息加密: 密码等凭据加密存储

#### 2.7.2 数据源代理

- 数据源作为独立代理配置，可在**流程编排**中通过 **数据库节点** 引用
- 每个数据源代理有独立的作用域和权限控制
- 支持在数据源上执行 SQL 查询 / 存储过程 / 集合操作
- 查询结果可作为**流程变量**传递给后续节点
- **SQL 定义（命名查询）**: 数据源下可定义并保存一组**命名 SQL 查询**（名称、SQL 文本、参数说明、描述）；定义可被 SQL 查询面板与流程数据库节点复用，避免同一 SQL 多处粘贴漂移。参数统一使用 `{{variable}}` 语法，由执行侧做参数绑定（防注入）

#### 2.7.3 数据库节点

- **SQL 查询节点**: 执行 SELECT 查询，返回结果集
- **SQL 执行节点**: 执行 INSERT/UPDATE/DELETE，返回影响行数
- **参数化查询**: 支持 `{{variable}}` 语法注入流程变量，防止 SQL 注入
- **结果映射**: 查询结果自动映射为 JSON，支持 JSONPath 提取
- **事务支持**: 多个数据库节点可组合在一个事务中执行
- **引用已定义 SQL**: 节点/步骤可选择引用数据源下的命名 SQL（见 2.7.2），或直接编写 SQL

#### 2.7.4 数据验证

- 数据库断言: 将数据库查询结果与接口响应进行比对验证，在**流程数据库节点**中完成
- 数据准备: 在流程执行前通过数据库节点准备测试数据
- 数据清理: 流程执行后自动清理测试数据。**注意**: 数据库操作可事务回滚，但流程中已真实发出的 HTTP 请求 (POST/PUT/DELETE) 无法回滚，需通过**补偿式清理** (调用反向接口 / 后置清理脚本) 处理
- **落库校验**: 「接口执行后查库确认已写入」在流程里表达为「请求节点 → 数据库节点」，由数据库节点断言返回行数 / 字段值。曾计划的「场景后置 SQL 校验」已随场景撤回（v1.5）

### 2.8 接口用例

#### 2.8.1 接口用例

- **用例定义**: 一个接口用例 = Endpoint 请求快照 + 环境 + 前置处理 + 后置处理 + 声明式校验
- **处理阶段**: 前置处理修改最终请求；后置处理由字段提取和响应脚本组成；复杂响应遍历、聚合、
  相关校验与变量生成应在一个响应脚本内一次完成，不拆成两次重复遍历
- **用例参数化**: 支持多组参数数据驱动（从 CSV/JSON/数据库读取多组入参，逐组执行）
- **用例标签**: 支持打标签分类（如 smoke/regression/sanity）
- **用例导入导出**: 支持 JSON/YAML 格式导入导出，方便版本管理

#### 2.8.2 场景组合（已撤回，v1.5）

> **本节能力不再交付。** 场景曾按「多个步骤按顺序拼接 + 数据传递 + 条件逻辑」实现过一轮
> （步骤类型为接口用例 / 数据库步骤，含后置 SQL 校验），实测后撤回。
>
> **原因**：场景与流程编排（2.6 DAG）解决同一个问题，但场景的「步骤只引用用例」模型把上下文
> 切开了。一条真实链路——先调生券接口、再把券号传给发券接口——在场景里要求先去接口工作台
> 给生券用例定义提取变量、回到场景、再进发券用例把 `{{券号}}` 写进请求体：请求参数属于用例，
> 不能在场景页编辑，于是「谁产出、谁消费」必须跨多个页面对齐。若把入参绑定与前置/后置脚本
> 都搬进场景页，它就成了流程编排的第二套实现。
>
> **替代方式**：多步骤业务链路统一用**流程编排**表达——同一张画布上编排请求节点、数据库节点、
> 脚本节点与条件节点，变量提取与消费在一处可见。批量回归用**测试套件**（2.9）。
>
> **一并撤回**：「场景后置 SQL 校验」。它的真实需求是「请求之后紧跟一步查库校验」，在流程里
> 就是加一个数据库节点，不需要一个只能挂在末尾、语义特殊的第二种校验位置。

#### 2.8.3 用例与流程的关系

- **用例是单接口的回归单元**: 一条用例 = 一个接口的请求快照 + 校验，适合快速验证与批量回归
- **多接口链路用流程**: 需要跨接口传值、库校验、分支或并行时，建成一条流程
- **共享执行引擎**: 用例与流程共享同一套 Worker 执行引擎和报告系统

### 2.9 测试套件 (新增)

> 解决「一键运行一组用例/流程」的高频需求 (如回归、冒烟)。

- **套件定义**: 测试套件 = 一组**接口用例与流程**的集合 (可手动选取，或按标签动态匹配，如 `tag = smoke`)。
- **成员相互独立**: 每个成员是一次独立执行——用例走单次执行，流程走流程执行；成员之间**不传变量**。
  需要传值的链路本身就应该建成一条流程，而不是靠套件的成员顺序隐式串起来。
- **执行策略**: 串行 / 并行 (可配置并发度)、失败即停 (fail-fast) 或全部执行。
- **触发方式**: 手动、定时调度、Webhook/API、CI 回调，与流程共用触发能力。
- **套件报告**: 汇总每个成员的通过/失败/耗时，输出总通过率，可导出 JUnit/JSON。

### 2.10 仓库模式 (代码仓库接入)

> **定位**: 本模式 (即 1.4 的「模式 B」) 与「接口模式」平行。测试逻辑在开发者的 Git 仓库中，平台不拥有测试代码，只负责**接收上报 / 触发 CI + 记录 + 覆盖可视化**。包含两个相互独立的能力：
> - **仓库用例 (上报式)**: 开发者用 SDK 封装请求并上报，平台把用例归位到「系统 → 接口 → 接口用例」树中展示 —— 与接口模式的结构完全一致，只是用例来源是上报。
> - **CI 任务 (自研 Runner)**: 平台自研 Runner 拉代码并在沙箱运行用户脚本，不依赖 Jenkins；代码怎么跑由用户自定义。

> **边界声明**: 平台内置能力 (**Mock、数据源节点、MCP 节点、变量细粒度注入、单步调试、DAG 编排**) 对本模式不生效；与接口模式仅在「仓库用例树 + 报告监控」层面归一。

> **基数约束 (重要，贯穿数据模型与 UI)**:
> - **一个项目 = 一个仓库** (`Repository.project_id` 唯一)。项目即系统，系统对应其唯一代码仓库。
> - **一个仓库 = 多个 CI 任务** (`CITask.repository_id` 多对一)。同一仓库可建多个任务 (如 冒烟/回归/夜间全量)，共享同一份 SCM 配置。
> - 因此: 仓库在 UI 上是**单例视图** (不是列表)；`case_key` 在项目内即唯一 (无需再叠加仓库维度)；CI 任务列表挂在该仓库之下。

#### 2.10.1 仓库用例 (上报式) — 核心

**结构**: 新增独立页面「仓库用例」，树形与接口模式一致：**系统 (项目) → 接口 (Endpoint) → 接口用例 (来自上报的仓库用例)**。接口下有用例 = 已覆盖，没有 = 未覆盖，覆盖情况一目了然，**分母天然就是本系统已登记的接口，无需另外绑基线**。

**采集 SDK (零改动接入，P4 落地时定稿)**:

- 平台发布采集库 `pip install apitrack-sdk` (导入名 `apitrack`)。**接入只有三步：装包 + 配两个环境变量 (`APITRACK_URL` / `APITRACK_TOKEN`) + 原样跑 `pytest`**。不改 `conftest.py`、不加 `-p` 参数、CI 命令一个字都不改。
  - pytest 插件靠 `pytest11` entry point 自动加载；
  - 请求靠**传输层打桩**采集 (`requests` 打 `HTTPAdapter.send`，`httpx` 打 `HTTPTransport.handle_request` 及其 async 版)，两个库都靠探测，装了哪个打哪个；
  - 桩体只往内存 list 追加一条 dict，**全程零网络**；上报只在 `pytest_sessionfinish` 发生一次，5 秒超时 + 一次重试，失败只 warning、**不动退出码**。
  - `APITRACK_TOKEN` 缺失时 SDK 完全 no-op，连桩都不打——本地跑测试等于它不存在。
- 用例身份与显示名分开：`case_key` = pytest nodeid 去掉 `[...]`，`name` = docstring 首行 (`@case("…")` 可覆盖)，`description` = 首行之后全部，`tags` = pytest marker ∪ `@case(tags=[…])`。参数化用例在树上是**一条**用例、多个子结果。
  ```python
  # 可选：只在需要让身份独立于文件位置与函数名时才用
  from apitrack import case

  @case("创建订单-VIP", key="order.create.vip", tags=["smoke"])
  def test_create_order():
      ...
  ```
- 每次请求采集: method、raw path、状态码、耗时、脱敏后的请求头键名、`phase` (setup/call/teardown)。请求体**默认不传** (`APITRACK_BODY_CAPTURE=1` 才带且截断)；`Authorization` / `Cookie` 与名字含 `token|secret|key|password` 的头在 SDK 侧就只留键名。
- **撤销原方案**：先前示例要求把请求集中封装进约定文件 `http_req.py` 并改用 `apitest_sdk.client`。那等于让用户重写全部请求代码，接入率会止步于「愿意重构的那几个仓库」，与「零改动接入」这条硬指标直接冲突。用户仍可自行封装，但那不是接入前提。

**唯一的上报接口 (平台只暴露这一个入口)**:

- SDK/脚本在运行结束时，把「用例清单 (inventory) + 执行记录 (records)」一次性 POST 到平台上报接口，携带 run 元数据 (repo、commit、branch、ci_run_id、`is_full_inventory`)。
- 平台按上报数据把用例归位到「系统→接口→用例」树。**上报接口用项目级 API Token 鉴权** (存 CI Secrets)，Token 的生成/吊销放在「仓库用例」页面，不进项目设置。
- **仓库绑定 (首次绑定 + 后续校验，防越权)**: 项目内仓库唯一。首次上报时按 Token 所属项目 + 上报的 repo 元数据**登记该唯一仓库**；此后每次上报都校验 `git_url` 是否与已绑定仓库一致，**不一致直接拒绝** (返回明确错误)，避免持有 Token 的 CI 伪造任意 repo 污染用例树。需更换仓库时由项目管理员在 UI 显式解绑/重绑。

**合并 / 去重 / 删除判定 (每次上报是一个 commit 快照，非增量)**:

- **身份键 `case_key`**: 默认 `模块/文件::用例名` 归一，可用 `@case(key=...)` 覆盖；按声明的 `endpoint` 归位。
- **合并 (upsert)**: 按 `(project, case_key)` upsert，更新 `last_seen_commit` / `last_ingest_run_id` / `last_result` / tags；请求记录按 run 落**不可变日志**，接口覆盖按 endpoint 取**并集**。
  - **用例不存 endpoint 外键，而是一张关系表** (`repo_case_endpoints`)：一个用例打三个接口是常态 (先登录再下单再查详情)，归给「主接口」会让另外两个显示未覆盖，而它们明明被测到了。因此 upsert 的键里**没有** endpoint。
  - **只有 `call` 阶段的请求建立用例关系**。登录、造数这类请求发生在 fixture 里，直接归进去会得到「登录接口的用例数 = 恰好第一个用例」这种失真。`setup` / `teardown` 的请求**仍进不可变日志并计入接口覆盖**，但不挂到用例节点上——于是 `POST /login` 显示为「已覆盖，但没有专属用例」，这正是事实。
  - **两个计数口径不同，UI 必须写明**：接口节点上的「用例数」数的是**关系数**，项目级「仓库用例总数」数的是 **distinct `case_key`**，两者相加不相等。
  - **有 inventory 但一个接口都没打到的用例进「未归位用例」分组**：树是「系统 → 接口 → 用例」，它没有地方挂，但不能藏——藏了之后「上报了 36 条用例，树上只数出 31 条」会被当成 bug 报上来。
- **去重**:
  - 同一 run 内同 `case_key` (参数化) → 归并到同一用例、按 param 存子结果；批次重投用 `(run_id, record_seq)` 幂等。
  - 跨 run 以 `(repo, commit, ci_run_id)` 幂等，重投是合并不是新增。
- **删除判定 (「范围内的全量」，P4 落地时收窄)**: 全量对账要过**两道闸门** —— run 标记 `is_full_inventory=true` **且** 上报分支等于仓库配置的 `tracking_branch`；实际有没有对账记在 `ingest_runs.reconciled`。
  - **范围与过滤是两件事**。`scope` (相对仓库根的路径数组，随上报带上，取自 pytest 自己的参数) 说的是「这次跑的是哪一片」；`is_full_inventory` 说的是「**这一片里**有没有过滤」。`-k` / `-m` / `--lf` / `--ff` / `-x` / `--deselect` / 收集报错一律判非全量。
  - **对账只删 `file_path` 在 `scope` 之内、且本次 inventory 里缺失的用例**，范围外一律不动。原写法把「指定了目录参数」当作「不是全量」，会让按目录划分系统的仓库 (CI 每次都是 `pytest tests/order/`) **永远不对账**，树只增不减。
  - **对账只看 inventory，绝不看 records**：被 `skip` 的用例、这一轮没打到任何接口的用例，records 里是空的；按「没报到就算消失」去删，一次全量跑会把所有 skip 的用例判成消失。因此**用例 ↔ 接口关系是累积的，不是每轮重建**。
  - 软删保留历史与最后结果；用例重新出现 → 自动恢复 `active`。**宁可少报一次全量** (本该删的用例多留一天)，**不可多报一次** (跑个 smoke 删掉 90% 的树)。
- **path 归一**: `/users/123` → `/users/{id}`，规则**留在平台侧** (SDK 只报 raw path)——归一规则会随 endpoints 的实际形状演进，而 SDK 装在用户仓库里、版本不可控，冻在用户机器上就再也改不动了。匹配不到的路径进「未匹配诊断」区：它是**按频次排序的诊断视图，不是待办清单**，保留「关联到已登记接口」「标注为外部依赖前缀」与**单行**「登记接口」三个动作（2026-08-30 修订：登记是显式决定，建最小接口并按匹配语义归位历史、同形 409 拦截；**不做批量补登记/「全部处理」**——批量清空列表的形状才会把别的系统的接口拉进本项目的分母）。

#### 2.10.2 CI 任务 (自研 Runner 执行) — 不依赖 Jenkins

> **决策**: 放弃集成 Jenkins，改为平台自研轻量 **Runner (执行 Worker)**。理由：彻底消除 Jenkins 版本/插件差异、能力探测、`config.xml` 镜像、日志接口降级等一整类兼容负担；执行协议由平台自己定义与版本化。

**Runner 只需具备四项核心能力**: ① 沙箱运行脚本 ② SSH 拉取代码 ③ 状态回调 ④ 报告/产物上传。

##### a) 执行模式 (两种，默认常驻)

| 模式 | 启动 | 依赖复用 | 隔离 | 定位 |
|------|------|---------|------|------|
| **常驻 Worker Pool** (`shared`) | **秒级** | **天然复用** (同一文件系统, pip/npm cache 常驻) | 与传统 CI agent 同级 | ✅ **默认** |
| **Job per build** (`isolated`) | 较慢 (镜像拉取 + 依赖重装) | 需缓存卷/预装镜像 | 每次全新环境，强隔离 | 可选，按任务开启 |

- **默认走常驻模式**: 一组常驻 Worker 主动领取任务，每个任务使用**独立 workspace 目录**，执行完清理；同一仓库二次执行时依赖缓存仍在，避免每次 `pip install`。
- **强隔离模式**: 每次构建新建容器/K8s Job，跑完销毁；必须配合下方冷启动优化，否则体验不可接受。

##### b) 冷启动与依赖优化 (必须实现)

- **预装镜像 (最有效)**: 平台提供基础运行镜像 (python/node/git/allure 等)；**任务级可指定自定义镜像**，团队可把自己的 `requirements.txt` 预烤进镜像，使 `pip install` 近似 no-op。
- **依赖缓存卷**: 常驻挂载缓存目录 (`PIP_CACHE_DIR` / npm cache)，从本地 wheel 安装，不走网络。
- **venv/依赖目录缓存**: 以 `requirements.txt` 等依赖清单的 hash 为 key 缓存整个依赖目录，**命中则完全跳过安装**；未命中安装后回写缓存。
- **镜像预热**: 节点侧预拉常用镜像，配合 `IfNotPresent` 策略消除拉取耗时。

##### c) Worker 与平台的通信协议 (全部由 Worker 主动发起)

> **关键设计**: 平台**永不主动连接 Worker**，只有 Worker 连平台。因此自托管 Worker 可部署在客户内网/NAT 之后**无需开放任何入站端口**，天然解决"测试内网服务"的可达性问题。

```
Worker → POST /worker/register           {worker_token, labels, capacity} → worker_id
Worker → GET  /worker/jobs/claim         长轮询领取任务
         ← JobSpec {
             run_id, repo{ssh_url, deploy_key}, ref,
             image, steps[], env, secrets[], timeout,
             cache_config, report_paths[], proxy_config
           }
Worker → POST /worker/jobs/:id/heartbeat 心跳 (响应可下发 cancel 指令)
Worker → POST /worker/jobs/:id/logs      {offset, chunk} 增量日志流
Worker → POST /worker/jobs/:id/artifacts 产物/报告上传 (pre-signed URL 直传对象存储)
Worker → POST /worker/jobs/:id/complete  {status, summary, exit_code}
```

##### d) 执行流程 (借鉴成熟 CI 的 durable 做法)

```
claim job → 准备独立 workspace → git clone (SSH deploy key, 浅克隆)
  → 依赖缓存命中判定 (命中则跳过安装)
  → 将 steps 写为脚本文件 → spawn 进程 (注入 env/secret)
  → 流式读 stdout/stderr → 按 offset 增量推送日志
  → 退出码写入文件并捕获        # Worker 崩溃重启后可判定任务真实状态
  → 收集 report_paths → 上传 artifacts
  → complete 回调
```

- **退出码落文件**: 避免 Worker 异常重启导致 `PipelineRun` 永久卡在 `running`。
- **孤儿任务恢复**: 心跳超时未续约的任务由平台判定 `aborted` 并可重新入队。

##### e) 沙箱与安全 (核心成本，必须做对)

- **隔离**: 容器化执行 (常驻模式为容器内独立 workspace；强隔离模式为一次性容器/K8s Job)，rootless 运行。
- **资源限额**: CPU / 内存 / 磁盘配额 / **执行超时**，超时需 kill **整个进程树**，防僵尸进程占用槽位。
- **网络策略**: 出站白名单，**禁止访问平台内网、数据库与云元数据服务** (如 `169.254.169.254`)。
- **Secret 处理**: 通过环境变量注入，**日志输出做 masking 脱敏**，防止 `echo $TOKEN` 类泄露。
- **Worker 鉴权**: 注册需 worker token，未授权机器不得加入集群领取任务。
- **workspace 生命周期**: 任务结束即清理，配合磁盘配额与定期回收，防止磁盘打满。

##### f) 并发与调度

- 并发度 = Worker 实例数 × 每实例 executor 槽位数。
- 支持 **项目级并发上限**，避免单项目占满整个 Worker 池。
- 通过 `labels` 做任务与 Worker 的亲和匹配 (如需特定运行环境或内网可达的自托管 Worker)。

##### g) 报告与覆盖率

- **报告归一化**: 按任务配置的 `report_paths` 收集 JUnit / Allure 结果，解析为平台统一用例结果 (落 `PipelineRunCase`)，console log 与原始报告归档对象存储。
- **零侵入覆盖率采集 (自研 Runner 的额外收益)**: 因平台控制执行环境，可在 Worker 侧注入 capture 代理 (设置 `HTTP_PROXY`、镜像内预置 CA)，**测试代码零改动**即可捕获全部请求 → path 归一 → 匹配 endpoint → 计算覆盖。
  - **SDK 上报 (2.10.1)** = 精确路径，可拿到 case ↔ endpoint 语义映射；
  - **Proxy 捕获** = 零侵入路径，覆盖率够用但缺 case 语义；
  - 两者可并存，由任务配置选择。

#### 2.10.3 与其它模块打通

- **调度**: CI 任务由平台定时调度 (Scheduler) 统一驱动，不再依赖外部 CI 的触发器；上报式由外部 CI (GitHub Action 等) 自身按 push/定时触发。
- **归一**: 仓库用例进入「系统→接口→用例」树与接口模式并列展示；执行结果通过 `ExecutionIndex` 在「报告监控」统一聚合，底层各自独立。

---

## 3. 架构设计

### 3.1 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                        Client Layer                         │
│    Web UI (React)    CLI    API Client                      │
└──────────────────────┬──────────────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────────────┐
│                    API Service (Fastify)                     │
│  ┌─────────────┐  ┌──────────────┐  ┌───────────────────┐  │
│  │ Auth / RBAC  │  │ Project/EP/  │  │ Flow / TestCase │  │
│  │ User/Member  │  │ DataSource   │  │ / Suite         │  │
│  │              │  │ Management   │  │ CRUD (编排元数据) │  │
│  └─────────────┘  └──────────────┘  └─────────┬─────────┘  │
│  ┌─────────────┐  ┌──────────────┐            │             │
│  │ Scheduler    │  │ Event Rcvr   │  ┌─────────▼─────────┐  │
│  │ (Cron+分布式 │  │ (Webhook     │  │ BullMQ Producer   │  │
│  │  锁/单主选举) │  │  HMAC 校验)  │  │ (Enqueue Task)    │  │
│  └─────────────┘  └──────────────┘  └─────────┬─────────┘  │
│  ┌────────────────────────────────┐            │             │
│  │ Runner Orchestrator (任务派发/   │            │             │
│  │ Worker 注册·心跳/日志流/报告归一化)│            │             │
│  └────────────────────────────────┘            │             │
└─────────────────────────────────────────────────┼────────────┘
                                                   │
                    ┌───────────────────────────────┼──────────┐
                    │           Redis (BullMQ)       │          │
                    │  ┌────────────┐  ┌──────────┐  │          │
                    │  │ Task Queue │  │ Result   │  │          │
                    │  │ (List)     │  │ (Stream) │  │          │
                    │  └────────────┘  └──────────┘  │          │
                    └───────────────────────────────────────────┘
                                                   │
                    ┌───────────────────────────────┼──────────┐
                    │         Worker Service (独立集群)          │
                    │  ┌─────────────────────────────────────┐  │
                    │  │  BullMQ Worker (可多实例水平扩展)     │  │
                    │  │  concurrency: 10 (每实例可配置)      │  │
                    │  └──────────────┬──────────────────────┘  │
                    │                 │                         │
                    │  ┌──────────────▼──────────────────────┐  │
                    │  │         Flow Execution Engine        │  │
                    │  │  (DAG 解析 + 节点调度 + 数据传递)     │  │
                    │  └──────────────┬──────────────────────┘  │
                    │                 │                         │
                    │  ┌──────────────▼──────────────────────┐  │
                    │  │         Execution Runtime            │  │
                    │  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ │
                    │  │  │HTTP Client│ │ Script   │ │ Database │ │
                    │  │  │(axios)    │ │ Runner   │ │ Connector│ │
                    │  │  └──────────┘ │(sandbox) │ │(SQL/Mongo│ │
                    │  │               └──────────┘ │ /Redis)  │ │
                    │  │  ┌──────────┐ ┌──────────┐ └──────────┘ │
                    │  │  │Assertion │ │ Data     │               │
                    │  │  │ Engine   │ │ Transformer│              │
                    │  │  └──────────┘ └──────────┘               │
                    │  └──────────────────────────────────────────┘
                    └───────────────────────────────────────────┘

                    ┌───────────────────────────────┼──────────────────┐
                    │         MCP Gateway (独立部署)                   │
                    │  ┌──────────────────────────────────────────┐   │
                    │  │  MCP Client 连接外部 MCP Server          │   │
                    │  │  MCP 工具节点执行 / 智能断言 / 报告分析   │   │
                    │  └──────────────────────────────────────────┘   │
                    └──────────────────────────────────────────────────┘

                    ┌──────────────────────────────────────────────────┐
                    │      仓库模式 执行侧                              │
                    │  ┌──────────────────────────────────────────┐   │
                    │  │ 上报式: CI 跑 SDK/脚本 → 唯一上报接口       │   │
                    │  │   → 归位「系统→接口→用例」树 (合并/去重/删除)│   │
                    │  └──────────────────────────────────────────┘   │
                    │  ┌──────────────────────────────────────────┐   │
                    │  │ Runner Pool (自研执行 Worker, 常驻默认)     │   │
                    │  │  claim → git clone(SSH) → 沙箱跑脚本        │   │
                    │  │  → 增量日志流 → 报告/产物上传 → complete     │   │
                    │  │  (Worker 主动外连, 支持自托管/内网)          │   │
                    │  └──────────────────────────────────────────┘   │
                    └──────────────────────────────────────────────────┘

                    ┌───────────────────────────────┼──────────┐
                    │           Data Layer                     │
                    │  ┌─────────────┐  ┌──────────────┐      │
                    │  │ PostgreSQL   │  │ Object Store  │      │
                    │  │ (主数据)      │  │ (报告/日志)    │      │
                    │  └─────────────┘  └──────────────┘      │
                    └──────────────────────────────────────────┘
```

### 3.2 组件职责

| 组件 | 职责 | 扩缩容方式 |
|------|------|-----------|
| **API Service** | 接口/用例/流程/套件/数据源等 CRUD、用户/成员/RBAC、认证鉴权、Webhook 接收、任务入队 | 水平扩展，无状态 |
| **Scheduler** | Cron 定时调度。**多实例下通过分布式锁 / 单主选举 / BullMQ repeatable jobs 去重，避免重复触发** | 主从 (仅一个 active) |
| **Runner Orchestrator** | Worker 注册/心跳/存活判定、任务派发与并发控制、接收增量日志流、收集产物、解析 JUnit·Allure 归一化 | 随 API 或独立部署 |
| **Runner Worker** | 执行 CI 任务：git clone(SSH)、沙箱跑脚本、推日志、传报告。**主动外连平台**，支持自托管于内网 | 水平扩展 (常驻池) |
| **Ingestion / Coverage** | 接收上报式 SDK 的 run/records、API Token 鉴权、path 模板归一匹配契约、计算接口覆盖率与用例清单 | 水平扩展，无状态 |
| **Worker Service** | 从 BullMQ 消费任务、执行 DAG 流程、HTTP 调用、脚本执行、数据库查询、MCP 工具调用、断言验证、结果回写 | 水平扩展，无状态 |
| **MCP Gateway** (可选) | MCP Client 运行时，连接外部 MCP Server，执行 MCP 工具调用，提供智能断言和报告分析能力 | 独立部署，按需扩展 |
| **Redis** | BullMQ 任务队列、结果流、缓存、分布式锁 | 集群模式 |
| **PostgreSQL** | 持久化存储（项目/用户/接口/流程/执行记录） | 主从/集群 |
| **Object Store** | 报告归档、大日志、大响应快照 (DB 仅存引用) | 按需扩展 |

### 3.2.1 执行引擎与长耗时节点 (设计澄清)

> BullMQ 本质是**任务队列**而非工作流引擎，DAG 的编排状态需在其之上补齐：

- **DAG 状态机**: 每次执行创建一份可持久化的执行上下文 (存 Redis + 落 DB 快照)，由 Flow Execution Engine 按拓扑推进节点，记录已完成/待执行节点与变量。
- **长耗时 / 等待节点不阻塞 Worker**: `等待节点`、`子流程`、异步 CI 执行采用**挂起-恢复**模型——上下文出队暂存，由延时任务 (BullMQ delayed job) 或外部事件 (回调/轮询) 再次入队恢复，避免长期占用 `concurrency` 槽位。
  **当前实现与此不同**（见 2.2.3）：等待是有上限的同步短等待，占着流程并发额度；子流程与循环
  在父任务内部原地跑，容器本身不取并发令牌。挂起-恢复留给需要长等待或外部事件唤醒时再做。
- **可选升级**: 对超长运行 / 复杂补偿场景，可引入 Temporal 等工作流引擎替换自研状态机 (见附录参考)。

### 3.3 技术选型

| 层次 | 技术 | 选型理由 |
|------|------|---------|
| 前端框架 | React 18 + TypeScript | 生态成熟，组件库丰富 |
| UI 组件库 | Ant Design 5.x | 企业级组件，表格/表单/树形控件完善 |
| 多语言 (i18n) | react-i18next | 支持动态切换语言，按需加载翻译文件 |
| 可视化编排 | React Flow | DAG 编辑器首选，支持自定义节点 |
| 设计风格 | 简洁清晰，大面积留白，克制使用色彩 | 降低认知负荷，聚焦核心操作路径 |
| API 服务 | Node.js + Fastify | 高性能 HTTP 框架，适合 I/O 密集型场景 |
| Worker 服务 | Node.js + BullMQ Worker | 独立进程，与 API 服务解耦，可独立水平扩展 |
| MCP 网关 (可选) | Node.js + MCP Client SDK (mcp-cli) | 独立部署，连接外部 MCP Server，调用 MCP 工具 |
| 流程引擎 | BullMQ (Redis) | 轻量可靠的任务队列，支持延迟/重试/并发控制 |
| 数据库 | PostgreSQL 15+ | 稳定可靠，支持 JSONB 存储灵活 Schema |
| 缓存/队列 | Redis 7+ | 任务队列 + 缓存 + 实时状态 |
| 对象存储 | MinIO / S3 | 报告归档、大日志存储 |
| 沙箱执行 | JS: isolated-vm；Python: 子进程/容器沙箱 (gVisor/nsjail 或独立容器) | 安全的用户自定义脚本执行，JS 与 Python 隔离机制不同 |
| API 客户端 | axios (with keep-alive) | 成熟的 HTTP 客户端 |
| 数据库连接 | 连接池 (mysql2/pg/mssql/oracledb/mongodb/ioredis) | 支持多种数据库，连接池管理，加密存储凭据 |
| 认证 | JWT + RBAC | 无状态认证，权限控制 |
| 敏感信息 | 信封加密 (KMS) + 字段级加密 | 数据源密码 / 环境 secret / CI 凭据加密存储 |
| CI 执行 | 上报式: 采集 SDK + 唯一上报接口；自研 Runner: 常驻 Worker Pool (容器沙箱) + 可选 Job per build | 不依赖 Jenkins，协议自主可控；常驻模式秒级启动且依赖可复用 |
| Runner 沙箱 | 容器化 (rootless) + 资源限额 + 出站网络白名单；强隔离可选 K8s Job / gVisor | 执行用户任意脚本，须隔离 |
| 依赖加速 | 预装镜像 + 缓存卷 (pip/npm) + 依赖目录按 hash 缓存 + 镜像预热 | 消除每次重装依赖的冷启动开销 |
| 定时调度去重 | Redis 分布式锁 / BullMQ repeatable jobs | 多实例下避免 cron 重复触发 |
| 部署 | Docker Compose + K8s | 容器化部署，API 与 Worker 独立扩缩容 |

### 3.4 数据模型 (核心 ER)

> **统一执行索引**: `FlowExecution` / `SuiteExecution` / `IngestRun` / `PipelineRun` 各自独立，互不混用；「报告与监控」及统计下钻统一查 `ExecutionIndex`。

```
ExecutionIndex                 # 跨来源统一执行索引 (报告/统计/告警的唯一查询入口)
  - id: UUID                   # 统一执行 ID, 前端 canonical 路由用它
  - project_id: UUID (FK)
  - source_type: enum (flow/scenario/suite/ingest/runner)
  - source_run_id: UUID        # 指向对应明细表主键
  - display_name: string       # 流程名/套件名/任务名
  - status: enum (pending/running/success/failed/aborted/cancelled)
  - trigger_type: string
  - environment_id: UUID (可空; 仓库模式无平台环境概念)
  - total / passed / failed: int
  - pass_rate: float
  - started_at: timestamp
  - finished_at: timestamp
  - duration_ms: int
  # 由各来源执行在状态变更时同步写入(或用物化视图);
  # 索引: (project_id, started_at DESC), (project_id, status), (project_id, source_type)

User
  - id: UUID
  - name: string
  - email: string (unique)
  - password_hash: string
  - status: enum (active/disabled)
  - locale: string (zh-CN/en)
  - is_system_admin: boolean
  - created_at: timestamp

Role
  - id: UUID
  - name: string (project_admin/developer/viewer 等)
  - scope: enum (system/project)
  - permissions: JSONB (权限点集合)

UserProjectRole            # 用户 × 项目 × 角色 绑定
  - id: UUID
  - user_id: UUID (FK)
  - project_id: UUID (FK)
  - role_id: UUID (FK)
  - created_at: timestamp

Project
  - id: UUID
  - name: string
  - description: text
  - owner_id: UUID (FK -> User)
  - system_type: string          # 系统类型
  - status: enum (active/archived)
  - tags: string[]
  - created_at: timestamp
  - updated_at: timestamp

ProjectSetting                 # 项目级默认配置
  - project_id: UUID (FK, unique)
  - default_environment_id: UUID
  - default_timeout: int
  - default_retry_policy: JSONB
  - notification_channel_ids: UUID[]
  # 注: 不在此存 repository_id —— 仓库与项目是 1:1, 由 Repository.project_id (unique) 唯一确定,
  #     避免双源不一致; 也不存 default_executor (接口模式/仓库模式为并行范式, 非二选一开关)

AuditLog
  - id: UUID
  - user_id: UUID (FK)
  - project_id: UUID (FK, nullable)
  - action: string (create/update/delete/execute/member_change)
  - target_type: string
  - target_id: UUID
  - diff: JSONB (变更前后)
  - created_at: timestamp

Environment
  - id: UUID
  - project_id: UUID (FK)
  - name: string (dev/staging/prod)
  - variables: JSONB            # 普通变量
  - secrets: JSONB              # secret 变量, 字段级加密 (KMS 信封加密) 存储, 接口返回时脱敏

Endpoint
  - id: UUID
  - project_id: UUID (FK)
  - name: string
  - method: enum (GET/POST/PUT/DELETE/PATCH)
  - url: string
  - headers: JSONB
  - query_params: JSONB
  - body: JSONB
  - auth: JSONB
  - tags: string[]
  - created_at: timestamp

Flow
  - id: UUID
  - project_id: UUID (FK)
  - name: string
  - description: text
  - nodes: JSONB (DAG 节点定义)
  - edges: JSONB (DAG 边定义)
  - variables: JSONB
  - version: int
  - created_at: timestamp
  - updated_at: timestamp

FlowExecution
  - id: UUID
  - flow_id: UUID (FK)
  - environment_id: UUID (FK)
  - status: enum (pending/running/success/failed/cancelled)
  - trigger_type: enum (manual/scheduled/webhook/api/cli)
  # 注: 不设 executor_type —— FlowExecution 仅由内部 Worker 产生;
  #     跨来源统一聚合由 ExecutionIndex 承担 (见下)
  - input: JSONB
  - output: JSONB
  - node_results: JSONB                 # 大响应/大结果集快照落对象存储, 此处仅存摘要+引用
  - started_at: timestamp
  - finished_at: timestamp
  - duration_ms: int

Schedule
  - id: UUID
  - flow_id: UUID (FK)
  - cron: string
  - timezone: string
  - enabled: boolean
  - created_at: timestamp

MockRule
  - id: UUID
  - endpoint_id: UUID (FK)
  - response_status: int
  - response_headers: JSONB
  - response_body: JSONB
  - delay_ms: int
  - condition: JSONB (optional)

TestCase
  - id: UUID
  - project_id: UUID (FK)
  - endpoint_id: UUID (FK)
  - name: string
  - description: text
  - environment_id: UUID (FK)
  - assertions: JSONB (断言规则列表)
  - pre_script: text (前置脚本)
  - post_script: text (后置脚本)
  - parameters: JSONB (数据驱动参数组)
  - tags: string[]
  - created_at: timestamp
  - updated_at: timestamp

/* Scenario / ScenarioExecution 已随场景撤回（v1.5，见 2.8.2），编排统一由 Flow 承担。 */

DataSource
  - id: UUID
  - project_id: UUID (FK)
  - name: string
  - type: enum (mysql/postgresql/sqlserver/oracle/mongodb/redis)
  - host: string
  - port: int
  - database_name: string
  - username: string
  - password: string (加密存储)
  - ssl: JSONB (SSL 配置)
  - max_connections: int
  - connection_timeout: int
  - tags: string[]
  - created_at: timestamp
  - updated_at: timestamp

SqlDefinition               # v1.4 数据源下的命名 SQL 查询
  - id: UUID
  - project_id: UUID (FK)
  - datasource_id: UUID (FK)
  - name: string
  - description: text
  - sql: text
  - params: JSONB (参数声明: [{name, description, source}], source: 流程变量/前序节点输出/手动输入)
  - updated_at: timestamp

MCPServer
  - id: UUID
  - project_id: UUID (FK)
  - name: string
  - url: string (MCP Server 地址)
  - auth_type: enum (none/bearer/api_key)
  - auth_config: JSONB (认证配置)
  - tools: JSONB (自动发现的工具列表缓存)
  - last_tools_synced_at: timestamp   # 工具列表最近同步时间
  # 注: 连接状态(connected/disconnected)为运行时探活结果, 每个 Worker/Gateway 实例独立,
  #     不持久化为业务字段; 前端展示时实时探测
  - tags: string[]
  - created_at: timestamp
  - updated_at: timestamp

NotificationChannel            # 告警通知渠道
  - id: UUID
  - project_id: UUID (FK)
  - name: string
  - type: enum (webhook/email/wecom/dingtalk/slack)
  - config: JSONB (地址/密钥等, 敏感字段加密)
  - template: text (可选, 消息模板)
  - enabled: boolean

ScheduleRun                    # 调度触发记录 (排查漏跑/重复)
  - id: UUID
  - schedule_id: UUID (FK)
  - planned_at: timestamp
  - triggered_at: timestamp
  - execution_id: UUID (关联产生的执行)
  - status: enum (triggered/skipped/missed)

FlowVersion                    # 流程版本快照 (支持回滚/diff)
  - id: UUID
  - flow_id: UUID (FK)
  - version: int
  - nodes: JSONB
  - edges: JSONB
  - variables: JSONB
  - created_by: UUID (FK -> User)
  - created_at: timestamp

TestSuite                      # 测试套件 (批量执行)
  - id: UUID
  - project_id: UUID (FK)
  - name: string
  - selection_type: enum (manual/tag)
  - member_ids: JSONB (手动: 用例/流程 id 列表)
  - tag_filter: JSONB (动态: 标签匹配条件)
  - execution_strategy: JSONB (serial/parallel, concurrency, fail_fast)
  - created_at: timestamp

SuiteExecution
  - id: UUID
  - suite_id: UUID (FK)
  - environment_id: UUID (FK)
  - status: enum (pending/running/success/failed/cancelled)
  - item_results: JSONB (每个用例/流程结果, 大结果落对象存储存引用)
  - pass_rate: float
  - started_at: timestamp
  - finished_at: timestamp

Repository                     # 代码仓库 (首次上报绑定; 一项目仅一个)
  - id: UUID
  - project_id: UUID (FK, **unique** -> 一个项目只有一个仓库)
  - name: string
  - provider: enum (github/gitlab/other)
  - git_url: string              # 绑定后作为校验基准, 上报 git_url 不一致则拒绝
  - default_credential_id: UUID  # SCM 凭据, 供其下所有 CI 任务共享
  - tracking_branch: string      # 用于删除对账的跟踪分支, 默认 master
  - bound_at: timestamp
  - last_ingest_at: timestamp

ProjectApiToken                # 项目级 API Token (上报鉴权, UI 在「仓库用例」页管理)
  - id: UUID
  - project_id: UUID (FK)
  - name: string
  - token_hash: string          # 仅存哈希, 明文只在创建时展示一次
  - scopes: string[]            # 如 ["ingest:write"]
  - last_used_at: timestamp
  - revoked: boolean
  - created_at: timestamp

IngestRun                      # 上报式一次执行 (快照)
  - id: UUID
  - repository_id: UUID (FK)
  - commit_sha: string
  - branch: string
  - ci_run_id: string           # CI run id (如 GitHub Action)
  - ci_run_url: string
  - is_full_inventory: boolean  # 是否本次枚举了全部用例(决定是否做删除对账)
  - status: enum (running/success/failed)
  - summary: JSONB              # {passed, failed, total, covered_endpoints}
  - started_at: timestamp
  - finished_at: timestamp
  # 幂等键: (repository_id, commit_sha, ci_run_id)

IngestRecord                   # 上报的单次请求记录 (不可变日志)
  - id: UUID
  - ingest_run_id: UUID (FK)
  - case_key: string            # 稳定身份键
  - case_name: string
  - param_id: string            # 参数化子项 (可空)
  - tags: string[]
  - method: string
  - path_raw: string            # 实际请求路径
  - endpoint_id: UUID           # 归一模板匹配到的接口 (可空->未匹配)
  - status_code: int
  - latency_ms: int
  - passed: boolean
  - error: text
  - request_summary: JSONB      # 大 body 落对象存储存引用

RepoTestCase                   # 仓库用例 (合并后的用例清单, 挂在 系统→接口 树下)
  - id: UUID
  - project_id: UUID (FK)
  - endpoint_id: UUID (FK, 可空->未匹配区)
  - repository_id: UUID (FK)     # 冗余便于 join; 因 项目:仓库=1:1, 可由 project_id 推导
  - case_key: string
  - name: string
  - file: string
  - tags: string[]
  - status: enum (active/removed)      # removed=软删, 全量快照对账缺失
  - last_seen_commit: string
  - last_seen_run_id: UUID
  - last_result: enum (passed/failed/unknown)
  - first_seen_at: timestamp
  - updated_at: timestamp
  # 唯一键: (project_id, case_key)  [endpoint 归位, 支持跨 commit 稳定合并]

CoverageSnapshot               # 一次 run 的接口覆盖度快照 (可选, 用于趋势)
  - id: UUID
  - project_id: UUID (FK)
  - source_run_id: UUID         # IngestRun 或 PipelineRun
  - source_type: enum (ingest/ci)
  - commit_sha: string
  - total_endpoints: int
  - covered_endpoint_ids: UUID[]
  - coverage_rate: float
  - created_at: timestamp

Worker                         # 执行 Worker (自研 Runner); 主动外连注册
  - id: UUID
  - name: string
  - token_hash: string          # 注册鉴权, 仅存哈希
  - labels: string[]            # 亲和匹配 (如 内网可达/特定运行环境)
  - capacity: int               # executor 槽位数 = 并发上限
  - mode: enum (shared/isolated)# 常驻复用 / 一次性隔离
  - status: enum (online/offline/draining)   # 由心跳判定, 非人工维护
  - is_self_hosted: boolean     # 是否客户自托管 (内网)
  - last_heartbeat_at: timestamp
  - registered_at: timestamp

CITask                         # CI 任务 (由自研 Runner 执行); 一仓库可多任务
  - id: UUID
  - project_id: UUID (FK)
  - repository_id: UUID (FK)    # 归属仓库 (多任务 : 一仓库)
  - name: string
  - description: text
  - ref: string                 # 分支/tag/refspec; git_url 与凭据继承 Repository
  - image: string               # 运行镜像 (可指定自定义预装镜像)
  - isolation_mode: enum (shared/isolated)   # 默认 shared(常驻, 秒级启动)
  - steps: JSONB                # 执行脚本步骤 (用户自定义, 不限制)
  - env: JSONB                  # 普通环境变量
  - secret_refs: UUID[]         # 引用加密凭据, 注入时解密, 日志脱敏
  - parameters: JSONB           # 参数化执行定义 (string/choice/bool...)
  - cache_config: JSONB         # {paths[], key_from: requirements.txt hash}
  - report_config: JSONB        # {format: junit/allure, report_paths[]}
  - coverage_mode: enum (none/sdk/proxy)     # 零侵入 proxy 捕获 或 SDK 上报
  - timeout_sec: int
  - worker_labels: string[]     # 需要匹配的 Worker 标签
  - enabled: boolean
  - created_at: timestamp

PipelineRun                    # 一次 CI 任务执行 (本地读模型主表)
  - id: UUID
  - ci_task_id: UUID (FK)
  - project_id: UUID (FK)
  - worker_id: UUID (FK, 可空 -> 尚未被领取)
  - trigger_type: enum (manual/scheduled/webhook/api)
  - ref: string
  - commit_sha: string
  - params: JSONB
  - status: enum (queued/running/success/failed/aborted/timeout)
  - exit_code: int
  - result_summary: JSONB       # {passed, failed, skipped, duration}
  - console_log_ref: string     # 对象存储引用 (完整日志)
  - claimed_at: timestamp
  - started_at: timestamp
  - finished_at: timestamp
  # 幂等/防重: 同一 task 的排队任务按 (ci_task_id, commit_sha, params_hash) 可选去重

PipelineRunCase                # 解析报告得到的 case 级结果 (支撑详情与统计)
  - id: UUID
  - pipeline_run_id: UUID (FK)
  - suite_name: string
  - class_name: string
  - case_name: string
  - status: enum (passed/failed/skipped/error)
  - duration_ms: int
  - error_message: text
  - stack_trace_ref: string     # 长堆栈落对象存储
  - report_source: enum (junit/allure)

PipelineRunArtifact            # 产物/报告归档
  - id: UUID
  - pipeline_run_id: UUID (FK)
  - type: enum (console_log/junit/allure/archive)
  - object_ref: string          # 对象存储引用
  - size_bytes: bigint
  - created_at: timestamp

PipelineRunLogState            # 实时日志同步状态 (不存全文, 只存状态)
  - pipeline_run_id: UUID (FK, unique)
  - current_offset: bigint
  - stream_status: enum (streaming/finished/failed)
  - tail_preview: text          # 尾部预览, 供列表快速展示
  - last_chunk_at: timestamp

IngestCaseResult               # 上报式: 某次 run 中某个 case 的结果 (支撑历史回放与 diff)
  - id: UUID
  - ingest_run_id: UUID (FK)
  - case_key: string
  - param_id: string            # 参数化子项 (可空)
  - status: enum (passed/failed/skipped)
  - duration_ms: int
  - error: text
  # 唯一键: (ingest_run_id, case_key, param_id)
```

---

## 4. API 设计

### 4.1 RESTful API 规范

- 基础路径: `/api/v1`
- 响应格式:
  ```json
  {
    "code": 0,
    "message": "success",
    "data": {},
    "meta": { "page": 1, "page_size": 20, "total": 100 }
  }
  ```
- 错误码: 0 成功, 1001 参数错误, 1002 未认证, 1003 无权限, 2001 资源不存在, 5001 内部错误
- **列表通用 Query 参数**: `page`, `page_size`, `keyword`, `sort` (如 `sort=-created_at`), `tags`, 以及各资源特有过滤字段 (如 `method`, `status`)，所有列表接口统一支持
- **触发类接口**: 支持 `Idempotency-Key` 请求头做幂等去重

### 4.2 核心 API 列表

| Method | Path | 说明 |
|--------|------|------|
| POST | /auth/login | 登录 (返回 JWT) |
| POST | /auth/logout | 登出 |
| GET | /auth/me | 当前用户信息与权限 |
| GET | /users | 用户列表 (系统管理员) |
| POST | /users | 创建用户 |
| PUT | /users/:id | 更新用户 |
| GET | /projects | 项目列表 |
| POST | /projects | 创建项目 |
| GET | /projects/:id | 项目详情 |
| PUT | /projects/:id | 更新项目 |
| POST | /projects/:id/archive | 归档项目 |
| DELETE | /projects/:id | 删除项目 |
| GET | /projects/:id/settings | 项目设置 |
| PUT | /projects/:id/settings | 更新项目设置 |
| GET | /projects/:id/members | 成员列表 |
| POST | /projects/:id/members | 添加成员 (指定角色) |
| PUT | /projects/:id/members/:userId | 变更成员角色 |
| DELETE | /projects/:id/members/:userId | 移除成员 |
| GET | /projects/:id/audit-logs | 审计日志 |
| GET | /projects/:id/environments | 环境列表 |
| POST | /projects/:id/environments | 创建环境 |
| GET | /projects/:id/endpoints | 接口列表 |
| POST | /projects/:id/endpoints | 创建接口 |
| GET | /projects/:id/endpoints/:endpointId | 接口详情（定义 + 最近结果摘要） |
| PUT | /projects/:id/endpoints/:endpointId | 更新接口 |
| DELETE | /projects/:id/endpoints/:endpointId | 删除接口 |
| GET | /projects/:id/endpoints/:endpointId/executions | **该接口执行历史**（支持状态/环境/时间/分页） |
| POST | /projects/:id/endpoints/import | 批量导入 |
| GET | /projects/:id/flows | 流程列表 |
| POST | /projects/:id/flows | 创建流程 |
| GET | /flows/:id | 流程详情 |
| PUT | /flows/:id | 更新流程 |
| DELETE | /flows/:id | 删除流程 |
| POST | /flows/:id/execute | 执行流程 |
| GET | /flows/:id/executions | 执行历史 |
| GET | /executions/:id | 执行详情 |
| POST | /flows/:id/schedules | 创建定时任务 |
| GET | /flows/:id/schedules | 定时任务列表 |
| PUT | /schedules/:id | 更新定时任务 |
| DELETE | /schedules/:id | 删除定时任务 |
| GET | /projects/:id/mocks | Mock 规则列表 |
| POST | /projects/:id/mocks | 创建 Mock 规则 |
| GET | /reports/trends | 趋势数据 |
| GET | /projects/:id/executions | **统一执行列表** (查 ExecutionIndex, 支持 source_type/status/时间筛选) |
| GET | /executions/by-index/:indexId | **统一执行详情入口** (按 source_type 解析并重定向到对应明细) |
| GET | /dashboard | **全局数据看板**（按可见项目、项目/标签/时间筛选聚合） |
| GET | /system/preferences | 当前用户个人偏好（主题、语言、恢复上次页面） |
| PUT | /system/preferences | 更新当前用户个人偏好 |
| GET | /projects/:id/test-cases | 接口用例列表 |
| POST | /projects/:id/test-cases | 创建接口用例 |
| GET | /test-cases/:id | 接口用例详情 |
| PUT | /test-cases/:id | 更新接口用例 |
| DELETE | /test-cases/:id | 删除接口用例 |
| POST | /test-cases/:id/execute | 执行接口用例 |
| GET | /projects/:id/data-sources | 数据源列表 |
| POST | /projects/:id/data-sources | 创建数据源 |
| GET | /data-sources/:id | 数据源详情 |
| PUT | /data-sources/:id | 更新数据源 |
| DELETE | /data-sources/:id | 删除数据源 |
| POST | /data-sources/:id/test | 测试数据源连接 |
| POST | /data-sources/:id/query | 执行 SQL 查询 |
| GET | /data-sources/:id/sql-definitions | SQL 定义列表 |   # v1.4
| POST | /data-sources/:id/sql-definitions | 创建 SQL 定义 |   # v1.4
| PUT | /sql-definitions/:id | 更新 SQL 定义 |   # v1.4
| DELETE | /sql-definitions/:id | 删除 SQL 定义 |   # v1.4
| POST | /sql-definitions/:id/test | 测试运行 SQL 定义 (填参数预览结果) |   # v1.4
| GET | /projects/:id/mcp/servers | MCP Server 列表 (项目级) |
| POST | /projects/:id/mcp/servers | 注册 MCP Server |
| GET | /mcp/servers/:id | MCP Server 详情 |
| PUT | /mcp/servers/:id | 更新 MCP Server |
| DELETE | /mcp/servers/:id | 删除 MCP Server |
| GET | /mcp/servers/:id/tools | 获取 MCP Server 工具列表 (含实时连接状态) |
| POST | /mcp/servers/:id/execute | 调用 MCP 工具 |
| GET | /projects/:id/test-suites | 测试套件列表 |
| POST | /projects/:id/test-suites | 创建测试套件 |
| POST | /test-suites/:id/execute | 执行测试套件 |
| GET | /test-suites/:id/executions | 套件执行历史 |
| GET | /projects/:id/notification-channels | 通知渠道列表 |
| POST | /projects/:id/notification-channels | 创建通知渠道 |
| POST | /ingest | **唯一上报接口** (API Token 鉴权; 含 run 元数据+inventory+records; 幂等) |
| GET | /projects/:id/repo-cases/tree | 仓库用例树 (系统→接口→用例) |
| GET | /endpoints/:id/repo-cases | 某接口下的仓库用例 |
| GET | /repo-cases/:id | 仓库用例详情 (含上报历史) |
| GET | /projects/:id/coverage | 接口覆盖概览 (已覆盖/未覆盖清单/趋势) |
| GET | /projects/:id/ingest-tokens | 上报 Token 列表 (仓库用例页管理) |
| POST | /projects/:id/ingest-tokens | 生成上报 Token (明文仅返回一次) |
| DELETE | /ingest-tokens/:id | 吊销上报 Token |
| GET | /projects/:id/repository | 查看本项目唯一仓库 (未绑定返回空) |
| PUT | /projects/:id/repository | 绑定/更换仓库 (换绑需项目管理员显式确认) |
| DELETE | /projects/:id/repository | 解绑仓库 |
| GET | /workers | Worker 列表 (在线状态/负载/labels，系统管理员) |
| POST | /workers | 生成 Worker 注册 Token |
| DELETE | /workers/:id | 下线/移除 Worker |
| GET | /projects/:id/ci-tasks | CI 任务列表 (即该仓库下的全部任务) |
| POST | /projects/:id/ci-tasks | 创建 CI 任务 |
| PUT | /ci-tasks/:id | 更新 CI 任务 |
| DELETE | /ci-tasks/:id | 删除 CI 任务 |
| POST | /ci-tasks/:id/trigger | 触发执行 (支持 Idempotency-Key) |
| GET | /ci-tasks/:id/runs | 执行历史 |
| GET | /pipeline-runs/:id | 执行详情 (含归一化 case 结果) |
| GET | /pipeline-runs/:id/logs | 历史完整日志 (对象存储，支持 offset 分段) |
| GET | /pipeline-runs/:id/logs/stream | **运行中实时日志 (SSE)** |
| POST | /pipeline-runs/:id/cancel | 取消执行 (经心跳下发 cancel 指令) |

**Worker 协议** (Worker 主动外连，`worker_token` 鉴权，不走用户 JWT)

| Method | Path | 说明 |
|--------|------|------|
| POST | /worker/register | 注册并上报 labels/capacity，返回 worker_id |
| GET | /worker/jobs/claim | 长轮询领取任务，返回 JobSpec |
| POST | /worker/jobs/:id/heartbeat | 心跳续约 (响应可下发 cancel) |
| POST | /worker/jobs/:id/logs | 增量日志上报 `{offset, chunk}` |
| POST | /worker/jobs/:id/artifacts | 申请 pre-signed URL 并上传报告/产物 |
| POST | /worker/jobs/:id/complete | 完成回调 `{status, exit_code, summary}` |

---

## 5. 非功能需求

### 5.1 性能

- **API 服务**: 无状态，支持水平扩展，单实例 API 响应 P99 < 50ms
- **Worker 服务**: 单实例默认 10 并发，支持水平扩展，总吞吐 = 实例数 x 每实例并发数
- 报告查询 P99 < 500ms (统一走 `ExecutionIndex`，避免跨多表 union)
- API 服务与 Worker 服务独立扩缩容，执行负载不影响 API 响应
- **Runner 冷启动**: 常驻模式 (`shared`) 从 claim 到开始执行 < 5s；强隔离模式 (`isolated`) 在镜像已预热且依赖缓存命中时 < 60s
- **上报接口**: 单次 run 支持 ≥ 5000 条 records，超出需分批；请求体上限与限流阈值可配置，超限 payload 落对象存储
- **实时日志**: 单实例支持 ≥ 500 条并发 SSE 连接；增量推送延迟 < 2s；单次执行日志超阈值 (如 50MB) 时截断并提示下载完整日志
- **覆盖率计算**: 单项目 1000 endpoints × 5000 用例的覆盖统计 P99 < 3s (预计算 + 快照)

### 5.2 可用性

- 系统可用性 99.9%
- 任务不丢失 (Redis 持久化 + 数据库持久化)
- 执行记录永久保存 (可归档到对象存储)
- **Runner 容错**: 心跳超时 (默认 90s) 未续约的任务判定 `aborted` 并可重新入队；**退出码落盘**保证 Worker 重启后可判定任务真实状态，不出现永久 `running`
- **Worker 优雅下线**: `draining` 状态不再领新任务，等待在跑任务结束后退出

### 5.3 安全

- 所有 API 需认证 (JWT)
- RBAC 权限模型: 系统管理员 / 项目管理员 / 开发者 / 只读用户，通过 `User` / `Role` / `UserProjectRole` 落地 (见 2.0.2 与数据模型)
- 用户脚本沙箱执行，禁止文件系统/网络访问 (JS: isolated-vm；Python: 容器/子进程沙箱)
- 敏感变量加密存储: 环境 secret / 数据源密码 / CI·Git 凭据均字段级加密 (KMS 信封加密)，接口返回脱敏
- Webhook / CI 回调 HMAC 签名校验 + 防重放；触发接口支持幂等键
- 审计日志: 记录所有变更操作 (AuditLog)

### 5.4 可扩展性

- 插件机制: 支持自定义节点类型
- Webhook 输出: 流程执行结果可推送到外部系统
- OpenAPI 导出: 平台自身 API 可导出为 OpenAPI 规范
- **MCP 扩展**: MCP Gateway 作为独立可选组件，通过标准 MCP 协议连接外部 MCP Server，可随时接入或替换 MCP 工具提供商

---

## 6. 实施路线图

| 阶段 | 内容 | 预估周期 |
|------|------|---------|
| P0 (MVP) | **用户/登录/项目与成员管理 (RBAC 基座)** + 接口管理 + 环境管理 + 单接口测试 + 基础报告 + 多语言框架 | 6 周 |
| P1 | 流程编排 (可视化 DAG) + 变量传递 + 断言 + 接口用例管理 + **ExecutionIndex 统一执行索引 (地基, 须先立)** | 7 周 |
| P2 | 流程节点补全 + 测试套件批量执行 + 数据库源连接 + Mock 服务 | 6 周 |
| P3 | 定时调度 (分布式去重) + Webhook 触发 (HMAC) + 告警通知 + 趋势分析 | 3 周 |
| P4 | **仓库模式: 仓库用例(上报式,「系统→接口→用例」树+唯一上报接口+合并/去重/删除)** | 3 周 |
| P4.5 | **自研 Runner: Worker 注册·心跳·claim 协议 + 容器沙箱 + SSH 拉取 + 实时日志流 + 报告归一化 + 依赖缓存** | 4 周 |
| P5 | MCP Gateway + MCP 工具节点 + MCP Server 管理 + 审计日志完善 | 4 周 |
| P6 | 性能优化 + 插件机制 + 版本历史(diff/回滚) + 多语言完善 | 3 周 |

---

## 7. 附录

### 7.1 参考项目

- Hoppscotch: 开源 API 开发平台
- Postman: API 测试与协作平台
- n8n: 开源工作流自动化
- Temporal: 分布式工作流引擎

### 7.2 术语表

| 术语 | 说明 |
|------|------|
| DAG | 有向无环图，流程编排的底层模型 |
| Flow | 流程，由多个节点组成的自动化任务 |
| Endpoint | 接口定义，包含 URL、方法、参数等 |
| Environment | 环境，包含变量集合 |
| Mock | 模拟服务，返回预设响应 |
| Sandbox | 沙箱，安全执行用户自定义脚本 |
| MCP | Model Context Protocol，外部工具/服务接入标准协议 |
| MCP Server | 提供 MCP 协议的工具服务，可被平台发现和调用 |
| MCP Gateway | 平台内部的 MCP 客户端网关，负责连接和调用外部 MCP Server |
| TestSuite | 测试套件，一组接口用例与流程的集合，支持批量执行 |
| Repository | 上报首次自动登记的代码仓库 |
| 仓库用例 | 来自上报、挂在「系统→接口→用例」树下的用例 (RepoTestCase) |
| 上报式 | 开发者用 SDK 封装请求，运行后经唯一上报接口上报平台 |
| is_full_inventory | 上报是否枚举全部用例，决定是否做删除对账 |
| IngestRun / IngestRecord | 一次上报及其单请求不可变记录 |
| 接口覆盖 | 某接口下是否存在仓库用例；分母=系统已登记接口 |
| CI 任务 (CITask) | 由平台自研 Runner 执行的任务定义 (脚本步骤/镜像/缓存/报告路径)，一仓库可多任务 |
| Runner Worker | 平台自研执行 Worker，主动外连领取任务，沙箱内 git clone + 跑脚本 + 推日志 + 传报告 |
| 常驻 / 强隔离模式 | `shared` 常驻复用 (秒级启动、依赖缓存复用，默认) vs `isolated` 每次全新环境 |
| ExecutionIndex | 跨来源统一执行索引，报告监控与统计下钻的唯一查询入口 |
| PipelineRun | 一次 CI 任务构建执行 |
| RBAC | 基于角色的访问控制，用户×项目×角色三元绑定 |
